# Mapa de Migrations — E-commerce Criatório Capuã / VLUMA

**Última atualização:** 01/08/2026
**Propósito:** referência rápida de todas as migrations em `supabase/migrations/`, o que cada uma faz e se já foi aplicada no banco. Detalhe completo de cada decisão de produto por trás de uma migration está em `docs/ESCOPO_PROJETO.md`; este documento é só o mapa técnico objetivo.

> Migrations são aplicadas manualmente pelo usuário via SQL Editor do Supabase (nunca pelo assistente) — ver `ESCOPO_PROJETO.md` §6.

---

## O "buraco" do número 014 — explicado

**Não é um erro nem um arquivo perdido.** Não existe `014_*.sql` em `supabase/migrations/` de propósito. Histórico:

1. Em 30/07/2026, `013_products_tenant_default.sql` e `014_products_codigo.sql` foram criadas (feature Código do Produto).
2. Antes de aplicar qualquer uma das duas, decidimos tratar primeiro o isolamento total por tenant (decisão #21, `ESCOPO_PROJETO.md`) — um gap de segurança mais urgente e estruturalmente anterior.
3. Como o isolamento precisava logicamente rodar **antes** das migrations de Produtos, ele assumiu o número `013` (`013_rls_tenant_isolation.sql`), e as duas migrations de Produtos foram **renumeradas** via `git mv`: `013_products_tenant_default.sql` → `015_products_tenant_default.sql`, `014_products_codigo.sql` → `016_products_codigo.sql`.
4. Resultado: o número `014` nunca chegou a existir como arquivo — foi "pulado" na renumeração, não removido. Nenhum conteúdo foi perdido; a rastreabilidade completa dessa renumeração (o quê, por quê) já está registrada em `ESCOPO_PROJETO.md` §0 (regra de rastreabilidade) e §6 (nota de rastreabilidade da tabela de migrations).

**Se você (ou uma sessão futura) vir a numeração pular de `013` para `015` e estranhar: é isso — não procure um `014_*.sql` desaparecido, ele nunca existiu.**

**Atualização de 31/07/2026 — o buraco foi preenchido, mas por acaso, não por engano:** ao testar o isolamento da `013` com o tenant sintético (`supabase/tests/isolamento_test.sql`), foi encontrado um SEGUNDO vazamento de leitura cross-tenant (as policies `*_select_public` valiam com a mesma condição pra `anon` e `authenticated`, deixando a branch pública — sem filtro de tenant — vazar leitura pra qualquer sessão autenticada). A correção virou uma migration nova, e como o número `014` estava livre (pela renumeração explicada acima), ela recebeu esse número: `014_rls_select_anon_authenticated_split.sql`. É uma migration **nova e distinta**, não um reaproveitamento por engano do número vago — só uma coincidência conveniente do número estar disponível exatamente quando surgiu a próxima migration a criar. Ver decisão #21 em `ESCOPO_PROJETO.md` §2 e §5 para o histórico completo do vazamento e da correção.

---

## Tabela completa (001–018)

| # | Arquivo | O que faz | Status |
|---|---|---|---|
| 001 | `001_keepalive.sql` | Tabela singleton `keepalive_ping`, pingada por GitHub Action a cada 8h pra manter o projeto Supabase free tier ativo. | ✅ Aplicada |
| 002 | `002_core.sql` | Núcleo: `tenants`, `profiles`, `customers`, `store_settings`. Funções `set_updated_at()`, `current_tenant_id()`, `is_admin()`, `is_staff()`. | ✅ Aplicada |
| 003 | `003_catalog.sql` | Catálogo v1: `categories`/`subcategories` (2 níveis fixos), `subcategory_fields`, `products` (com preço/estoque direto na tabela), `product_field_values`, `product_images`, `product_price_history`. **Substituída pela v2 na `009`.** | ✅ Aplicada (estrutura depois alterada pela `009`) |
| 004 | `004_rls.sql` | Habilita RLS e cria as policies originais de todas as tabelas de `002`/`003` (leitura pública + escrita staff, sem filtro de tenant — gap corrigido só na `013`). | ✅ Aplicada |
| 005 | `005_delivery_cities.sql` | Tabela `delivery_cities` (cidades de entrega) + RLS própria. Primeiro CRUD administrativo do painel. | ✅ Aplicada |
| 006 | `006_handle_new_user.sql` | Trigger `handle_new_user()` em `auth.users`: direciona signup pra `profiles` (staff) ou `customers` (cliente) conforme metadata. Hardcoda tenant `'capua'` (ver nota em `ESCOPO_PROJETO.md` §2 — fora de escopo do isolamento, tema da Fase SaaS). | ✅ Aplicada |
| 007 | `007_store_settings_fechamento.sql` | Dois níveis independentes de fechamento da loja: `loja_aberta` e `pedidos_abertos` em `store_settings`, com mensagens amigáveis. | ✅ Aplicada |
| 008 | `008_delivery_cities_tenant_default.sql` | `delivery_cities.tenant_id` ganha `DEFAULT current_tenant_id()`. | ✅ Aplicada |
| 009 | `009_catalog_v2.sql` | Catálogo v2: `categories` vira árvore (`parent_id`), `category_attributes` substitui `subcategory_fields`, `product_variants` (SKU) nasce e assume preço/estoque/promoção, `products` perde essas colunas, view `products_com_status` (status derivado), `product_field_values`→`product_attribute_values`. Dropa `subcategories`/`subcategory_fields`. | ✅ Aplicada |
| 010 | `010_categories_tenant_default.sql` | `categories.tenant_id` ganha `DEFAULT current_tenant_id()`. | ✅ Aplicada |
| 011 | `011_categories_inativado_em_cascata.sql` | `categories.inativado_em_cascata` + RPC `set_category_ativo_cascade` (cascata atômica de inativação/reativação em subárvore). **RPC corrigida na `013`** (gap de tenant). | ✅ Aplicada (RPC revisada pela `013` — **atualização de 31/07/2026: `013` também já aplicada**, correção em vigor) |
| 012 | `012_category_attributes_tenant_default.sql` | `category_attributes.tenant_id` ganha `DEFAULT current_tenant_id()`. | ✅ Aplicada |
| 013 | `013_rls_tenant_isolation.sql` | **Isolamento total por tenant** (decisão #21): todas as policies de leitura privilegiada/escrita staff (tenants, profiles, customers, store_settings, categories, category_attributes, products, product_variants, product_attribute_values, product_images, product_price_history, delivery_cities) ganham `and tenant_id = current_tenant_id()`. Corrige a RPC `set_category_ativo_cascade` (mesmo gap, já em produção). | ✅ **Aplicada** em 31/07/2026 |
| 014 | `014_rls_select_anon_authenticated_split.sql` | **Fecha o SEGUNDO vazamento** (confirmado por teste real com o tenant sintético, depois da `013` aplicada): as policies `*_select_public` valiam com a mesma condição pra `anon` e `authenticated`, e a branch pública (`ativo = true`/`using(true)`, sem tenant) vazava leitura cross-tenant pra qualquer sessão autenticada — staff **e** cliente logado. Separa em `_select_anon` (mantém aberta, pra vitrine pública) e `_select_authenticated` (sempre `tenant_id = current_tenant_id()`) nas 8 tabelas afetadas: categories, category_attributes, products, product_variants, delivery_cities, store_settings, product_attribute_values, product_images. **Não é o "buraco" original preenchido por engano — é migration nova, ver nota acima.** | ✅ **Aplicada e validada** em 31/07/2026 — reteste do isolamento confirmou `categorias_do_caua_que_vejo = 0` (era 6 antes da correção) |
| 015 | `015_products_tenant_default.sql` | `products.tenant_id` e `product_variants.tenant_id` ganham `DEFAULT current_tenant_id()` (mesmo gap de `008`/`010`/`012`, nunca corrigido desde a `003`/`009`). Renumerada de `013` (nome original, 30/07/2026). | ✅ **Aplicada e validada** em 31/07/2026 |
| 016 | `016_products_codigo.sql` | Feature **Código do Produto** (decisão #18): `categories.prefixo_codigo`, `products.codigo`/`codigo_visivel`, tabela `category_code_sequences`, RPCs `gerar_codigo_produto` e `criar_produto_com_variacoes` (criação atômica produto+variações). Renumerada de `014` (nome original, 30/07/2026); policy de `category_code_sequences` já nasce com filtro de tenant embutido. Corrigida em 31/07/2026 (`CREATE OR REPLACE VIEW` → `DROP`+`CREATE` em `products_com_status`, ver commit `8cf3593`). | ✅ **Aplicada e validada** em 31/07/2026 — colunas/tabela/RPCs conferidas presentes no banco; `npm run build` (typecheck) passou depois de `src/types/database.ts` atualizado |
| 017 | `017_atualizar_produto_com_variacoes.sql` | Edição atômica de produto+variações (RPC `atualizar_produto_com_variacoes`, simétrica à `criar_produto_com_variacoes`) — `codigo` nunca entra no payload (imutável), variações removidas viram soft delete (`ativo=false`). Também revisa `criar_produto_com_variacoes` (016, **já aplicada**) via `create or replace` — não é descarte, é extensão de comportamento (SKU automático), rastreabilidade no cabeçalho do arquivo. Novas funções auxiliares `abreviar_rotulo`, `gerar_sku_variacao`, `mensagem_erro_variacao`. | ✅ **Aplicada e validada** em 01/08/2026 — revisada e aprovada pelo usuário antes da aplicação; testada via edição de produto com Chromium (contra `localhost`, re-teste na URL pública ainda pendente — ver `ESCOPO_PROJETO.md` §0) |
| 018 | `018_products_com_status_security_invoker.sql` | **Fecha o TERCEIRO vazamento cross-tenant** (decisão #21), desta vez numa **view**, não numa policy: `products_com_status` (009, redefinida na 016) foi criada sem `security_invoker = true`, então roda com os privilégios de quem a criou, não de quem consulta — ignora o RLS de `products`/`categories`/`product_variants` por completo, mesmo com `013`/`014` corretas nas tabelas. Efeito real: staff do Cauã via produtos do tenant sintético `_teste_isolamento` na listagem `/painel/produtos` (que consulta a view), enquanto a edição (lê `products` direto) isolava certo — foi essa discrepância que revelou o bug. Diagnóstico confirmado: `select reloptions from pg_class where relname = 'products_com_status'` retornou `null`. Correção: `alter view ... set (security_invoker = true)`. Levantamento em toda a árvore de migrations confirma que é a **única view** do projeto — nenhuma outra a corrigir. | ✅ **Aplicada e validada** em 01/08/2026 — `reloptions` reconferido no banco (`security_invoker=true`); reteste de isolamento com o canário, desta vez consultando a view diretamente, confirmou `de_outro_tenant=0` |
| 019 | `019_unidades_venda.sql` | **Cadastro de Unidades de Venda** (Melhoria 3, decisão do usuário em 01/08/2026): tabela nova `unidades_venda` (por tenant, RLS padrão pós-`014`) + seed inicial (`Unidade`, `Dupla`, `Cardume`, `Kg`, `Litro`, `Pacote`) por tenant já existente. Adiciona `products.unidade_venda_id` (nullable), faz backfill a partir do texto livre antigo (`products.unidade_venda`, case-insensitive, sem perder o que o lojista já tinha digitado), aplica fallback pra "Unidade" em qualquer produto que não casou, e só então torna a coluna `not null`. Revisa `criar_produto_com_variacoes`/`atualizar_produto_com_variacoes` (`create or replace`) trocando `unidade_venda` (text) por `unidade_venda_id` (uuid), validando que a unidade pertence ao tenant (mesmo padrão do `category_id`). `DROP` da coluna antiga de propósito **fora** desta migration — ver `020`. | ✅ **Aplicada** em 01/08/2026. **⚠️ Incidente, mesmo dia**: o frontend não tinha sido atualizado antes da aplicação — toda tentativa de criar/editar produto em produção passou a falhar com "Unidade de venda não encontrada." até o fix do frontend ser deployado (ver nota de incidente em `ESCOPO_PROJETO.md` §0) |
| 020 | `020_drop_products_unidade_venda_text.sql` | Remove a coluna antiga `products.unidade_venda` (texto livre), depois que `unidade_venda_id` (019) já está em uso. Separada de propósito da 019 — só aplicar depois de confirmar que a seleção de unidade funciona ponta a ponta. **Irreversível**: o texto livre original de cada produto é perdido para sempre. | ✅ **Aplicada com sucesso** em 01/08/2026 (2ª tentativa). **1ª tentativa falhou**: `products_com_status` usa `p.*`, que trava a lista de colunas no momento da criação da view — o Postgres recusou o `DROP COLUMN` com `2BP01 cannot drop column ... view products_com_status depends on it`. Corrigida no mesmo arquivo (nunca tinha sido aplicada com sucesso, sem necessidade de renumerar): passou a fazer `DROP VIEW` + `DROP COLUMN` + `CREATE VIEW ... WITH (security_invoker = true)` na mesma migration. Reconfirmado ao vivo contra a URL pública: listagem/criação de produto funcionando normalmente sem a coluna antiga |

---

| 021 | `021_stock_movements.sql` | **Módulo de Estoque** (decisão de produto fechada em 01/08/2026, reaproveitando a lição do projeto Almox da VLUMA: saldo atualizado na aplicação/RPC, nunca por trigger): tabela `stock_movements` (ledger imutável — `quantidade` é delta assinado, `saldo_anterior`/`saldo_novo` são snapshots, sem `updated_at`, sem policy de `UPDATE`/`DELETE`), enum `stock_movement_type`, RPC `registrar_movimentacao_estoque` (`FOR UPDATE` trava a linha antes de calcular, bloqueia saldo negativo, amarra tipo↔sinal via `check`, aceita delta direto ou "novo saldo desejado" pra Ajuste). `revoke update(saldo_estoque) on product_variants from authenticated` — só a RPC (SECURITY DEFINER) consegue mexer nesse campo dali pra frente. Backfill: movimentação `inventario` retroativa pra variações que já tinham saldo antes do módulo existir | ✅ **Aplicada** em 01/08/2026 |
| 022 | `022_estoque_inicial_produto.sql` | Integra o módulo de Estoque às RPCs de produto: `criar_produto_com_variacoes`/`atualizar_produto_com_variacoes` (`create or replace`) passam a gravar `saldo_estoque = 0` sempre no INSERT da variação, e se `estoque_inicial` (payload renomeado de `saldo_estoque`) veio preenchido, uma função auxiliar nova (`registrar_estoque_inicial_variacao`) gera a movimentação `inventario` correspondente — tanto na criação do produto quanto numa variação nova adicionada durante uma edição. Variação **existente** numa edição nunca mais tem `saldo_estoque` tocado (nem lido, nem escrito) | ✅ **Aplicada** em 01/08/2026 |
| 023 | `023_product_images_variant_e_storage.sql` | **Produtos Etapa 3 (Imagens)**: estende `product_images` (já existia desde a `003`, isolada por tenant desde `013`/`014`, nunca usada até agora) com `variant_id` (nullable — imagem de variação, opcional), `check` impedindo capa em imagem de variação, `DEFAULT current_tenant_id()` (gap que essa tabela nunca tinha corrigido, diferente de toda outra tabela de domínio), RPC `definir_imagem_principal` (troca de capa atômica). Cria o bucket `product-images` (público pra leitura, 5MB, JPG/PNG/WebP) e 3 policies de `storage.objects` (insert/update/delete, isoladas por tenant via o primeiro segmento do path do arquivo) — **de propósito sem policy de select**: uma versão revisada antes de aplicar usava `for all` (cobria select também), o que descasava leitura anônima (bypassa RLS via bucket público) vs. autenticada (seria avaliada pela policy e barrada) — mesma classe de bug da migration `014`, corrigida antes de aplicar | ✅ **Aplicada e testada com Chromium real** em 02–03/08/2026 — isolamento por tenant confirmado no `storage_path` de imagens reais, leitura pública confirmada com requisição HTTP isolada (200), limite de 10 imagens/produto testado até o fim (11ª recusada), exclusão real confirmada |
| 024 | `024_product_code_por_nome.sql` | **Código do produto — modo automático por nome** (decisão #25, revisão da #18): tabela nova `product_code_sequences` (contador de sequência chaveado por `(tenant_id, prefixo)`, não por categoria nem por produto — dois produtos com nomes diferentes que derivam o mesmo prefixo dividem a sequência), RLS staff-only tenant-scoped no mesmo padrão de `category_code_sequences`. RPC nova `gerar_codigo_produto_por_prefixo(p_prefixo)` — recebe o prefixo **já derivado em TypeScript** (`derivarPrefixo()`, `src/lib/produto-codigo.ts`, decisão #24, reaproveitada sem duplicar a regra em SQL), só faz a reserva atômica do próximo número (`insert ... on conflict (tenant_id, prefixo) do update`). **Nenhuma mudança** em `criar_produto_com_variacoes`/`atualizar_produto_com_variacoes` (016/017) — as duas já recebiam o código pronto como string, indiferente de como foi decidido; o Route Handler que chama a RPC nova antes de criar o produto, exatamente como já fazia com `gerar_codigo_produto` (categoria) desde a `016`. Opção A (RPC de reserva separada) escolhida sobre Opção B (fundir tudo numa RPC atômica só) por manter as RPCs de criação/edição já testadas intocadas — trade-off aceito: mesmo risco pré-existente do modo categoria (número "queimado" se a criação falhar depois da reserva) | ✅ **Aplicada pelo usuário em 04/08/2026, testada com Chromium real contra a URL pública em 05/08/2026** — "Ração de Fundo Pyotar" → `RAFP-0001`; "Ração Filhotes Premium" (nome diferente, mesmo prefixo derivado) → `RAFP-0002`, confirmando a sequência por prefixo; `product_code_sequences.ultimo_numero=2` reconferido direto no banco. Ver `ESCOPO_PROJETO.md` §4 (tabela "Concluídas") para o detalhe completo do teste, incluindo o regressivo do módulo de Produtos inteiro |

| 025 | `025_product_attribute_values_caracteristicas.sql` | **Produtos Etapa 2 (Características por produto)**: `product_attribute_values.tenant_id` ganha `DEFAULT current_tenant_id()` (mesmo gap fechado em `categories`/`products`/`product_variants`/`product_images` nas `010`/`012`/`015`/`023`). `criar_produto_com_variacoes`/`atualizar_produto_com_variacoes` ganham parâmetro novo `p_caracteristicas jsonb default '[]'::jsonb` — valida obrigatoriedade server-side contra as características **ativas** da categoria sendo salva, sincroniza os valores (edição: apaga o que não pertence à categoria atual — decisão de produto, sem órfão invisível — e faz upsert dos valores atuais; característica inativada na mesma categoria preserva o valor). Confirmado por diff automatizado contra a versão anterior (`022`) que nenhuma das 9 camadas existentes das duas RPCs foi alterada, só inserções | ⚠️→✅ **Aplicada pelo usuário em 05/08/2026, com efeito colateral corrigido pela `026`** (ver linha abaixo). ✅ **Frontend implementado e testado com Chromium real contra a URL pública em 06/08/2026** — ver `ESCOPO_PROJETO.md` §4 (tabela "Concluídas") para o detalhe completo do teste, incluindo os 2 bugs de frontend encontrados e corrigidos no processo |
| 026 | `026_fix_overload_produto_variacoes.sql` | **Corrige o efeito colateral da `025`**: `drop function if exists` das duas versões **antigas** (assinatura sem `p_caracteristicas` — `criar_produto_com_variacoes(jsonb, jsonb)` e `atualizar_produto_com_variacoes(uuid, jsonb, jsonb)`), mantendo só as novas. Assinaturas antigas confirmadas contra o histórico de migrations (nunca mudaram de `016` até `022`) e contra `pg_proc` ao vivo (reportado pelo usuário). Nota adicionada no cabeçalho da `025` (comentário, sem mudar SQL executável) apontando pra esta correção, pro caso de alguém reaplicar a sequência `001`–`026` do zero num banco novo | ✅ **Aplicada pelo usuário em 06/08/2026** — `pg_proc` reconferido pelo usuário: só uma versão de cada RPC, com 3 e 4 argumentos respectivamente |
| 027 | `027_minimos_variacao_e_sku_por_palavra.sql` | **Lote de ajustes de UX, itens 1 e 3 (07/08/2026)**: `product_variants.quantidade_minima` renomeada pra `quantidade_minima_estoque` (preserva dado — alerta de reposição, módulo de Estoque) + nova coluna `quantidade_minima_venda` (default 1, mínimo de compra do cliente, checkout futuro sem uso ainda). `abreviar_rotulo()` reimplementada com a mesma regra de `derivarPrefixo()` (decisão #24) em vez do corte cru dos 4 primeiros caracteres — depende da extensão `unaccent`. As duas RPCs de produto (`criar_produto_com_variacoes`/`atualizar_produto_com_variacoes`) **não mudam de assinatura** (diff automatizado contra a `025` confirmou — zero risco de overload como na `025`/`026`) | ⚠️→✅ **Aplicada pelo usuário em 07/08/2026 antes do frontend/backend acompanhar** — efeito real: **página de Estoque quebrada em produção** (coluna inexistente) e mínimo de estoque gravando sempre `1` nos produtos criados nesse intervalo (payload com a chave antiga, RPC caindo no default). Corrigido na hora, mesmo dia: todos os pontos de código atualizados (Route Handlers, hooks, tipos, formulário) e **testado com Chromium real contra a URL pública** — Estoque voltou a carregar, produto de teste com `quantidade_minima_estoque=3`/`quantidade_minima_venda=2` confirmados no banco, SKU `TEFU-0001-OSAL` confirmando a nova regra por palavra ("Oscar Albino" → `OSAL`). Ver `ESCOPO_PROJETO.md` §4 (tabela "Concluídas") pro detalhe completo e a lição de processo registrada |
| 028 | `028_vitrine_fase0_tenant_por_host.sql` | **Vitrine Fase 0 — fundação de tenant público ("Opção C")**: tabela `tenant_domains` (host → tenant, sem RLS permissiva — só via RPC), RPC `resolve_tenant_by_host` (sem fallback), quatro RPCs públicas `SECURITY DEFINER` (`get_public_store_settings`/`get_public_categories`/`get_public_products`/`get_public_product_detail`, filtro de tenant resolvido dentro de cada uma a partir do slug), e drop das 8 policies `*_select_anon` tenant-blind da `014` (leitura anônima passa a ser só via RPC). Bloco de seed comentado no final (associar `ecommercecauahml.vluma.com.br`/`testeecommerce.vluma.com.br` aos tenants `capua`/`_teste_isolamento`, com o toggle temporário de `ativo` do canário) — não roda automático, é pra ser executado manualmente só depois da migration aplicada | 📐→✅ **Criada em 10/08/2026, aplicada pelo usuário e confirmada em uso real desde 12/08/2026** (Vitrine Fase 1 Etapa 2 — ver `ESCOPO_PROJETO.md` §0 item 24 — consome as 4 RPCs desta migration em produção/HML). Esta linha ficou desatualizada ("NÃO aplicada") por uma sessão não ter voltado pra corrigir o status depois da aplicação; corrigido agora, 13/08/2026, sem apagar o registro original |
| 029 | `029_preco_varia_produtos_publicos.sql` | **"A partir de" só quando há faixa de preço (padrão de mercado, pedido do usuário)**: `get_public_products` ganha a coluna `preco_varia` (boolean) — `true` quando há variações ativas com preço base (não promocional) diferente entre si, calculado via `max(v.preco) is distinct from min(v.preco)` (trata `null` corretamente pra produto sem variação ativa). Precisou **`drop function` antes do `create`** (não é `create or replace` simples) — mudar o `RETURNS TABLE` de uma função existente com a mesma assinatura de entrada é um dos dois casos que o Postgres rejeita silenciosamente diferente do esperado: aqui dá **erro explícito** ("cannot change return type"), diferente do caso de mudar parâmetros de entrada (que cria overload em vez de substituir, lição das migrations `025`/`026`) — mas de qualquer forma exige tratamento manual, não um `create or replace` ingênuo. Isolamento por tenant, `security definer` e a garantia de nunca expor estoque numérico **inalterados** (diff automatizado contra a `028` confirmou — só a coluna nova e o `drop`/`create` no lugar de `create or replace`) | 📐→✅ **Criada em 13/08/2026, revisada e aplicada pelo usuário no mesmo dia.** Frontend já estava deployado tolerando os dois estados antes da aplicação (sem quebra no intervalo). **Re-teste pós-aplicação confirmado contra a HML**: `preco_varia` confirmado via REST direto na RPC; dos 23 produtos de `/produtos`, exatamente os 5 com faixa de preço real mostrando "a partir de" e os 18 de preço único sem o prefixo — zero falso positivo/negativo. Ver `ESCOPO_PROJETO.md` §0 item 26 pro detalhe completo |
| 030 | `030_vitrine_identidade_editavel.sql` | **Vitrine Fase 1 Etapa 3 — banner/selos/WhatsApp/identidade editáveis**: `store_settings` ganha `banner_titulo`/`banner_subtitulo`/`banner_botao_texto`/`banner_botao_href`/`banner_tipo_fundo` (`'cor'\|'imagem'`)/`banner_cor_fundo`/`banner_imagem_path`, `selos` (JSONB, 4 fixos)/`whatsapp_numero`/`whatsapp_mensagem`/`cor_principal` — todos `ADD COLUMN IF NOT EXISTS` com `DEFAULT` igual ao valor hoje hardcoded no frontend (aplicar não muda a aparência de nenhum tenant existente). `get_public_store_settings` estendida pra devolver os campos novos — mesmo `drop function`+`create` da `029` (`RETURNS TABLE` não aceita `create or replace` com a mesma assinatura de entrada). Isolamento por tenant e `security definer` **inalterados** (diff automatizado contra a `028` confirmou). Nenhum campo novo é sensível | 📐→✅ **Criada em 13/08/2026, revisada e aplicada pelo usuário no mesmo dia.** Frontend já estava deployado tolerando os dois estados antes da aplicação (sem quebra no intervalo). **Re-teste pós-aplicação confirmado contra a HML**: banner com imagem real + overlay escuro, cor do banner configurável (testada com laranja, revertida ao default), `cor_principal` propagando pro tema inteiro sem afetar a cor do banner (testada com violeta, revertida ao default), selos respeitando `ativo=false` e edição de texto (testado e revertido ao default), botão flutuante do WhatsApp com `href` `wa.me` corretamente formado e mensagem url-encoded (deixado configurado com número de teste, não é o número real da loja). Ver `ESCOPO_PROJETO.md` §0 item 27 pro detalhe completo |
| 031 | `031_vitrine_configuracoes_rascunho_publicar.sql` | **Vitrine Fase 1 Etapa 4 — Configurações da Vitrine, rascunho/publicar**: `store_settings` ganha `logo_path` (campo publicado novo, null = logo estática atual) e `rascunho` (jsonb nullable, snapshot em edição). `get_public_store_settings` ganha `logo_path` (drop+create de novo, mesmo motivo da `029`/`030` — contrato dos campos existentes não muda). RPCs de staff (`get_configuracao_vitrine`/`salvar_rascunho_vitrine`/`publicar_vitrine`, isoladas por `current_tenant_id()`, nunca grantadas a anon). Isolamento por tenant e `security definer` inalterados (diff automatizado contra a `030` confirmou pra `get_public_store_settings`). **Revisada em 14/08/2026 (mesmo dia, antes de aplicar)**: a primeira versão incluía uma tabela de tokens de preview + 2 RPCs pra um link de prévia utilizável sem sessão — descartado a pedido do usuário (projeto vai a produção comercial, link vazável não é adequado); o preview agora é 100% protegido por sessão de staff, resolvido inteiramente no frontend (`src/app/vitrine-preview/page.tsx`), sem precisar de nenhuma peça nova no banco além do que já está listado aqui | 📐→✅ **Criada em 13/08, revisada em 14/08/2026, aplicada pelo usuário no mesmo dia.** **Re-teste completo pós-aplicação com sessão de staff REAL** (magic link via API administrativa do Supabase, conta real `adm@criatoriocapua.com.br`/tenant `capua` — não um curl com service role): `/vitrine-preview` deslogado → redireciona pro login, nunca renderiza; logado → mostra o rascunho (testado com um título de banner marcado) usando os mesmos componentes da vitrine real; vitrine pública (anônimo) confirmada **sem** o rascunho enquanto não publicado; "Publicar" refletindo na pública imediatamente (reconferido sem sessão); Open Graph com `og:title`=nome da loja, `og:image`=banner configurado. Estado de teste revertido ao original ao final. Ver `ESCOPO_PROJETO.md` §0 itens 28 (proposta original) e 29 (revisão aplicada + teste de ponta a ponta) pro detalhe completo, incluindo dois bugs reais encontrados e corrigidos em revisão/teste (path da policy do Storage, título duplicado) |
| 032 | `032_public_delivery_cities.sql` | **Fase 2 (Carrinho/Checkout), incremento 1 do roteiro — RPC pública `get_public_delivery_cities`**: fecha a pendência registrada desde a `028` (leitura anônima de `delivery_cities` fechada por completo, sem RPC equivalente criada na época, deliberadamente adiada pro desenho do Carrinho). Diagnóstico prévio do schema real (`005`/`008`, sem mudança desde então, conferido também em `database.ts`): `id, tenant_id, nome, uf, ponto_entrega, horario, observacoes, ordem, ativo, created_at, updated_at` — sem coluna de taxa de entrega ainda (decisão futura, `REGRAS_DE_NEGOCIO.md` §5, fora do escopo desta RPC). Segue exatamente o padrão das 4 RPCs da `028`: `SECURITY DEFINER`, `stable`, `search_path` fixo, filtro de tenant resolvido internamente a partir do `p_tenant_slug`, slug inválido/tenant inativo devolvem zero linhas. Filtra `tenants.ativo=true` e `delivery_cities.ativo=true`, ordena por `ordem, nome` (mesmo critério já usado em `/api/painel/cidades`). Expõe `id, nome, uf, ponto_entrega, horario, observacoes, ordem` — de propósito não expõe `tenant_id`, `ativo` (sempre `true` no retorno), `created_at`/`updated_at`. Puramente aditiva — cria função nova, não altera nenhuma tabela/policy/função existente | 📐→✅ **Criada e aprovada em 18/08/2026, aplicada pelo usuário no mesmo dia, testada em seguida com a `anon key` (não service role).** Slug válido (`capua`) devolveu as 2 cidades ativas com exatamente as 7 colunas esperadas; slug inválido devolveu 0 linhas sem erro; filtro `ativo=true` confirmado com dado real (tenant tem uma 3ª cidade inativa — `Vitória da Conquista` — corretamente excluída, não é coincidência de não haver caso pra testar); nenhum de `tenant_id`/`ativo`/`created_at`/`updated_at` apareceu na resposta. Script de teste era descartável, removido do repositório depois. Ver `ESCOPO_PROJETO.md` §0 item 35 pro detalhe completo do diagnóstico, da decisão de colunas expostas e do teste |
| 033 | `033_handle_new_user_role_via_app_metadata.sql` | **Fase 2, incremento 2 (Contas de cliente) — fecha gap de segurança CRÍTICO em `handle_new_user`**: a trigger decidia staff-vs-cliente lendo `role` de `raw_user_meta_data`, campo que qualquer `signUp()` público (`anon key`) pode preencher livremente — nada impedia alguém de virar staff mandando `role:'admin'` fora do formulário `/cadastro`. Correção: `role` passa a vir de `raw_app_meta_data` (`app_metadata`), campo que a Auth API do Supabase nunca deixa o próprio usuário gravar (só a Admin API/service role) — signup público fica estruturalmente incapaz de virar staff, não importa o payload. `nome`/`whatsapp`/`delivery_city_id` continuam de `raw_user_meta_data` normalmente (dado de perfil, sem risco de autorização). Diagnóstico prévio (Admin API, só leitura) confirmou que os 2 staff reais hoje foram provisionados por canal já privilegiado (Admin API com `email_confirm:true`, ou INSERT direto em `profiles`) — fechar o gap não quebra o onboarding atual. `create or replace function` — não altera tabela/coluna/policy nenhuma, staff já cadastrado não é tocado | 📐→✅ **Criada e aprovada em 18/08/2026, aplicada pelo usuário no mesmo dia, blindagem testada ao vivo (ataque simulado de verdade, não só leitura de código).** Signup real via `anon key` com `role:'admin'` no metadata confirmado virando `customers` (zero linha em `profiles`); signup normal de cliente confirmado preenchendo `customers` corretamente (nome/whatsapp/email/cidade); 2 usuários de teste removidos ao final, zero resíduo. Ver `ESCOPO_PROJETO.md` §0 item 36 pro detalhe completo do diagnóstico, da decisão de desenho e do teste |
| 034 | `034_customers_email_not_null_e_limpeza.sql` | **Fase 2, incremento 2 — `customers.email NOT NULL` + `DROP COLUMN cidade_entrega`**: `email` é o identificador de login (`REGRAS_DE_NEGOCIO.md` §15.2), sempre preenchido na prática mas nunca formalizado como `NOT NULL`. Diagnóstico prévio (rodado na hora de escrever a migration): as 4 linhas reais de `customers` têm `0` com `email` nulo — seguro sem backfill. `cidade_entrega` (text, coluna original da `002`) removida — morta desde que `delivery_city_id` (uuid, FK, migration `005`) virou o campo real, confirmado por grep sem nenhum uso em `src/` | 📐→✅ **Criada e aprovada em 18/08/2026, aplicada pelo usuário no mesmo dia.** Reconfirmado direto no banco: `select cidade_entrega from customers` devolve erro "column does not exist" — coluna removida de verdade, não só na migration. Ver `ESCOPO_PROJETO.md` §0 item 37 |
| 035 | `035_impedir_conta_dupla_staff_customer.sql` | **Fase 2, incremento 2 — 2 triggers `BEFORE INSERT` (`profiles`/`customers`) impedindo a mesma conta acumular linha nas duas tabelas ao mesmo tempo**: reforça isolamento de papéis (`REGRAS_DE_NEGOCIO.md` §1/§14). Caso "duas contas diferentes, mesmo e-mail" já é impossível (unicidade nativa de `auth.users.email`); o que faltava era a mesma conta virar as duas coisas — só alcançável via INSERT manual privilegiado (mesmo caminho de criar staff da `033`), defesa contra erro operacional, não risco externo. Escopo limitado a `INSERT` (não `UPDATE` de `id`/`auth_user_id`, vetor incomum, limitação conhecida registrada no cabeçalho). Não interfere no fluxo normal de `handle_new_user` (nunca insere nas duas tabelas pro mesmo usuário) | 📐→✅ **Criada e aprovada em 18/08/2026, aplicada pelo usuário no mesmo dia, guarda testada de verdade.** Tentativa real de `INSERT` em `profiles` reutilizando o `auth_user_id` de um `customer` já existente foi rejeitada com a mensagem exata do cabeçalho ("Este usuario ja possui cadastro de cliente..."), sem deixar resíduo. Ver `ESCOPO_PROJETO.md` §0 item 37 |
| 036 | `036_expor_permite_autocadastro.sql` | **Fase 2, incremento 2 — expõe `permite_autocadastro` em `get_public_store_settings`**: campo existe desde a `002`, sempre foi excluído da RPC pública de propósito (nenhuma tela lia); `/cadastro` público vai passar a ler essa flag pra decidir se mostra o formulário. `drop`+`create` de novo (mesmo motivo das `029`/`030`/`031` — `RETURNS TABLE` não aceita `create or replace` com a mesma assinatura de entrada), reproduzindo as 18 colunas já existentes (lidas do estado atual da função na `031`, não presumidas) mais a nova. Isolamento de tenant e `security definer` inalterados | 📐→✅ **Criada e aprovada em 18/08/2026, aplicada pelo usuário no mesmo dia, testada em produção/HML.** RPC pública (`anon key`) confirmada devolvendo `permite_autocadastro`; ponta a ponta com o frontend (`/cadastro`) confirmado mostrando "Cadastro fechado no momento" com a flag `false` e o formulário normal com `true`. Ver `ESCOPO_PROJETO.md` §0 item 37 |
| 037 | `037_orders_modelo_pedidos.sql` | **Fase 2, incremento 3 — modelo de pedidos (só dados, sem tela ainda)**: cria `orders`/`order_items`/`order_number_sequences` + RPC `criar_pedido`. Enum `order_status` (4 valores, `REGRAS_DE_NEGOCIO.md` §19.1). `modalidade_entrega` texto+CHECK (extensível, hoje só `'ponto_encontro'`), `delivery_city_id` **nullable** na coluna — obrigatoriedade de cidade é regra de negócio da modalidade "ponto de encontro", vive na RPC (checagem condicionada a `modalidade_entrega`), nunca `NOT NULL`/CHECK na tabela (modalidades futuras sem cidade não devem travar a estrutura). Número de pedido legível via contador atômico por tenant (mesmo padrão de `product_code_sequences`), `unique(tenant_id, numero)` como rede de segurança extra. **RPC `criar_pedido` não mexe em estoque** (correção de princípio registrada no item 39/§16.4 — baixa migra pra validação, incremento 7): sem `FOR UPDATE`, sem tocar `saldo_estoque`, sem `stock_movements`. `customer_id` sempre resolvido de `auth.uid()` (nunca parâmetro), `delivery_city_id` validado internamente, `quantidade_minima_venda` checada por item, defesa em profundidade (`loja_aberta`/`pedidos_abertos`). RLS: zero policy de escrita pra `anon`/`authenticated` (só a RPC), `SELECT` staff vê tudo do tenant / cliente vê só os próprios. Cliente convidado não suportado (`customer_id`/`auth_user_id` continuam `not null`) — fica pro incremento 5 | 📐→✅ **Criada em 18/08/2026, corrigida no mesmo dia antes de aplicar (`delivery_city_id` voltou a nullable), aprovada e aplicada pelo usuário no mesmo dia, testada com sessões reais de cliente (não service role).** Pedido válido criado corretamente (`numero=1`, `total` certo, `order_items` com snapshot de preço) e **confirmado que `saldo_estoque`/`stock_movements` da variação ficaram intocados** (antes=depois nos dois); erro amigável sem cidade e com quantidade abaixo do mínimo; RLS confirmada isolando pedidos entre 2 clientes de teste reais. Ver `ESCOPO_PROJETO.md` §0 item 40 |

## Ordem de aplicação recomendada (das pendentes)

`014` → `015` → `016`, nessa ordem (`013` já aplicada) — `015`/`016` dependem do isolamento (`013`/`014`) já estar totalmente em vigor pra fazerem sentido dentro da estratégia adotada (decisão #21). **Atualização de 31/07/2026: `013` e `014` já aplicadas e validadas — só `015` e `016` seguem pendentes.** **Atualização final de 31/07/2026: as quatro migrations desta lista (`013`, `014`, `015`, `016`) estão todas aplicadas e validadas. Nenhuma migration pendente no momento — o próximo trabalho (CRUD de Produtos) não depende de nenhuma migration nova além das que já existem.** **Nova atualização de 31/07/2026: migration `017` criada (edição atômica de produto+variações + SKU automático) — pendente de revisão e aplicação manual. Não bloqueia a criação de produtos (que já usa a `016`), só a edição.** **Atualização final de 01/08/2026: `017` revisada, aprovada e aplicada. Todas as migrations `001`–`017` estão aplicadas no banco. Nenhuma migration pendente — a pendência atual é de re-teste (Chromium contra a URL pública), não de banco. Ver `ESCOPO_PROJETO.md` §0.**

**Nova atualização de 01/08/2026: migration `018` criada** (fecha o terceiro vazamento cross-tenant, desta vez numa view — `products_com_status` sem `security_invoker`) — pendente de revisão e aplicação manual pelo usuário. Diferente de `013`–`017`, não depende de nenhuma outra migration pendente pra fazer sentido; é uma correção isolada e idempotente. Depois de aplicada, o isolamento por tenant precisa ser reconfirmado incluindo views (não só tabelas), reaproveitando o canário `_teste_isolamento`.

**Atualização final de 01/08/2026: `019` aplicada pelo usuário; frontend correspondente (Route Handlers, tipos, Combobox no formulário de produto, tela `/painel/unidades-venda`) implementado e testado com Chromium real contra a URL pública no mesmo dia — ver `ESCOPO_PROJETO.md` §0 itens 7–9 para o histórico completo do incidente de produção que isso causou e como foi fechado. Única migration pendente agora é a `020` (corrigida após 1ª tentativa falhar — ver linha acima), que remove a coluna de texto livre antiga; não bloqueia nenhuma funcionalidade, é só limpeza de schema.**

**Atualização final de 01/08/2026: `018` revisada, aprovada e aplicada pelo usuário.** Todas as migrations `001`–`018` estão aplicadas no banco. `reloptions` da view reconferido (`security_invoker=true`); reteste de isolamento com o canário, agora cobrindo a view (não só tabela), confirmou `de_outro_tenant=0`. Nenhuma migration pendente no momento.

**Atualização de fechamento de sessão, 03/08/2026: migrations `019`–`023` também aplicadas.** `001`–`023` estão **todas aplicadas e validadas** no banco — nenhuma pendente. Resumo do que cada uma trouxe (detalhe completo nas linhas da tabela acima e em `ESCOPO_PROJETO.md`): `019`/`020` — cadastro de Unidades de Venda; `021`/`022` — módulo de Estoque (ledger de movimentações); `023` — Produtos Etapa 3, `product_images` ganha `variant_id` + bucket do Storage. Próxima migration a criar (quando o trabalho retomar) seria pra Produtos Etapa 2 (Características por produto, `product_attribute_values`) — ainda não desenhada.

**Atualização de fechamento de sessão, 03/08/2026 (continuação) — Produtos Etapa 3 (Imagens) incremento 2 (mini-uploader por variação) não precisou de nenhuma migration nova.** A `023` já tinha sido desenhada desde o início pensando em `variant_id` (coluna nullable, `check` de capa só em imagem de produto, contagem de limite escopável) — o incremento 2 foi 100% frontend em cima do que já estava no banco. Continua nenhuma migration pendente; `001`–`023` seguem sendo o estado completo aplicado. Ver `ESCOPO_PROJETO.md` §0 item 11 e §4 (tabela "Concluídas") para o detalhe do teste.

**Atualização de 05/08/2026: migration `024` aplicada pelo usuário e testada.** `001`–`024` estão **todas aplicadas e validadas** no banco — nenhuma pendente. `024` traz o modo automático de código de produto derivado do nome (decisão #25) — tabela `product_code_sequences` + RPC `gerar_codigo_produto_por_prefixo`, sem nenhuma mudança nas RPCs de criação/edição de produto já existentes. Testada com Chromium real contra a URL pública (prefixo derivado corretamente do nome, sequência por prefixo incrementando entre produtos com nomes diferentes) — ver `ESCOPO_PROJETO.md` §4 (tabela "Concluídas") para o detalhe completo, incluindo o regressivo do módulo de Produtos inteiro rodado na mesma sessão. Próxima migration a criar continua sendo pra Produtos Etapa 2 (Características por produto, `product_attribute_values`) — ainda não desenhada.

**Atualização de 06/08/2026: migrations `025` e `026` aplicadas pelo usuário e testadas — módulo de Produtos 100% completo.** `001`–`026` estão **todas aplicadas e validadas** no banco — nenhuma pendente. `025` traz Produtos Etapa 2 (Características por produto — `product_attribute_values` ganha `DEFAULT` de tenant, RPCs de produto ganham `p_caracteristicas`); `026` corrige um efeito colateral real da `025` (overload de função por mudança de assinatura — ver linhas acima). Frontend implementado e testado com Chromium real contra a URL pública na mesma sessão, incluindo 2 bugs reais de frontend encontrados e corrigidos (race entre duas chamadas de `useCaracteristicas`; `Controller` sem `defaultValue` quebrando a validação de obrigatoriedade) — ver `ESCOPO_PROJETO.md` §4 (tabela "Concluídas") e §0 item 12 para o detalhe completo. **Com isso, o módulo de Produtos (Etapas 1, 2 e 3) está fechado** — próxima frente de trabalho ainda não decidida com o usuário.

**Atualização de 06/08/2026 (mesmo dia) — remoção do modo de estoque "disponibilidade" da UI, sem migration.** Bug real reportado pelo usuário: estoque inicial de variação em modo `disponibilidade` era descartado silenciosamente ao salvar (`registrar_estoque_inicial_variacao`, migration `022`, sempre ignorou esse campo nesse modo de propósito — só faltava o formulário avisar). Decisão de produto: remover o seletor de modo de estoque da tela por completo (negócio só usa quantitativo) em vez de só melhorar o aviso. **Nenhuma migration nova** — enum/coluna `modo_estoque` continuam no banco intactos, mudança é só de frontend (campo escondido, variações novas nascem `quantitativo` via valor padrão do formulário). Testado com Chromium real: produto com 2 variações e estoque inicial confirmado registrado corretamente pras duas. Ver `ESCOPO_PROJETO.md` §4 (tabela "Concluídas") para o detalhe completo.

**Fechamento de sessão, 07/08/2026 — migration `027` aplicada e testada, incidente de produção corrigido no mesmo dia.** `001`–`027` estão **todas aplicadas e validadas** no banco — nenhuma pendente. `027` traz os itens 1 e 3 do lote de ajustes de UX (dois mínimos na variação + SKU derivado por palavra) — ver linha acima e `ESCOPO_PROJETO.md` §4 (tabela "Concluídas") pro relato completo do incidente (migration aplicada antes do código acompanhar, página de Estoque ficou quebrada por um tempo) e da correção + teste que fecharam o dia. Itens 2, 4 e 6 do mesmo lote (moeda/campos numéricos, remoção do "preço a partir de", botão de salvar fixo) também testados e fechados na mesma sessão, sem migration. **Pendente pra próxima sessão**: item 5 do lote (busca redesenhada com árvore de categorias + destaque de variação que bateu na busca) — ainda não iniciado, é o maior dos 6 itens.

**Atualização de 08–09/08/2026: item 5 do lote entregue e aprovado — lote de 6 itens de UX (07/08/2026) 100% fechado.** Sem migration nova — as 3 partes do item 5 (destaque de variação na busca, filtro de categoria em árvore, busca unificada por status) são só frontend + um endpoint novo de leitura (`GET /api/painel/produtos/contagem-por-categoria`, agregação em JS sobre `products_com_status`, sem view/RPC nova no banco). `001`–`027` seguem sendo todas as migrations aplicadas — nenhuma pendente. Ver `ESCOPO_PROJETO.md` §0 itens 14–15 e §4 (tabela "Concluídas") para o detalhe completo do teste e da aprovação. Próxima frente de trabalho fora de Produtos ainda não decidida com o usuário.

**Atualização de 10/08/2026: migration `028` (Vitrine Fase 0) aprovada com 1 ajuste (tenant de teste dedicado `_teste_vitrine`, não reaproveitando o canário `_teste_isolamento` — ver linha da tabela acima, que descreve a versão original proposta antes do ajuste; a versão final removeu o bloco de seed de dentro da migration, movido para `supabase/tests/vitrine_isolamento_test.sql`), aplicada pelo usuário no SQL Editor e testada em seguida — as 5 funções (`resolve_tenant_by_host` + as 4 `get_public_*`) confirmadas no banco. Teste de isolamento rodado (via `@supabase/supabase-js` + service role key, já que este ambiente não tem driver Postgres direto pra colar o `.sql` no SQL Editor) reproduzindo o mesmo roteiro do script: **14/14 checks passando** — `_teste_vitrine` criado e associado a `testeecommerce.vluma.com.br`, `capua` associado a `ecommercecauahml.vluma.com.br`; nenhum dos dois hosts nunca devolve dado do outro (testado nos dois sentidos, mais `get_public_product_detail` devolvendo `null` pro slug de teste quando chamado com o slug do Cauã); host forjado não resolve pra tenant nenhum; leitura direta da tabela `products` com a anon key (bypassando as RPCs) confirmada vazia — as 8 policies `*_select_anon` estão mesmo dropadas. **001–028 estão todas aplicadas e validadas no banco — nenhuma pendente.**

**Pendência de funcionalidade registrada (não é gap de segurança) — `delivery_cities` ficou sem leitura pública.** A migration `028` dropou a policy `cities_select_anon` (tenant-blind, junto com as outras 7) mas **não criou nenhuma RPC pública** pra cidades de entrega — decisão deliberada na hora, registrada no comentário do próprio SQL: a Vitrine ainda não decidiu como/quando expor isso (só faz sentido quando o Carrinho/Checkout for desenhado, pra calcular frete/área de entrega). Efeito prático imediato: leitura anônima de `delivery_cities` está **completamente fechada** agora (nem pela tabela, nem por RPC) — sem regressão visível hoje porque nenhuma tela pública consulta isso ainda, mas é uma peça que falta antes do Carrinho/Checkout poder funcionar pro visitante anônimo. Uma RPC `get_public_delivery_cities(p_tenant_slug)` (mesmo padrão `SECURITY DEFINER` das outras 4) precisa ser criada nessa fase futura — ver também `ESCOPO_PROJETO.md` pendências abertas.

**Atualização de 14/08/2026, fechamento de sessão: `001`–`031` estão todas aplicadas e validadas no banco — nenhuma migration pendente.** Com a `031` (revisada, sem tokens de preview), a Vitrine Fase 1 (dados de teste, telas públicas, identidade editável, Configurações com rascunho/publicar/preview seguro por sessão, Open Graph) está 100% completa e testada em produção/HML, inclusive com sessão de staff real (não só service role). Pendência de funcionalidade que segue aberta (não é migration faltando, é RPC ainda não desenhada): `get_public_delivery_cities` — bloqueia o início da Fase 2 (Carrinho/Checkout), ver o parágrafo acima e `ESCOPO_PROJETO.md` §0 item 20/29.

**Atualização de 18/08/2026: migration `032` criada (Fase 2, incremento 1 do roteiro), aguardando revisão e aplicação manual pelo usuário.** `001`–`031` continuam sendo tudo que está aplicado no banco — `032` é a única migration pendente no momento. Fecha a pendência de `get_public_delivery_cities` registrada logo acima e desde a `028`. Puramente aditiva (nenhuma tabela/policy/função existente é tocada) — sem risco de regressão pro que já está em produção/HML. Ver linha da tabela acima (`032`) e `ESCOPO_PROJETO.md` §0 item 35 pro detalhe completo do diagnóstico de schema e da decisão de colunas expostas.

**Atualização final de 18/08/2026: migration `032` aplicada pelo usuário e testada no mesmo dia — `001`–`032` estão todas aplicadas e validadas no banco, nenhuma pendente.** Teste com `anon key` (não service role) confirmou: slug válido devolve só as cidades ativas (com uma cidade inativa real no tenant `capua` corretamente excluída), slug inválido devolve zero linhas sem erro, resposta nunca traz `tenant_id`/`ativo`/`created_at`/`updated_at`. Ver linha da tabela acima (`032`) e `ESCOPO_PROJETO.md` §0 item 35 pro detalhe completo. Consumo pelo frontend (seletor de cidade no checkout) segue não iniciado — fica pro incremento 5 do roteiro da Fase 2 (item 34/ponto 8).

**Atualização de 18/08/2026 (continuação): migration `033` criada (Fase 2, incremento 2 — fecha gap crítico de segurança em `handle_new_user`), aguardando revisão e aplicação manual pelo usuário.** `001`–`032` continuam sendo tudo que está aplicado no banco — `033` é a única migration pendente no momento. Puramente `create or replace function` (nenhuma tabela/coluna/policy tocada) — sem risco de regressão pro que já está em produção/HML. Ver linha da tabela acima (`033`) e `ESCOPO_PROJETO.md` §0 item 36 pro detalhe completo do diagnóstico e da decisão de desenho.

**Atualização final de 18/08/2026: migration `033` aplicada pelo usuário e a blindagem testada com um ataque simulado de verdade — `001`–`033` estão todas aplicadas e validadas no banco, nenhuma pendente.** Signup real via `anon key` com `role:'admin'` no metadata confirmado virando `customers`, não `profiles`; signup normal de cliente confirmado funcionando corretamente. Gap crítico fechado e comprovado, não só corrigido no papel. Ver linha da tabela acima (`033`) e `ESCOPO_PROJETO.md` §0 item 36 pro detalhe completo.

**Atualização de 18/08/2026 (continuação): migrations `034`/`035`/`036` criadas (Fase 2, incremento 2 — resto do pacote de Contas de cliente), aguardando revisão e aplicação manual pelo usuário.** `001`–`033` continuam sendo tudo que está aplicado no banco — essas 3 são as únicas pendentes no momento. `034` (`customers.email NOT NULL` + drop de coluna morta) e `036` (expor `permite_autocadastro`) são puramente aditivas/defensivas, sem risco de regressão. `035` (triggers de guarda) é `BEFORE INSERT` — não toca em nenhuma linha existente, mesmo havendo hoje uma sobreposição real e pré-existente só no canário de teste (`teste-isolamento@vluma.local`, tenant sintético `_teste_isolamento`, presente em `profiles` **e** `customers` ao mesmo tempo — achado do diagnóstico rodado antes de aplicar, não afeta o Cauã). Aplicar `035` não falha nem mexe nesse dado — só passa a impedir a mesma sobreposição em contas **novas** dali pra frente. Ver linhas da tabela acima (`034`/`035`/`036`) e `ESCOPO_PROJETO.md` §0 item 37 pro detalhe completo.

**Atualização final de 18/08/2026: migrations `034`/`035`/`036` aplicadas pelo usuário e testadas no mesmo dia — `001`–`036` estão todas aplicadas e validadas no banco, nenhuma pendente.** `034` reconfirmada por leitura direta (`cidade_entrega` não existe mais); `035` reconfirmada com uma tentativa real de `INSERT` cruzado, rejeitada com a mensagem certa; `036` reconfirmada pela RPC pública e ponta a ponta com o `/cadastro` (gate funcionando nos dois sentidos). Com isso, o pacote completo do incremento 2 (SQL + código: restyle das 4 telas, dropdown de cidade corrigido via RPC `032`, gate de autocadastro) está fechado e testado na URL pública. Ver `ESCOPO_PROJETO.md` §0 item 37 pro relato completo do teste de ponta a ponta.

**Atualização de 18/08/2026 (continuação): migration `037` criada (Fase 2, incremento 3 — modelo de pedidos, só dados), aguardando revisão e aplicação manual pelo usuário.** `001`–`036` continuam sendo tudo que está aplicado no banco — `037` é a única pendente no momento. Cria 3 tabelas novas (`orders`/`order_items`/`order_number_sequences`) + a RPC `criar_pedido` — puramente aditiva, nenhuma tabela/policy/função existente é tocada. RPC não mexe em estoque (correção de princípio do item 39 de `ESCOPO_PROJETO.md`) — baixa fica pra RPC de validação do incremento 7, ainda não desenhada. Sem tela nenhuma consumindo isso ainda. Ver linha da tabela acima (`037`) e `ESCOPO_PROJETO.md` §0 item 40 pro detalhe completo do desenho.

**Atualização final de 18/08/2026: migration `037` aplicada pelo usuário (após revisão de ponta a ponta do arquivo completo e a correção do `delivery_city_id`) e testada no mesmo dia com sessões reais de cliente — `001`–`037` estão todas aplicadas e validadas no banco, nenhuma pendente.** Pedido de teste criado com sucesso, `saldo_estoque`/`stock_movements` da variação confirmados intocados (prova viva, não só leitura de código), validações de cidade/quantidade mínima com mensagem amigável, RLS isolando pedidos entre 2 clientes reais. Zero resíduo de teste ao final. Modelo de pedidos do MVP (Fluxo A) está completo — próximo passo é o incremento 4 (Carrinho client-side). Ver `ESCOPO_PROJETO.md` §0 item 40 pro relato completo do teste.

---

*Ver `docs/ESCOPO_PROJETO.md` §6 "Ambientes e referências" para o contexto completo de como as migrations são versionadas e aplicadas, e §0 para a regra de rastreabilidade de migrations ajustadas/descartadas.*
