# ESCOPO DO PROJETO — E-commerce Criatório Capuã / VLUMA

**Última atualização:** 03/08/2026 (data original de criação do documento: 30/07/2026)
**Baseado em:** leitura completa do código-fonte, migrations `001` a `012`, histórico de commits e `docs/VISAO_CAUA.md`.

> **Princípio norteador:** este é um produto **SaaS** (VLUMA). O Criatório Capuã é o primeiro cliente e validador, mas as decisões são de **produto** — o critério é "isso serve qualquer lojista do segmento?", não "o Cauã precisa disso?". Justificativas de arquitetura baseadas só na realidade de um cliente devem ser evitadas.

### 📌 Pendências abertas agora (checar primeiro, 01/08/2026)

> **⚠️ ATENÇÃO — bug de produção aberto, registrado em 21/08/2026, detalhe completo no item 46 mais abaixo**: `admin.createUser({ app_metadata: { role: '...' } })` numa chamada só cria silenciosamente uma conta de **cliente** em vez de **staff** — é o caminho que a migration 033 documenta como oficial pra provisionar staff novo, e falha sem erro nenhum. Não corrigido ainda, não afeta os 2 staff reais existentes. Antes de criar qualquer staff novo (real ou de teste) nesta plataforma, ler o item 46.

1. ~~Re-teste da Etapa 1 de Produtos contra a URL pública (`ecommercecauahml.vluma.com.br`), não `localhost` — o teste anterior rodou local antes do push.~~ **✅ Resolvido — reconfirmado em 02/08/2026** com um teste dedicado contra a URL pública (além de toda a atividade cumulativa do resto desta sessão — Bug 1/2, Unidades de Venda, Melhoria 4, módulo de Estoque — que já exercitou CRUD de produto repetidamente na URL pública sem problema): confirmados os 3 bugs originalmente encontrados e corrigidos continuam corrigidos (`preco_promocional` vazio vira `null`, não `0`; nenhum warning de acessibilidade de `nativeButton` no console; validação Zod aparece pro preço negativo, sem tooltip nativo do navegador) — mais criar produto, adicionar variação, remover variação (soft delete confirmado via diff-sync), busca na listagem, inativar/reativar. Produto de teste limpo (inativado) ao final. Ver linha "CRUD de Produtos — Etapa 1" na tabela "Concluídas" pra detalhe completo.
2. ~~Sessão expirando (~1h) sem renovação — suspeita não confirmada, pode impactar o Cauã em produção.~~ **✅ Resolvido em 10/08/2026 — ver item 18 mais abaixo.** Ver nota detalhada em §2 "Padrão de autenticação" (`proxy.ts` tem `setAll()` vazio de propósito; comentário em `server.ts` contradiz isso). Não reproduzido ao vivo ainda — precisa investigar antes de decidir a correção.
3. Migrations `015`–`017` já aplicadas, tudo commitado e pushado (`git log origin/main` = `git log` local) — sem pendência de banco ou de git no momento em que este documento foi escrito.
4. ~~`product_images` (Etapa 3, fotos de produto) e `product_attribute_values` (Etapa 2, ficha técnica por produto) do CRUD de Produtos — não iniciados, fora do escopo da Etapa 1.~~ **Desatualizado — ver item 10 (fechamento de sessão, 03/08/2026)**: Etapa 3 (Imagens) teve seu incremento 1 (galeria do produto) implementado e testado; Etapa 2 (Características por produto) segue não iniciada.
5. Arquivo `supabase/migrations/novo 6.txt` não rastreado pelo git — não foi criado por nenhuma sessão do assistente (verificado: já aparecia como untracked no primeiro `git status` desta sessão). Provavelmente rascunho do usuário; não mexer sem perguntar.
6. ~~TERCEIRO vazamento cross-tenant diagnosticado em 01/08/2026, migration `018_products_com_status_security_invoker.sql` criada — aguardando revisão e aplicação manual.~~ **✅ Resolvido em 01/08/2026** — migration `018` aplicada pelo usuário; `reloptions` da view confirmado `security_invoker=true`; reteste de isolamento com o canário confirmou `de_outro_tenant=0`. Ver §2 e §6 para o detalhe completo.
7. **⚠️→✅ Incidente de produção, 01/08/2026 — cadastro/edição de produto quebrado por alguns minutos.** A migration `019` (Melhoria 3, Unidades de Venda) foi aplicada pelo usuário no banco antes do frontend correspondente existir: as RPCs `criar_produto_com_variacoes`/`atualizar_produto_com_variacoes` passaram a exigir `unidade_venda_id` (uuid), mas o painel ainda enviava o campo de texto livre antigo (`unidade_venda`) — toda tentativa de criar/editar produto em produção falhava com "Unidade de venda não encontrada." Confirmado ao vivo com Chromium (`POST /api/painel/produtos` → 400) antes da correção. **Corrigido no mesmo dia**: frontend atualizado (Route Handlers, tipos, formulário com Combobox de unidade + novo cadastro `/api/painel/unidades-venda`), commitado, pushado, e reconfirmado ao vivo (`POST` → 201, produto de teste criado e depois inativado via soft delete, sem deixar lixo em produção). **Lição de processo**: quando uma migration muda o contrato de uma RPC consumida pelo frontend (não só adiciona/relaxa algo), o frontend correspondente precisa estar pronto e deployado **antes** ou **junto** da aplicação da migration no banco — não depois. Migrations puramente aditivas/RLS (`013`–`018`) não tinham esse risco por não mudarem payload de RPC nenhuma; `019` foi a primeira migration desta série a mudar assinatura de RPC consumida pelo painel.
8. ~~Migration `020` (drop da coluna antiga `products.unidade_venda`) ainda não aplicada~~ **✅ Aplicada com sucesso em 01/08/2026** (2ª tentativa, versão corrigida). Confirmado ao vivo contra a URL pública: `GET /api/painel/produtos` retorna as colunas sem `unidade_venda` (só `unidade_venda_id`), e `POST /api/painel/produtos` (criar produto) continua funcionando normalmente (201) — a view `products_com_status` foi recriada corretamente. `security_invoker = true` foi declarado explicitamente na própria `CREATE VIEW` desta migration (não reverificado via `pg_class` desta vez — a confirmação é por leitura do SQL aplicado, não por reteste de isolamento cross-tenant; se quiser essa confirmação extra, é rodar `select reloptions from pg_class where relname = 'products_com_status'` no SQL Editor). Ver `docs/MIGRATIONS.md` (migration `020`).
9. ~~Tela de gestão `/painel/unidades-venda` ainda não implementada~~ **✅ Resolvido em 01/08/2026** — tela `/painel/unidades-venda` implementada (mesmo padrão de Cidades: nome + ativo, listagem/criar/editar/inativar/reativar), adicionada ao menu lateral, **testada com Chromium real contra a URL pública**: listagem confirmada com as 6 unidades do seed, unidade de teste criada e depois inativada com sucesso (soft delete, sem lixo em produção). **Melhoria 3 (Unidades de Venda) está completa** — falta só o usuário reaplicar a migration `020` corrigida (item 8 acima) para remover a coluna de texto livre antiga, que hoje só fica sem uso no banco.
10. **📌 Fechamento de sessão, 03/08/2026 — estado real, ler isto primeiro numa sessão nova.** Todas as migrations `001`–`023` estão aplicadas no banco (nenhuma pendente — ver `docs/MIGRATIONS.md`). `git status`/`git log origin/main` conferidos limpos ao encerrar: local = `origin/main`, nada só local. O que ficou pronto e testado na URL pública nesta sessão, além do que já estava nos itens acima: Bug de colisão de prefixo (derivação por palavras + sufixo numérico), Melhoria 3 completa (Unidades de Venda, cadastro + tela de gestão), Melhoria 4 completa (visualização read-only ao clicar na linha, Categorias e Produtos), módulo de Estoque completo (movimentações, listagem, histórico, "Estoque inicial" no formulário de produto), Produtos Etapa 3 (Imagens) **incremento 1** (galeria do produto — upload, capa, reordenar, remover, `alt_text`, isolamento por tenant confirmado no Storage). **Pendências reais pra próxima sessão** (nada mais além disso está em aberto): (a) ~~Produtos Etapa 3 **incremento 2** — mini-uploader de imagens por variação (limite 5, escopo `variant_id`; o backend/migration `023` já suporta, só falta a UI)~~ **✅ Resolvido em 03/08/2026 — ver item 11 abaixo**; (b) Produtos Etapa 2 — Características por produto (`product_attribute_values`), não iniciada; (c) item 2 acima (sessão expirando ~1h) — suspeita antiga, nunca investigada a fundo, ainda não confirmada nem descartada; (d) arquivo `novo 6.txt` (item 5) continua intocado, sem instrução do usuário sobre o que fazer com ele.

11. **📌 Fechamento de sessão, 03/08/2026 (continuação, mesmo dia) — Produtos Etapa 3 (Imagens) 100% completa.** Incremento 2 (mini-uploader de imagens por variação, limite 5, escopo `variant_id`) implementado, commitado, pushado e **testado com Chromium real contra a URL pública** — ver linha "Produtos Etapa 3 — Imagens: mini-uploader por variação (incremento 2)" na tabela "Concluídas" (§4) para o detalhe completo do teste. Nenhuma migration nova (a `023` já suportava tudo que a UI precisava). **Achado incidental, fora do escopo desta tarefa, sinalizado mas não mexido**: existem 2 produtos com nome "Ciclídeo Teste Automático" / "Ciclídeo Teste Automático 2" (ids `db0aa644-c554-42df-b917-77529dabde77` e `803e5cc6-c3a3-454f-8b5a-f05b24cc6cbd`) **ativos** no banco, aparentando ser lixo de teste de uma sessão anterior (01/08/2026) que não foi inativado ao final, diferente do padrão de limpeza seguido por todas as outras sessões de teste. Não foram tocados — decisão de manter ou inativar fica com o usuário. **Pendências reais pra próxima sessão, atualizadas**: (a) ~~Produtos Etapa 2 — Características por produto (`product_attribute_values`), não iniciada — próximo passo natural do módulo de Produtos~~ **✅ Resolvido em 06/08/2026 — ver item 12 abaixo**; (b) item 2 (sessão expirando ~1h), ainda não investigado; (c) `novo 6.txt`, ainda sem instrução; (d) os 2 produtos de teste ativos citados acima, aguardando decisão do usuário.

12. **📌 Fechamento de sessão, 06/08/2026 — Produtos Etapa 2 (Características) completa, módulo de Produtos 100% fechado.** Migrations `025`/`026` aplicadas pelo usuário; frontend implementado, testado com Chromium real contra a URL pública (2 bugs reais pegos e corrigidos no processo — race entre dois `useCaracteristicas` e `Controller` sem `defaultValue` quebrando o schema Zod antes da validação de obrigatoriedade rodar) — ver linha "Produtos Etapa 2 — Características por produto" na tabela "Concluídas" (§4) para o detalhe completo. **Pendências reais pra próxima sessão, atualizadas**: (a) item 2 (sessão expirando ~1h), suspeita antiga nunca investigada a fundo; (b) `novo 6.txt`, ainda sem instrução do usuário; (c) os 2 produtos "Ciclídeo Teste Automático"/"2" (item 11 acima — **nota de correção**: são do tenant sintético `_teste_isolamento`, não do Cauã, isolamento confirmado funcionando; foram inativados mesmo assim a pedido do usuário em 05/08/2026); (d) próximo módulo do roadmap (fora de Produtos) ainda não decidido com o usuário — Produtos (Etapas 1–3) está fechado, não há mais trabalho pendente nesta frente.

13. **📌 08/08/2026 — Fluxo de cadastro de produto passa a ficar na tela após salvar.** Melhoria de UX pontual (não fazia parte do lote de 6 itens de 07/08/2026, nem do item 5 dele — "busca redesenhada", que **continua sendo o próximo passo do lote, ainda não iniciado**): fechado o beco sem saída "salvar produto só pra poder subir foto, ser jogado de volta pra listagem, ter que reabrir o produto". Sem migration. Detalhe completo do comportamento e do teste com Chromium real (17/17 checks) na tabela "Concluídas" (§4), linha "Fluxo de cadastro de produto — permanece na tela após salvar". **Pendências reais pra próxima sessão, sem mudança em relação ao item 12**: (a) item 2 (sessão expirando ~1h); (b) `novo 6.txt`; (c) item 5 do lote de UX de 07/08/2026 (busca redesenhada: árvore de categorias + destaque de variação que bateu na busca) — maior item do lote, ainda não iniciado; (d) próximo módulo do roadmap fora de Produtos, ainda não decidido com o usuário.

14. **📌 08–09/08/2026 — Item 5 do lote de UX (busca redesenhada de Produtos), partes 1 e 2 entregues; parte 3 já existia.** Das 3 partes pedidas: **(2) busca mostrando qual variação bateu** — a mais urgente, pedida em primeiro lugar — e **(1) filtro de categoria em árvore navegável** foram implementadas, testadas com Chromium real contra a URL pública e pushadas. **(3) busca unificada por nome/código/SKU/rótulo seguindo o filtro de status** já estava implementada desde 05/08/2026 (decisão registrada em `REGRAS_DE_NEGOCIO.md` §4.4, "Busca da listagem inclui SKU e rótulo de variação") — conferida de novo nesta rodada (inclusive o cruzamento com o filtro de status, que já funcionava), nada novo a construir. Detalhe completo de ambas as partes entregues na tabela "Concluídas" (§4), linhas "Busca mostra qual variação bateu (item 5, parte 2)" e "Filtro de categoria em árvore navegável (item 5, parte 1)". Sem migration nas duas. **Item 5 do lote está completo** — não há mais pendência conhecida deste lote de 07/08/2026 (itens 1–6, todos fechados). Novo primitivo `components/ui/popover.tsx` (Base UI) criado nesta rodada, reaproveitável por qualquer feature futura que precise de popover.

15. **📌 09/08/2026 — Item 5 fechado: as 3 decisões de UX propostas no item 14 foram revisadas e aprovadas pelo usuário sem ressalva.** Confirmação explícita, uma a uma: (1) o contador de produtos por categoria na árvore reflete o filtro de status atual da tela (Ativos/Inativos/Todos), não um total fixo independente do filtro — aprovado; (2) quando mais de uma variação do mesmo produto bate na busca, a listagem mostra uma faixa por variação (todas empilhadas sob o produto), não só a primeira — aprovado; (3) a faixa de "variação encontrada" só aparece quando o motivo do produto ter aparecido na busca foi realmente uma variação (SKU/rótulo) — se o match foi pelo nome/código do próprio produto, nenhuma faixa aparece, mesmo que alguma variação tenha nome parecido com o termo buscado — aprovado. Nenhuma mudança de código foi necessária: o comportamento já implementado e testado no item 14 já correspondia exatamente ao aprovado, então não houve novo push nem novo teste nesta entrada — as evidências de teste continuam sendo as do item 14 (Chromium real contra a URL pública, 11/11 + 11/11 checks). **Com isso, o lote de 6 ajustes de UX do formulário/listagem de Produtos (decidido em 07/08/2026) está 100% fechado** — nenhum item pendente. **Módulo de Produtos (Etapas 1–3, mais este lote de UX) confirmado completo.** Próxima frente de trabalho segue em aberto — ver pendência (d) do item 14: próximo módulo do roadmap fora de Produtos, ainda não decidido com o usuário.

16. **📌 09/08/2026 — Regressivo final do módulo de Produtos rodado após a aprovação do item 15, sem código novo (só verificação).** Pedido do usuário: confirmar o módulo completo após concluir e testar o item 5. Rodado um teste regressivo cruzando Etapas 1–3 com o item 5 na mesma sessão (criar produto → fica na tela em edição → imagens destravadas → SKU automático → busca por nome → busca por SKU com faixa de variação destacada → filtro de categoria em árvore → módulo de Estoque carregando a variação nova) contra a URL pública — todos os pontos confirmados funcionando. **Achado no processo, investigado a fundo, descartado como bug real**: rodando o filtro de categoria em árvore repetidas vezes em sequência rápida (script automatizado, sem as pausas naturais de um humano lendo a tela entre digitar a busca e clicar num nó), a seleção ocasionalmente não registrava a tempo do primeiro request disparar — sempre que a mesma interação exata foi repetida em ritmo mais pausado (isolada, ou com espera maior após digitar no campo de busca da árvore), o comportamento foi 100% correto, inclusive confirmado por navegação direta pela URL do filtro (nunca falhou) e por captura do corpo da resposta da API (sempre trouxe o produto certo quando a categoria certa foi selecionada). Conclusão: artefato de automação rodando mais rápido que a UI consegue assentar entre digitar e clicar, não uma falha do produto — não bloqueia a confirmação abaixo. Produtos de teste desta rodada (inclusive os que ficaram presos ativos por scripts de diagnóstico que travaram) todos inativados ao final, checagem final confirmou zero produto de teste ativo em produção. **Módulo de Produtos reconfirmado 100% completo e saudável.**

17. **📌 10/08/2026 — Filtro de categoria da listagem de Produtos refeito de novo, desta vez pra escalar a CENTENAS de categorias.** O painel em árvore navegável do item 14/15 (sempre mostrava a árvore inteira, mesmo colapsada) foi rejeitado pelo usuário depois de revisar o mockup — não escala além de dezenas, uma árvore de centenas de categorias ainda vira uma lista impraticável de rolar. Substituído por `categoria-filter-popover.tsx` (o `categoria-tree-filter.tsx` da rodada anterior foi removido, não só desativado), com princípio novo: **o lojista nunca vê todas as categorias de uma vez**. Três caminhos: **(1) busca** (principal) — digitar filtra pelo **caminho inteiro** (nome + ancestrais, não só o nome próprio da categoria — corrigido depois da minha proposta inicial ter sugerido só o nome próprio; o usuário apontou que isso falha quando o lojista lembra pelo ramo ou quando há nomes repetidos entre ramos, ambiguidade que só o caminho completo resolve), lista achatada com o caminho completo em cada resultado, capada em 30 com aviso "mostrando 30 de N — refine a busca" se passar disso; **(2) navegação por nível** (sem texto digitado) — raízes na abertura, clicar numa com filhos entra nela (só os filhos diretos, nunca a árvore inteira), breadcrumb clicável pra voltar, e uma linha fixa "Selecionar `<nível atual>`" pra quando o próprio nível intermediário (não um filho) é o filtro desejado; **(3) recentes** — até 5 categorias usadas por último, aparecem no topo ao abrir sem digitar nem navegar. Seleção vira **chip removível** no lugar do botão "Categoria". Contador por categoria (reflete o filtro de status atual, decisão já aprovada no item 15) mantido em toda linha. Sem migration — reaproveita `useCategorias()` e o endpoint de contagem já existentes; toda filtragem (busca, filhos-por-nível, recentes) roda no client sobre a lista já buscada, sem novas queries ao servidor. `lib/category-tree.ts` ganhou `getPathSegments()` (breadcrumb clicável por segmento, ao lado de `getPath()` que já existia). **Limitação conhecida, registrada a pedido do usuário**: recentes ficam em `localStorage` do navegador — por dispositivo/navegador, não sincronizam entre a equipe nem entre computadores do mesmo funcionário; migrar pra sincronizado por usuário exigiria tabela nova + migration, deixado pra depois se for realmente necessário. **1 bug real encontrado e corrigido durante o teste**: `handleSelect` fechava o popover chamando `setOpen(false)` direto, sem passar pelo reset de busca/nível-atual que só rodava em `handleOpenChange` — reabrir o filtro depois de selecionar uma categoria dentro de um nível de navegação ficava preso naquele mesmo nível em vez de voltar pra raiz+recentes. Corrigido chamando `handleOpenChange(false)` no lugar. **Testado com Chromium real contra a URL pública em 10/08/2026, 28/28 checks automatizados passando** (depois do fix): raízes na abertura sem filhos nem categoria inativa aparecendo; drill-down em "Betas" mostra só os 2 filhos diretos, breadcrumb volta pra raiz; drill-down em "Ração" mostra só o filho **ativo** ("Ração de Superfície"), o inativo ("Ração para Peixes de Fundo") fica de fora; seleção de folha fecha o popover, vira chip, filtra a URL; "×" do chip limpa e volta pro botão "Categoria"; reabrir mostra "Recentes" com a categoria certa fixada no topo; busca por "Betas" encontra a própria categoria **e** "Beta Azul"/"Beta Negro" via caminho (confirmado que o nome "Beta Azul" sozinho não contém a substring "Betas" — prova que o match foi pelo caminho, não pelo nome próprio); selecionar direto um resultado de busca sempre seleciona (nunca navega, mesmo se a categoria tiver filhos); reabrir depois de selecionar não fica mais preso no nível anterior (fix confirmado); "Selecionar Betas" na linha fixa aplica a categoria-pai, não um filho; busca de produto por nome continua funcionando. Filtro de status combinado com categoria reconfirmado direto na API (`status=todos` com uma categoria retornou exatamente ativos+inativos somados, batendo com as contagens separadas de `status=ativos`/`status=inativos`) depois de uma leitura de tabela na UI ter mostrado vazio por timing (não reproduziu direto na API). Zero erros de console. Nenhum produto de teste criado nesta rodada (reaproveitou categorias/produtos já existentes) — nada pra limpar.

18. **📌 10/08/2026 — Sessão expirando ~1h sem renovação (pendência #2, aberta desde 01/08/2026): diagnosticada, causa raiz confirmada, corrigida e testada ao vivo.** Pedido do usuário: diagnóstico completo antes de qualquer correção, dado o histórico de bugs de cookie/redirect nesta área. **Diagnóstico** (leitura de código + `git log -- src/proxy.ts`): `getUser()`/`getSession()` já disparam refresh automaticamente quando faltam menos de 90s pra expirar (independe de `autoRefreshToken`) — o que decide se esse refresh fica de pé no cookie do navegador é só o `setAll` de quem chamou. `proxy.ts` tinha `setAll` vazio (descarta sempre); Server Components (`painel/layout.tsx`, `painel/page.tsx`, `page.tsx`) também descartam (cookie store é read-only nesse contexto, `ReadonlyRequestCookiesError` capturada em silêncio por `server.ts`); só Route Handlers de `/api/painel/**` (via `getStaffProfile()`) persistiam corretamente. **Achado que não estava nos docs**: o `setAll` vazio de `proxy.ts` não é a versão original — é o resultado de um incidente de debug real em 27-28/07/2026 (`git log` mostra o padrão oficial da Supabase pra middleware sendo implementado e depois revertido de propósito, "não sobrescreve cookie de sessão", sem causa raiz documentada na época; hipótese não confirmada: corrida entre o refresh do proxy e o de um Route Handler quase simultâneo). **Confirmado no dashboard Supabase, a pedido**: Access token expiry = 3600s (1h), **Refresh Token Rotation ativa** (reuse interval 10s) — confirma o pior cenário: o refresh descartado silenciosamente já **consumia** o refresh token da rotação sem o navegador saber, deixando o cookie com um refresh token já invalidado — sessão podia quebrar de formas menos previsíveis que só "expira limpo em 1h". **Correção implementada (Fase 1, escopo reduzido)**: `proxy.ts` volta a persistir a renovação — mesmas opções de cookie (`httpOnly, secure, sameSite=lax, path=/`) usadas em `server.ts`/login/sair, grava tanto no `request` encaminhado quanto na `response` (Server Components da mesma navegação já leem o token fresco, não tentam renovar de novo), log leve só quando um refresh de verdade acontece (não a cada request). Matcher do proxy **não foi alterado** — continua excluindo `/api/*`, o caminho de renovação via Route Handler que já funcionava fica intocado. Comentário desatualizado em `server.ts` ("middleware cuida da renovação") corrigido. **Fase 2** (incluir `/api/painel/*` no matcher, eliminando de vez a corrida entre proxy e Route Handler) fica pra depois, separada — só depois desta Fase 1 provar estável em uso real. **Testado com Chromium real contra a URL pública em 10/08/2026**: (a) 10/10 checks nos dois bugs de cookie já caçados aqui antes — login preserva sessão no redirect `/entrar`→`/`, logout via POST remove o cookie, deslogado em `/painel`→`/entrar` preservando `?proximo=`, sessão continua válida navegando depois do redirect; (b) **teste de renovação real**, com o JWT expiry baixado temporariamente pra 60s no dashboard (a pedido do usuário, ~~revertido pra 3600s logo depois do teste~~ **⚠️ correção de 21/08/2026: NÃO foi revertido de fato — ficou em 60s em produção até ser encontrado como causa raiz do bug bloqueador do item 48. A afirmação abaixo era o que se PRETENDIA fazer, registrada antes de confirmar que aconteceu de verdade — lição de processo, ver item 48.**): login, esperar passar dos 60s, navegar de novo — sessão continuou logada **e** o valor do cookie mudou (prova que renovou de verdade, não só sobreviveu por coincidência) — repetido em **2 ciclos consecutivos** pra descartar coincidência, os dois com sucesso. Zero erros de console nos dois testes. **Pendência #2 do topo do documento fica resolvida** — ver atualização inline em §2 "Padrão de autenticação" pro detalhe técnico completo.

19. **📌 10/08/2026 — Vitrine Fase 0 (fundação de tenant público, "Opção C" — ver §2) iniciada: plano + SQL propostos, aguardando revisão do usuário. Nada aplicado, nada implementado em código ainda.** Pré-requisito de segurança que bloqueia toda a Vitrine, pedido explicitamente antes de qualquer tela pública ser construída. Migration `028_vitrine_fase0_tenant_por_host.sql` criada (commitada, **não aplicada**) com: (1) tabela nova `tenant_domains` (host → tenant, um tenant pode ter vários domínios — pensada já pra Fase SaaS, sem precisar migrar de novo quando domínio próprio do cliente chegar) sem nenhuma policy de RLS pra `anon`/`authenticated` (acesso só via RPC, zero superfície de enumeração de domínio); (2) RPC `resolve_tenant_by_host` — único ponto que traduz host→tenant, sem fallback (host desconhecido/forjado nunca resolve pra tenant nenhum); (3) quatro RPCs públicas `SECURITY DEFINER` (`get_public_store_settings`/`get_public_categories`/`get_public_products`/`get_public_product_detail`), cada uma resolvendo o próprio filtro de tenant internamente a partir do slug (nunca aceitam `tenant_id` vindo de fora) — estoque numérico deliberadamente **não exposto** (só `esgotado`/`disponivel` derivados), código do produto só quando `codigo_visivel=true`, categoria inativa esconde a subárvore inteira (decisão #9 já registrada); (4) as 8 policies `*_select_anon` tenant-blind da migration `014` (sempre `using(true)`, abertas de propósito "até a Vitrine existir") **dropadas** — a partir de agora leitura anônima só entra pelas RPCs, fechando o segundo caminho de leitura que ficaria aberto se essas policies continuassem de pé. Proposta completa (incluindo como a resolução de tenant no proxy vai coexistir com a renovação de sessão da Fase 1 do item 18, tratamento de host forjado, e a associação do canário `_teste_isolamento` ao host de teste) apresentada ao usuário no bloco "PARA O PO/UX/ENGENHEIRO" da mesma sessão — **decisões e implementação de código (proxy.ts, tenant.ts, telas da Vitrine) ficam para depois da revisão**, registro completo aqui só depois de aplicado e testado com o mesmo rigor do canário que pegou os 3 vazamentos anteriores.

**Atualização de 10/08/2026, mesmo dia — plano aprovado com 1 ajuste, ainda aguardando aplicação.** Usuário aprovou (a) resolução no proxy sem redirecionar/bloquear, (b) host forjado sem fallback, (c) estoque numérico nunca exposto ao anônimo, (e) endurecer as 8 policies `*_select_anon` agora. **Ajuste pedido em (f)**: não reaproveitar o canário `_teste_isolamento` com toggle de `ativo` pro teste de isolamento público — risco de deixá-lo no estado errado e quebrar a ferramenta que já pegou os 3 vazamentos de RLS. Em vez disso, tenant de teste **dedicado** `_teste_vitrine` (nasce `ativo=true`, nunca precisa de toggle, mesmo padrão de identificação inequívoca do canário — slug `_teste_`, nome com "TESTE"). Migration `028` ajustada: **não carrega mais nenhum dado de teste, nem comentado** — seed/fixture movido pra `supabase/tests/vitrine_isolamento_test.sql`, script novo e separado, mesmo padrão já usado por `isolamento_test.sql` (idempotente, `begin`/`commit` explícitos por parte, testa positivo — host de teste vê o próprio dado — e negativo — host do Cauã nunca vê o dado de teste — e ainda confirma que host forjado não resolve pra tenant nenhum). Usuário pediu ver o SQL literal de uma RPC (`get_public_products`) antes de aprovar a aplicação — mostrado no mesmo bloco. **Ainda nada aplicado no banco.**

**Atualização de 10/08/2026 (mesmo dia) — migration `028` aplicada pelo usuário no SQL Editor e testada: isolamento público confirmado, 14/14 checks.** Usuário confirmou as 5 funções presentes no banco (`resolve_tenant_by_host` + 4 `get_public_*`). Teste de isolamento rodado em seguida (via `@supabase/supabase-js` + service role key, reproduzindo o mesmo roteiro de `supabase/tests/vitrine_isolamento_test.sql` — este ambiente não tem driver Postgres direto pra colar SQL num editor): criado o tenant dedicado `_teste_vitrine` (`ativo=true`, associado a `testeecommerce.vluma.com.br`), `capua` associado a `ecommercecauahml.vluma.com.br`. **Isolamento confirmado nos dois sentidos**: `get_public_products`/`get_public_categories` chamados com o slug `capua` nunca trazem o produto/categoria de teste; chamados com `_teste_vitrine` nunca trazem o produto real do Cauã (`Acará`); `get_public_product_detail('capua', 'teste-produto-vitrine')` devolve `null` (produto não existe nesse tenant). `resolve_tenant_by_host` confirmado certo nos dois hosts reais e devolvendo zero linhas pra um host forjado (`dominio-que-nao-existe.example.com`). Confirmado também que a resposta pública nunca inclui `saldo_estoque`/`estoque_total` (só o `esgotado`/`disponivel` derivado, decisão (c) aprovada) e que a leitura direta da tabela `products` com a **anon key** (bypassando as RPCs de propósito, simulando alguém tentando contornar o caminho oficial) vem vazia — as 8 policies `*_select_anon` estão mesmo dropadas, não só na teoria do SQL. **Migrations `001`–`028` todas aplicadas e validadas — nenhuma pendente.** Ver `MIGRATIONS.md` pro relato completo do teste.

**Pendência de funcionalidade aberta (não é gap de segurança) — leitura pública de `delivery_cities` ainda não existe.** A migration `028` fechou a leitura anônima tenant-blind de `delivery_cities` (dropou `cities_select_anon` junto com as outras 7) mas **não criou RPC pública nenhuma pra cidades de entrega** — decisão deliberada, não esquecida: a Vitrine ainda não decidiu como expor isso, só faz sentido junto do desenho do Carrinho/Checkout (cálculo de frete/área de entrega). Efeito prático: leitura anônima de `delivery_cities` está **completamente fechada** desde a `028` (nem tabela, nem RPC) — sem regressão hoje (nenhuma tela pública consulta isso ainda), mas é peça faltante que **bloqueia o Carrinho/Checkout pro visitante anônimo** até uma RPC `get_public_delivery_cities(p_tenant_slug)` (mesmo padrão `SECURITY DEFINER` das outras 4) ser criada nessa fase futura. Registrar como pendência a resolver quando o Carrinho/Checkout entrar no roadmap (ver §4 "Planejadas").

20. **📌 10/08/2026 — Pendência de funcionalidade (não é gap de segurança): RPC pública de `delivery_cities` ainda não existe, bloqueia Carrinho/Checkout pro visitante anônimo.** A migration `028` (Vitrine Fase 0, aplicada e testada — ver item 19 acima) fechou a leitura anônima tenant-blind de `delivery_cities` junto com as outras 7 tabelas, mas não criou nenhuma RPC pública equivalente (`get_public_delivery_cities`) — decisão deliberada, não esquecida: só faz sentido desenhar isso junto do Carrinho/Checkout (cálculo de frete/área de entrega), que ainda não começou. Efeito prático: leitura anônima de cidades de entrega está completamente fechada desde a `028` (nem tabela, nem RPC) — zero regressão hoje, mas é peça faltante a criar **quando o Carrinho/Checkout for desenhado** (mesmo padrão `SECURITY DEFINER` das outras 4 RPCs). Ver `MIGRATIONS.md` (entrada da `028`) pro detalhe completo.

21. **📌 10/08/2026 — Vitrine Fase 0, camada de código implementada e testada: `proxy.ts` resolve tenant por host, `tenant.ts` lê o resultado via header.** Depois do isolamento confirmado no banco (item 19), a peça de código: `src/proxy.ts` ganhou um bloco novo, posicionado **depois** dos dois redirects de auth existentes (nunca antes, nunca interferindo na renovação de sessão da Fase 1 — item 18) — chama `resolve_tenant_by_host` reaproveitando o client Supabase já instanciado pro fluxo de auth, e só anexa `x-tenant-id`/`x-tenant-slug` como headers do request encaminhado quando a RPC resolve algo; host sem match (ou erro na chamada) = fail-open, sem header, sem redirect, sem bloqueio — quem decide 404 é a página da Vitrine que ainda não existe. `src/lib/tenant.ts`: `getTenantSlug()`/`TENANT_SLUG` continuam intocados pro painel (single-tenant via env, nenhum call site usa hoje); `getTenantSlugFromHost()` (stub nunca implementado, só retornava a constante) removido — superado pelo fluxo real; `getTenantFromHeaders()` novo, lê os headers que o proxy anexou via `headers()` de `next/headers` (server-only, só pra Vitrine). **Testado contra a URL pública em 10/08/2026** com uma página de debug temporária (removida depois do teste, não fazia parte da Vitrine): `ecommercecauahml.vluma.com.br` resolve `{id, slug: "capua"}`; `testeecommerce.vluma.com.br` resolve `{id, slug: "_teste_vitrine"}` — os mesmos ids confirmados no teste de isolamento do item 19. **Host inválido testado em duas camadas**: um Host header totalmente forjado (`dominio-invalido-que-nunca-existe.example.com`) nem chega no código — bloqueado pelo próprio Vercel com 404 na borda; o domínio bruto do deploy (`ecommercecauadev.vercel.app`, que o Vercel roteia normalmente pra esta aplicação mas não está cadastrado em `tenant_domains`) chega até o proxy e resolve `tenant: null` — sem fallback pra nenhum tenant default, confirmado no código de verdade, não só na RPC isolada. **Sessão de autenticação reconfirmada intacta**: login, visita a uma rota não-protegida (exercitando o mesmo trecho de "passa direto" que agora também resolve tenant, com sessão ativa ao mesmo tempo), cookie de sessão continua presente depois da resolução de tenant, `/painel/produtos` continua acessível sem redirecionar, logout continua funcionando — 6/6 checks. Zero erros de console em todos os testes. **Vitrine Fase 0 (fundação de tenant público) está completa** — banco e código, testados. Próximo passo (fora do escopo desta Fase 0) é o desenho das telas da Vitrine em si, ainda não iniciado.

22. **📌 Fechamento de sessão, 10/08/2026 — checklist de encerramento seguro, ler isto primeiro numa sessão nova.** `git status`/`git log origin/main` conferidos limpos ao encerrar: local = `origin/main` (`8b1fec4`), nada só local, nada não commitado (exceto o eterno `novo 6.txt`, item 5, intocado). Migrations `001`–`028` todas aplicadas e validadas no banco — nenhuma pendente, nenhuma migration nova criada e não aplicada. Docs (`ESCOPO_PROJETO.md`, `REGRAS_DE_NEGOCIO.md`, `MIGRATIONS.md`) atualizados e pushados ao longo de toda a sessão, refletindo o estado real a cada passo.

    **⚠️ Discrepância encontrada e verificada antes de responder, não presumida**: ao encerrar, o usuário pediu confirmação do estado de uma "Fase 1 da Vitrine" com 4 etapas planejadas (1-dados de teste de peixes ornamentais, 2-vitrine pública, 3-campos editáveis em `store_settings`, 4-tela de Configurações da Vitrine). **Esse plano de 4 etapas não existe em nenhum lugar verificável**: não está em nenhum commit desta sessão (`git log --oneline --all | grep -i vitrine` só traz os commits da Fase 0, item 19/21 acima), não está em nenhum dos 3 docs vivos, e a checagem direta no banco (`categories`/`products` do tenant `capua`, mais recentes primeiro) não mostra nenhum dado temático de "peixes ornamentais" criado nesta sessão — as categorias mais recentes do capua são as reais e pré-existentes (Ciclídeo, Betas, Plantas, Ração, todas de 29/07–08/08), e os produtos mais recentes são todos lixo de teste já inativado das rodadas de UX/auth desta sessão (nomes `TESTE regressivo final ...`, `TESTE diag ...`), sem relação com peixes ornamentais. **A única "Fase 1" real registrada em qualquer lugar é a da sessão expirando (auth, item 18) — sem relação com a Vitrine.** Hipótese mais provável: um plano que o usuário tem em mente (ou anotado fora deste repositório) mas que ainda não foi comunicado nesta sessão — não uma perda de informação de uma sessão anterior, já que não há nenhum rastro dele em lugar nenhum revisável. **Pendência real pra próxima sessão**: usuário precisa descrever esse plano de 4 etapas (ou confirmar que ainda não existe) pra virar trabalho de verdade — não presumido nem inventado aqui.

    **Pendências reais confirmadas pra próxima sessão** (tudo que segue em aberto, sem mudança de estado além do que já está registrado acima): (a) item 20 — RPC pública `get_public_delivery_cities` ainda não existe, bloqueia Carrinho/Checkout anônimo, fica pra quando essa fase for desenhada; (b) `novo 6.txt` (item 5) — segue intocado, sem instrução do usuário; (c) desenho das telas da Vitrine em si (`(loja)/**`) — Fase 0 (fundação) está completa e testada, mas nenhuma tela pública foi construída ainda, e não há um plano formal de próximas fases registrado (ver discrepância acima); (d) Fase 2 da sessão de 1h (incluir `/api/painel/*` no matcher do proxy) — deliberadamente adiada até a Fase 1 provar estável em uso real, sem prazo definido.

23. **📌 12/08/2026 — Plano da Vitrine Fase 1 (4 etapas) recebido do usuário, resolvendo a discrepância do item 22; Etapa 1 (dados de teste) concluída.** Plano completo: (1) dados de teste — catálogo realista de peixes ornamentais no tenant `capua`; (2) telas públicas (home, listagem, ficha de produto) consumindo as RPCs da Fase 0; (3) campos editáveis em `store_settings` (banner, selos, identidade) com valores padrão; (4) tela de Configurações da Vitrine para o lojista editar esses campos. Regra de método fixada pelo usuário: uma etapa por vez, validar na URL pública antes da próxima, migrations só aplicadas pelo usuário após revisão.

    **Contexto importante, esclarecido pelo usuário nesta sessão**: este ambiente (`capua`, `ecommercecauahml.vluma.com.br`) é **HML** — a produção será um ambiente **novo e separado** (banco novo, branch à parte, a decidir depois). Dados de teste populados aqui **nunca vão para produção**; não há preocupação de "sujar produção" nesta fase.

    **Etapa 1 (dados de teste) — ✅ concluída e validada em 12/08/2026.** Populado direto no tenant real `capua` (não no `_teste_vitrine`, reservado só para o teste de isolamento de segurança da Fase 0). Método: script `scripts/seed_vitrine_fase1_dados_teste.mjs` (Node, `SUPABASE_SERVICE_ROLE_KEY`) — não usa migration (não muda schema) nem as RPCs de painel (`criar_produto_com_variacoes` etc. exigem `is_staff()`, que depende de `auth.uid()`, inexistente numa chamada via service role sem sessão de staff real) — em vez disso replica em JS a mesma lógica que essas RPCs/rotas aplicam (`derivarPrefixo()`/`abreviar_rotulo()` — mesma regra por palavras, decisão #24 —, geração de código via `product_code_sequences`, SKU automático com sufixo anticolisão, estoque inicial sempre via `stock_movements` tipo `inventario`, nunca gravando saldo direto). Estendida a árvore real já existente (Ciclídeo/Betas/Plantas/Ração), sem tocar nos 3 produtos reais pré-existentes (`Acará`, `Oscar`, `BETA SUPER RED`): 7 subcategorias novas (Acarás, Oscars, Ciclídeos Anões sob Ciclídeo; Beta Halfmoon, Beta Plakat sob Betas; Plantas de Fundo, Plantas Flutuantes sob Plantas), 28 características (`category_attributes`) distribuídas entre as 7 subcategorias novas + a raiz Ração, 20 produtos novos com nomes realistas (sem prefixo "TESTE" — decisão do usuário, já que os dados aparecem na vitrine pública pra validação visual da Etapa 2; a rastreabilidade fica só no manifesto), 24 variações, 20 movimentações de estoque inicial, 58 valores de característica preenchidos, 20 imagens placeholder (cor sólida + nome do produto, geradas com `sharp`, upload real no bucket `product-images`). Cobertura deliberada de casos de borda: 3 produtos totalmente esgotados (`Acará Bandeira Marmorizado`, `Oscar Albino`, `Betta Plakat Cambodia` — este último e o primeiro também em promoção, testando os dois badges simultâneos), 1 produto com uma variação esgotada mas produto disponível (`Ração em Flocos Tropical`), 5 produtos/variações em promoção, 5 produtos marcados `destaque`, mix de `codigo_visivel` true/false. Todo ID criado ficou registrado em `docs/vitrine_fase1_seed_manifest.json` — mecanismo de reversibilidade (sem urgência, ambiente é HML, mas mantido como boa prática a pedido do usuário).

    **Validação nas RPCs públicas (chamadas com a `anon key`, não a service role, simulando a vitrine de verdade)**: `get_public_categories('capua')` retorna 14 categorias (as 7 novas + as 3 pré-existentes + as 4 raízes), corretamente **sem** as 2 categorias de teste inativas (`TESTE características etapa2 A/B`) nem a subcategoria inativa pré-existente (`Ração para Peixes de Fundo`) — confirma que a regra "categoria inativa esconde a subárvore" (decisão #9) continua correta com os dados novos misturados. `get_public_products('capua')` retorna 23 produtos (20 novos + 3 reais), com `esgotado`/`em_promocao`/`destaque`/`codigo` (só quando `codigo_visivel=true`) calculados exatamente como esperado, e os 3 produtos reais pré-existentes aparecendo corretamente **sem** imagem (nunca foram tocados). `get_public_product_detail('capua', slug)` testado em 3 produtos variados (2 variações com promoção, produto esgotado, planta) — imagens/variações/características retornando certo. **Checagem de segurança**: resposta das RPCs públicas nunca contém `saldo_estoque` numérico, só o `esgotado`/`disponivel` derivado (mesma garantia já validada na Fase 0, reconfirmada com o volume de dados novo).

    **Correção de 12/08/2026 (mesma sessão, antes da Etapa 2): imagens placeholder trocadas por fotos reais e genéricas por espécie/tipo.** A pedido do usuário — as imagens de cor sólida validavam a mecânica da galeria, mas não davam pra avaliar visualmente os cards/telas da Etapa 2. Script `scripts/atualizar_imagens_vitrine_fase1.mjs` busca no **Wikimedia Commons** (acervo de imagens livres/CC) uma foto por espécie/tipo (ex.: Betta vermelho para "Betta Halfmoon Vermelho", disco azul para "Acará Disco Azul", flocos de ração para os produtos de Ração) via a API de busca do Commons, converte pra WebP (`sharp`) e **sobrescreve o mesmo objeto no Storage** (mesmo `storage_path` do manifesto, `upsert:true`) — a linha em `product_images` não muda, só o conteúdo do arquivo. Sem re-seed de produto nenhum. Das 20 imagens, 15 vieram certas na primeira busca automática; 5 não encontraram resultado e 3 vieram com match ruim (verificados **visualmente** baixando os thumbnails antes de aceitar — um caso trocava a imagem de "Ração em Pellets" por uma foto de **camarão**, outro usava uma gravura antiga de livro de 1908 para "Ração em Flocos Tropical", e um terceiro repetia a mesma foto de nadadeira azul tanto em "Betta Halfmoon Azul Royal" quanto em "Betta Plakat Amarelo" — claramente errado pra um produto "amarelo"). As 8 corrigidas manualmente (`scripts/corrigir_imagens_vitrine_fase1.mjs`, títulos escolhidos a dedo depois de inspeção visual): `Acará Bandeira Prata`, `Apistogramma Cacatuoides`, `Java Fern`, `Betta Plakat Amarelo`, e as 4 de Ração (2 usam a mesma foto genérica de comida seca variada — aceitável, o pedido foi "genéricas por tipo"). Créditos (autor/licença/URL da fonte no Commons) de cada imagem final registrados em `docs/vitrine_fase1_creditos_imagens.json` — a maioria CC BY-SA, uma em domínio público. **Nota para produção**: essas imagens continuam sendo dados de teste (o próprio Wikimedia Commons, não fotos do Cauã) — se alguma acabar indo pra produção por engano, imagens CC BY-SA pedem atribuição visível; o ideal é trocá-las pelas fotos reais do cliente antes disso, e o manifesto/créditos deixam claro quais são de teste.

    **Etapas 3 e 4 do plano ainda não iniciadas.**

24. **📌 12/08/2026 — Vitrine Fase 1, Etapa 2 concluída: as 3 telas públicas construídas e testadas contra a URL de HML (`ecommercecauahml.vluma.com.br`).** Consomem só as 4 RPCs públicas da Fase 0 (`get_public_store_settings`/`get_public_categories`/`get_public_products`/`get_public_product_detail`) — nenhuma tabela lida direto, tenant sempre resolvido via `getTenantFromHeaders()` (header anexado pelo proxy, nunca hardcoded).

    **Tema**: design aprovado usa teal água (`#0891b2`), mas o `globals.css` já tinha os mesmos tokens semânticos (`--primary` etc.) definidos globalmente em navy/vermelho pra marca do Cauã, usados pelo `/painel` inteiro — sobrescrever `:root` quebraria o painel. Resolvido com uma classe `.loja-theme` escopada (aplicada só no wrapper do layout da Vitrine) que redefine os mesmos nomes de token com os valores teal — como `@theme inline` mapeia `--color-primary: var(--primary)` (referência, não valor fixo), qualquer `bg-primary`/`text-primary` etc. dentro de `.loja-theme` resolve pro teal via cascata normal de CSS, sem duplicar componentes nem criar variantes novas de Button/Badge/etc. Painel continua 100% navy/vermelho, sem nenhuma mudança visual.

    **Conflito de rota resolvido**: já existia `src/app/page.tsx` (landing "em breve" antiga, link morto pra `/loja`, sem resolução de tenant) ocupando a rota `/` — colidia direto com `(loja)/page.tsx` (route group não muda a URL). Removido — seu papel (link pra `/entrar`/`/painel` quando logado) fica só implícito no header da Vitrine (link "Entrar" sempre visível); nada mais no código referenciava esse arquivo.

    **Home (`/`)**: header (nome da loja, busca, "Entrar", carrinho — visual, mostra toast "carrinho chega na próxima fase"), nav de categorias raiz, banner hero (título/subtítulo/botão **hardcoded no componente por enquanto** — Etapa 3 move pra `store_settings`), 4 selos de confiança (hardcoded, mesma nota), grid de categorias raiz com ícone heurístico por palavra-chave no nome (peixe/planta/ração — categoria não tem campo de ícone no modelo), grid de produtos `destaque=true` com badges Promoção/Novidade/Esgotado (podem aparecer combinados, ex. esgotado **e** em promoção ao mesmo tempo — caso de teste real da Etapa 1).

    **Listagem (`/categoria/[slug]` e `/produtos`)**: mesmo componente (`ListagemPage`) atende as duas rotas — a RPC `get_public_products` só filtra por match exato de `category_id` (decisão de propósito da migration 028, "fica pra UI decidir hierarquia"), então a inclusão de subcategorias na listagem por categoria (`getSelfAndDescendantIds`, `src/lib/loja/category-tree.ts`) é resolvida aqui. `/produtos` sem parâmetro = todos os produtos; com `?q=` (vindo da busca do header) = filtro por nome/descrição. Filtros (disponibilidade, promoção), ordenação (relevância/menor preço/maior preço/novidade/nome) e a árvore de categorias com contador **rodam no Server Component a partir de `searchParams`** — mesmo padrão de URL state já usado no painel, sem estado de filtro duplicado no client; contador de cada categoria respeita busca/disponibilidade/promoção já aplicados mas não a categoria em si (mesmo princípio já usado no contador do painel).

    **Ficha de produto (`/produto/[slug]`)**: galeria com thumbnails (imagens de variação, quando existirem, ficam de fora — só as do produto), variações como botões (nome + preço, promoção riscada, esgotado desabilitado), seletor de quantidade respeitando `quantidade_minima_venda` da variação selecionada, botão "Adicionar ao carrinho" (desabilitado e com texto "Esgotado" quando a variação selecionada não está disponível; carrinho de verdade é Fase 2 — clique mostra toast), ficha técnica (características) e descrição.

    **Testado com Chromium real contra a URL pública em 12/08/2026** (dois lotes, Home e depois Listagem+Ficha): Home — header/nav/banner/selos/grid de categorias/grid de destaques renderizando corretamente em desktop (1280×900) e mobile (390×844), sem bug de layout, zero erro de console (os únicos 404 registrados são prefetch automático do Next para rotas ainda não visitadas, comportamento esperado); clique em categoria e em produto navegando corretamente. Listagem (`/categoria/betas`) — árvore com contadores corretos, categoria ativa destacada, filtro "somente em promoção" reduzindo de 6 para 1 produto e atualizando a URL (`?promocao=1`), ordenação "Menor preço" reordenando por preço ascendente e atualizando a URL, clique em subcategoria (`Beta Halfmoon`) navegando e atualizando breadcrumb/grid. Ficha de produto (`Acará Disco Azul`) — seleção de variação "Médio" destacando o botão e atualizando o preço grande pra `R$ 65,00` com `R$ 75,00` riscado, quantidade incrementando corretamente, "Adicionar ao carrinho" mostrando o toast esperado; ficha de produto esgotado (`Oscar Albino`) — variação, botão de quantidade e "Adicionar ao carrinho" todos desabilitados corretamente, botão mostrando "Esgotado". Zero erros de console nos dois lotes.

    **Achado de dados, não é bug de código**: o teste de `/categoria/betas` mostrou o produto real pré-existente `Oscar` (ciclídeo) aparecendo dentro de "Betas" — investigado, é um dado real já existente no banco antes desta sessão (`category_id` desse produto já apontava pra "Betas", confirmado no diagnóstico somente-leitura feito antes da Etapa 1) — não foi tocado nem criado por este trabalho, e a instrução foi explicitamente não mexer nos 3 produtos reais pré-existentes. Provável miscategorização de uma sessão anterior. Fica registrado aqui, sem correção automática — ver bloco PARA O PO/UX/ENGENHEIRO.

    **Estado de `store_settings` do `capua` em HML alterado pra testar**: `loja_aberta`/`pedidos_abertos` estavam `false` (loja fechada), o que faria a Vitrine mostrar só a mensagem de loja fechada em qualquer tela — mudados pra `true` direto no banco (dado de configuração, não schema/migration) pra poder validar as 3 telas. Ambiente é HML, sem efeito em produção.

    **Pendência real pra próxima sessão**: Etapas 3 (campos editáveis em `store_settings`: banner, selos, identidade) e 4 (tela de Configurações da Vitrine) do plano, ainda não iniciadas — banner/selos/ícone de categoria seguem hardcoded no código por enquanto.

25. **📌 13/08/2026 — Vitrine: fidelidade ao mockup aprovado (`docs/mockups/mockup-vitrine-completa.html`) nas 3 telas + logo real do Criatório Capuã aplicada.** Ajuste solicitado pelo usuário depois de ver a Etapa 2 (item 24) — a home "ficou aquém do mockup" — sem mudança de dado/RPC, só de camada visual em cima do que já existia.

    **Logo**: `public/brand/logocp.jpeg` (1284×1135, render 3D com fundo gradiente azul-claro e sombra projetada, sem transparência) tratada em duas variantes, mantendo o arquivo original intocado. Remoção de fundo por **flood-fill a partir das 4 bordas da imagem** (não threshold global de cor — o gradiente escurece bastante no canto inferior-direito, o que fazia um threshold único classificar parte do fundo como logo e vice-versa; flood-fill segue a continuidade suave do gradiente e para nas bordas duras do desenho, robusto ao gradiente não-uniforme). Recortado só o emblema (par de peixes entrelaçados), sem o wordmark "CRIATORIO CAPUÃ" (que já existe como texto real no header/footer) — `logocp-icone.png` (cores originais, cabeçalho branco) e `logocp-icone-branco.png` (silhueta branca lisa — o peixe azul-marinho do arquivo original é quase preto, ficaria ilegível sobre o rodapé escuro do mockup `#0f172a`).

    **Home**: topbar de aviso (retirada/pedido mínimo — oculta o segmento "Pedido mínimo" quando `valor_minimo_pedido=0`, caso real do `capua` em HML que o mockup não cobre porque lá o texto é fixo)/nav de categorias virou barra teal sólida com "☰ Todas as categorias" como primeiro item; hero passou de faixa full-bleed pra card contido com cantos arredondados e peixe decorativo translúcido; selos de confiança viraram cards brancos com borda (título + subtítulo, antes só um texto corrido); grid de categorias com link "Ver todas →" e 6 colunas em telas largas; cards de produto ganharam rótulo de categoria, hierarquia de preço/unidade em duas linhas e botão explícito "Ver produto"/"Indisponível" (esgotado); cores das badges "Novidade"/"Esgotado" corrigidas pra verde/vermelho (estavam usando a cor primária e um cinza neutro, respectivamente — o mockup usa verde/vermelho, mesma paleta do resto do projeto pra sucesso/erro).

    **Listagem**: sidebar de filtros virou um card único com borda (árvore de categorias + bloco "Disponibilidade", antes o segundo vivia numa toolbar horizontal acima do grid); grid de produtos passou a usar 3 colunas em telas largas (era o mesmo componente com 4-5 colunas da home — `ProductGrid` ganhou uma prop `variante` pra diferenciar).

    **Ficha de produto**: linha de categoria (breadcrumb curto) acima do título, código do produto em fonte monoespaçada ("Cód. X", igual mockup); preço da variação selecionada maior (era `text-2xl`, virou próximo de 32px como no mockup); adicionado o texto "✓ Disponível" em verde (não existia) e a nota de retirada combinada por cidade (antes só aparecia a quantidade mínima, e só quando > 1); ficha técnica trocou de caixa com borda pra linhas com separador (`border-b`), igual ao mockup.

    **Rodapé**: trocado de uma coluna simples pra 4 colunas escuras (`#0f172a`, igual mockup) — marca+descrição, "Loja" (links reais: todas as categorias, promoções, novidades), "Ajuda" e "Sobre" (texto estático, sem link — as páginas de destino não existem ainda no roteador; o próprio mockup também não tem `onclick` nesses itens, então isso é fiel ao original, não um corte).

    **Decisão de dado, não implementada**: o mockup mostra "a partir de" antes do preço em alguns cards (quando o produto tem variações com preços diferentes) mas não em outros (preço único). A RPC pública (`get_public_products`) só devolve `preco_a_partir_de` no nível de produto, sem indicar se há 1 ou várias variações — não dá pra saber quando o prefixo é preciso sem estender a RPC. Mantido sem o prefixo por enquanto (mesmo comportamento de antes desta sessão) pra não mudar RPC sem revisão de SQL do usuário; registrado aqui como gap de dado, não esquecimento.

    **Testado com Chromium real contra a URL de HML (`ecommercecauahml.vluma.com.br`) em 13/08/2026**, desktop (1400×1000) e mobile (390×844): Home, Listagem (`/produtos`) e Ficha de produto (`/produto/acara`) — logo renderizando nas duas variantes (colorida no header, branca no rodapé), topbar sem o texto "Pedido mínimo" (loja do `capua` tem `valor_minimo_pedido=0`), badges de cor corrigida, grid da listagem em 3 colunas com sidebar em card, "✓ Disponível" e nota de retirada na ficha, ficha técnica em linhas. Responsivo conferido em mobile nas 2 telas (Home e Listagem) — sem quebra de layout. Zero erro de console/página em todos os testes. `npm run build` limpo antes do push.

    **Correção pós-entrega, mesmo dia (13/08/2026), a partir de print enviado pelo usuário**: dois problemas reais no resultado acima. (1) **Logomarca ilegível em tamanho pequeno** — o emblema (par de peixes entrelaçados) tem detalhe fino (nadadeiras serrilhadas, olho) que não sobrevive ao encolhimento pra 28-32px (header) / 20px (rodapé): virava um borrão sem se reconhecer como a logo real. Confirmado simulando o downscale localmente antes de decidir o tamanho novo: em 48px o desenho já lê claramente como dois peixes. Corrigido aumentando pra 36-40px no header e 40px no rodapé, e trocando a variante do rodapé de volta pra colorida (a silhueta branca lisa perdia o peixe azul-marinho inteiro contra o fundo escuro `#0f172a`, mesmo grande — só a versão colorida com os realces claros mantém as duas metades legíveis). (2) **Nome da loja sem acentuação** ("Criatorio Capua" em vez de "Criatório Capuã") — não era bug de código: `nome` vem direto de `tenants.nome` via `get_public_store_settings` (RPC da migration 028), e o dado real gravado no banco pro tenant `capua` estava sem acento. Corrigido com um `UPDATE` direto (dado de conteúdo, não schema — mesmo padrão já usado antes pra `store_settings.loja_aberta`/`pedidos_abertos`) pra `"Criatório Capuã"`. Reconfirmado com Chromium real contra a HML depois do push: logo legível em header e rodapé (zoom de verificação no pixel), nome acentuado corretamente em todas as ocorrências (header, rodapé, `<title>`, rodapé de copyright).

26. **📌 13/08/2026 — "A partir de" nos cards de produto passa a seguir o padrão de mercado (Shopify): só aparece quando há faixa de preço real.** Resolve o gap de dado registrado no item 25 acima. Pedido do usuário, 3 partes.

    **Parte 1 (migration `029`, criada e pushada, aguardando aplicação manual)**: `get_public_products` ganha a coluna `preco_varia` (boolean) — `true` quando há variações ativas com preço **base** (não o promocional) diferente entre si, via `max(v.preco) filter (where v.ativo) is distinct from min(v.preco) filter (where v.ativo)` (`is distinct from` trata `null` certo pra produto sem variação ativa, sem virar falso-positivo). Precisou `drop function` antes do `create` — mudar o `RETURNS TABLE` de uma função com a mesma assinatura de entrada é um caso que o Postgres **recusa explicitamente** (`cannot change return type of existing function`), diferente de mudar parâmetros de entrada (que cria overload silencioso em vez de recusar — lição das migrations `025`/`026`). Isolamento por tenant, `security definer` e a garantia de nunca expor estoque numérico confirmados inalterados via diff automatizado contra a `028` — só a coluna nova e a troca `create or replace`→`drop`+`create`. Detalhe completo em `MIGRATIONS.md` (linha da `029`).

    **Parte 2**: `ProductCard` (usado por Home e Listagem, mesmo componente) só renderiza o texto "a partir de" acima do preço quando `produto.preco_varia === true`; preço único ou variações no mesmo preço mostram só o valor, sem prefixo. `ProdutoPublico`/`Database['Functions']['get_public_products']` (tipos) atualizados pra incluir o campo novo. **Frontend deployado antes da migration ser aplicada, de propósito, e tolerante aos dois estados**: sem a migration, `preco_varia` vem `undefined` da API real (RPC ainda na versão antiga) — `undefined` é falsy em JS, então o prefixo simplesmente não aparece em lugar nenhum (comportamento idêntico ao que já existia antes desta feature), sem erro de runtime nem quebra de tipo (build/typecheck passam porque o tipo TS já assume o formato novo, só a API ainda não confirma em tempo de execução). Assim que a migration for aplicada, o prefixo passa a aparecer sozinho, sem novo deploy de código.

    **Parte 3 (ficha de produto)**: conferido — **já funcionava**, nenhum código mudou. `VariacoesSelector` (`src/components/loja/produto/variacoes-selector.tsx`) deriva o preço exibido de `selecionada` (`useMemo` sobre `selecionadaId`, atualizado pelo `onClick` de cada botão de variação), então trocar de variação já atualiza o preço grande (e o risco de preço promocional) na hora — confirmado com Chromium real contra a HML: `Acará Disco Azul` mostrando `R$ 45,00` na variação "Pequeno" (padrão, primeira disponível) e `R$ 65,00` (com `R$ 75,00` riscado) ao clicar em "Médio".

    **Testado contra a HML em 13/08/2026 (pré-aplicação da migration, comportamento esperado nesse estado)**: `/produtos` — zero ocorrências de "a partir de" em qualquer card (correto, `preco_varia` ainda não existe na API real), preços continuam aparecendo normalmente. Ficha de `acara-disco-azul` — troca de variação atualizando preço corretamente (Parte 3, acima). `npm run build` limpo antes do push. **Pendência real**: falta o usuário aplicar a migration `029` pra Parte 1/2 saírem do estado dormente — depois de aplicada, re-testar um produto com faixa de preço (ex.: `Acará Disco Azul`, `R$ 45`–`R$ 75`) mostrando "a partir de R$ 45,00" no card, e um produto de preço único (ex.: a maioria do catálogo de teste) continuando sem o prefixo.

    **Migration `029` aplicada pelo usuário em 13/08/2026, re-teste contra a HML confirmado no mesmo dia**: RPC `get_public_products` devolvendo `preco_varia` de verdade (confirmado direto via REST antes do teste de UI). Listagem (`/produtos`, 23 produtos) — exatamente os 5 produtos com faixa de preço real mostrando "a partir de" (`Acará Disco Azul` R$45–75, `Anúbia Nana` R$15–28, `Oscar` R$30–50, `Ração em Flocos Tropical` R$9,90–34,90, `Ração em Flocos Coloridos` R$8,90–32,90) e os outros 18 (preço único) sem o prefixo — nenhum falso positivo nem falso negativo. Ficha de `acara-disco-azul` — variação "Pequeno" (padrão) mostra R$ 45,00, clicar em "Médio" atualiza pra R$ 65,00 (com R$ 75,00 riscado) — Parte 3 reconfirmada. Feature "a partir de" da Vitrine está 100% ativa em produção/HML, nada mais pendente deste item.

27. **📌 13/08/2026 — Vitrine Fase 1, Etapa 3: banner, selos de confiança, WhatsApp e identidade (cor principal) deixam de ser fixos no código e viram campo em `store_settings`.** Etapa 4 (tela de edição no painel) segue não iniciada — esta etapa só cria os campos com DEFAULT = valor hoje hardcoded e faz a vitrine ler deles, sem UI de edição ainda.

    **Migration `030` (criada e pushada, aguardando revisão/aplicação manual)**: `ALTER TABLE store_settings ADD COLUMN IF NOT EXISTS` pra `banner_titulo`/`banner_subtitulo`/`banner_botao_texto`/`banner_botao_href`/`banner_tipo_fundo` (`'cor'|'imagem'`, CHECK)/`banner_cor_fundo` (hex, CHECK)/`banner_imagem_path` (nullable, sem CHECK contra `banner_tipo_fundo` de propósito — evita travar o fluxo de edição da Etapa 4 no meio de uma troca), `selos` (JSONB, 4 itens fixos — CHECK `jsonb_array_length(selos) = 4` —, cada um com `titulo`/`subtitulo`/`icone`/`ativo`), `whatsapp_numero` (nullable, CHECK formato E.164 sem símbolos) e `whatsapp_mensagem`, `cor_principal` (hex, CHECK). Todos os DEFAULT são o valor que já estava hardcoded em cada componente antes desta migration — aplicar não muda a aparência da vitrine em nenhum tenant existente. `get_public_store_settings` estendida pra devolver os campos novos — mesmo tratamento `drop function` + `create` da migration `029` (mudar `RETURNS TABLE` com a mesma assinatura de entrada não aceita `create or replace`), isolamento de tenant e `security definer` confirmados inalterados via diff automatizado contra a `028`. Nenhum campo novo é sensível (conteúdo de marketing/identidade, já pensado pra vitrine pública).

    **Frontend deployado ANTES da migration, de propósito (evita o incidente de contrato quebrado da migration `027`)**: `getPublicStoreSettings` (`src/lib/loja/rpc.ts`) mescla um objeto de defaults — idênticos aos `DEFAULT` das colunas — por cima do retorno real da RPC (`{...DEFAULTS, ...data[0]}`). Enquanto a `030` não roda, a RPC antiga não devolve essas chaves e os defaults preenchem sozinhos, sem `undefined` vazando pros componentes (mais estrito que o padrão de tolerância da `preco_varia`, que dependia de `undefined` ser falsy — aqui a garantia é "sempre populado", não "nunca quebra"). Confirmado com Chromium real contra a HML nesse estado: home pixel-idêntica à Etapa 2, zero erro de console.

    **Onde cada campo aparece**: banner (`banner-hero.tsx`) — gradiente construído a partir de `banner_cor_fundo` via `color-mix` inline (não fica achatado/sólido, mantém o efeito de gradiente que já existia) quando `banner_tipo_fundo='cor'`; imagem de fundo (`next/image` + `urlArquivoLoja()`, mesmo bucket público `product-images` das fotos de produto, sem bucket novo) com overlay `bg-black/45` automático quando `'imagem'` — cai pra `'cor'` se `banner_imagem_path` estiver vazio mesmo com `tipo_fundo='imagem'` (nunca renderiza sem fundo nenhum); cor do texto do banner calculada por contraste (`src/lib/loja/cor.ts`, luminância simplificada, não é a fórmula de contraste completa do WCAG) a partir de `banner_cor_fundo`, pra não ficar texto branco ilegível se o lojista escolher uma cor clara. Selos (`selos-confianca.tsx`) — mapa fixo de ícones pré-definidos (`truck`/`shield-check`/`headset`/`fish`/`leaf`/`sprout`/`package`/`heart`/`star`, chave desconhecida cai no ícone padrão em vez de quebrar), selo com `ativo=false` some da vitrine. WhatsApp (`whatsapp-float-button.tsx`, novo, renderizado no layout) — botão flutuante fixo, só aparece com `whatsapp_numero` configurado, link `wa.me/{numero}?text={mensagem url-encoded}`. Identidade — `cor_principal` aplicada como `--primary`/`--ring` via `style` inline no wrapper `.loja-theme` do layout (maior especificidade que a classe, sobrescreve sem tocar `--secondary`/`--accent`, que continuam neutros — escopo desta etapa é só a cor principal, não uma paleta derivada inteira), `--primary-foreground` calculado por contraste.

    **Imagem de banner de teste preparada pro re-teste pós-migration**: `File:African Cichlid Fish Tank.jpg` (Wikimedia Commons, CC BY-SA 4.0, autor LizWinfreyV — crédito completo em `docs/vitrine_fase1_creditos_imagens.json`), recortada pra proporção de banner (~2,3:1) e já enviada pro bucket `product-images` em `_store-settings/capua/banner.webp` (confirmado publicamente acessível, HTTP 200). **Ainda não ligada** em `store_settings` (`banner_tipo_fundo`/`banner_imagem_path` do tenant `capua`) — depende das colunas da migration `030` existirem primeiro; assim que o usuário confirmar a migration aplicada, o próximo passo é um `UPDATE` direto (dado de conteúdo, mesmo padrão já usado pra `loja_aberta`/`nome` do tenant) setando essas duas colunas pro `capua`, e então o re-teste completo (banner com foto + overlay, cor configurável, selos, WhatsApp).

    **Pendência real pra próxima sessão/continuação**: aguardando o usuário revisar e aplicar a migration `030`; depois disso, ligar a imagem de teste + validar cor configurável + selos + WhatsApp na HML, e só então a Etapa 3 fica 100% fechada. Etapa 4 (tela de edição no painel pro lojista mexer nesses campos sem SQL) segue não iniciada.

    **Migration `030` aplicada pelo usuário em 13/08/2026, re-teste completo contra a HML confirmado no mesmo dia**: `get_public_store_settings` devolvendo todos os campos novos com os defaults corretos (confirmado via REST antes do teste de UI). **Banner com imagem + overlay**: `banner_tipo_fundo='imagem'` + `banner_imagem_path='_store-settings/capua/banner.webp'` ligados via `UPDATE` direto (dado de conteúdo) — foto do aquário de ciclídeos aparecendo com o overlay escuro automático, texto totalmente legível. **Cor do banner configurável**: testado com `banner_cor_fundo='#ea580c'` (laranja, bem diferente do teal default) — gradiente recalculado a partir da cor nova, texto branco continuou legível (contraste calculado certo); revertido pro default `#0891b2` depois do teste (banner ficou em modo imagem, então o valor de cor nem aparece mais, mas evita deixar um resíduo de teste no dado). **Identidade (`cor_principal`)**: testado com `#7c3aed` (violeta) — confirmado propagando pra topbar/nav/logo/links/ícones de categoria em todo o site, **sem** afetar a cor do banner (prova que os dois campos de cor são independentes, como desenhado); revertido pro teal default depois do teste. **Selos**: testado com um selo desativado (`ativo=false`) e outro com título/subtítulo editados — vitrine mostrou exatamente 3 selos (o inativo sumiu) com o texto editado aparecendo corretamente; revertido pros 4 selos originais depois do teste. **WhatsApp**: testado com número de teste fictício (`5511900000000` — **não é o WhatsApp real da loja**, precisa ser trocado pelo número de verdade antes de divulgar) — botão flutuante aparecendo (canto inferior direito, verde), `href` confirmado formado corretamente (`https://wa.me/5511900000000?text=Ol%C3%A1!%20Vim%20da%20loja...`, mensagem url-encoded certa); deixado configurado (não revertido pra `null`) pra o usuário poder ver/clicar o botão funcionando na HML. Estado final deixado no `capua`: banner com a foto real, cor principal teal (default), 4 selos originais, WhatsApp com número de teste. Zero erro de console em todos os testes. **Etapa 3 está 100% fechada** — falta só a Etapa 4 (tela de edição no painel).

28. **📌 14/08/2026 — Vitrine Fase 1, Etapa 4 (última do plano): tela de Configurações da Vitrine no painel + fluxo rascunho → visualizar → publicar + preview seguro + Open Graph dinâmico.** Move a edição de banner/selos/WhatsApp/identidade/logo do SQL manual (Etapa 3) pra uma tela dedicada, com um fluxo profissional em vez de gravar direto no publicado.

    **Modelo de dado (migration `031`, criada e pushada, aguardando revisão/aplicação manual)**: `store_settings` ganha `logo_path` (novo campo publicado — null usa o arquivo estático atual, `public/brand/logocp-icone.png`) e `rascunho` (jsonb nullable — snapshot completo dos campos editáveis em edição, nunca lido por `get_public_store_settings`). Os campos publicados de sempre (`banner_*`/`selos`/`whatsapp_*`/`cor_principal`, migration `030`) continuam sendo o que a vitrine pública lê — o contrato de `get_public_store_settings` pros campos já existentes não muda, só ganha `logo_path` a mais (extensão, mesmo espírito das migrations `029`/`030` — precisou `drop`+`create` de novo, `RETURNS TABLE` mudou).

    **RPCs de staff, isoladas por `current_tenant_id()`, nunca grantadas a `anon`**: `get_configuracao_vitrine()` (publicado + rascunho), `salvar_rascunho_vitrine(p_rascunho)` (grava o snapshot em edição, sem validar formato — rascunho pode estar incompleto enquanto o lojista digita), `publicar_vitrine()` (copia rascunho pros campos publicados e limpa o rascunho, atômico — se alguma CHECK constraint dos campos publicados rejeitar o dado, desfaz tudo e o rascunho continua intacto; falha se não houver rascunho pendente).

    **Preview seguro (Parte 2, o ponto de segurança desta etapa)**: o host da vitrine (`ecommercecauahml.vluma.com.br`) é diferente do host onde o staff loga no painel — o cookie de sessão do Supabase é próprio do host que o emitiu (sem `domain` compartilhado, ver `proxy.ts`), então **não atravessa pro domínio da loja**. Resolvido com um token de posse de curta duração (tabela `vitrine_preview_tokens`, 2h, sem policy de RLS permissiva — mesmo padrão de `tenant_domains`): `gerar_token_preview_vitrine()` (staff-only) gera o token + devolve o domínio público do tenant (`tenant_domains`) pra montar o link `https://{domínio}/preview?token={token}`; `get_preview_vitrine(slug, token)` (chamável por `anon` — a página `/preview` roda sem sessão de staff) só devolve o rascunho se o token existir, não tiver expirado, **e pertencer ao mesmo tenant do slug da URL** — qualquer outra combinação vira `null`, sem distinguir o motivo, tratado como 404 pela página. `src/app/preview/page.tsx` é a **única** rota do projeto que chama essa RPC, de propósito fora do layout de `(loja)` (que só sabe ler publicado) — superfície de auditoria mínima: só um arquivo pra conferir que rascunho nunca vaza pra anônimo.

    **Tela `/painel/vitrine`**: formulário completo (banner com toggle cor/imagem + upload, os 4 selos com seletor de ícone pré-definido, WhatsApp, identidade com color picker + upload de logo), estilo visual do painel (não o tema da loja). Três ações — "Salvar rascunho", "Visualizar" (salva o rascunho, gera o link de preview, abre em nova aba) e "Publicar" (salva o rascunho, pede confirmação explícita — "isto vai ao ar para os clientes" — só então publica). Upload de imagem (`_store-settings/vitrine/upload`) sobe pro bucket `product-images` já usado pelas fotos de produto, path `{tenant_id}/_store-settings/{tipo}-{uuid}.{ext}` — **achado de revisão**: a policy de insert desse bucket (migration `023`) exige `tenant_id` como **primeiro** segmento do path; a primeira versão do upload tinha `_store-settings` primeiro e teria sido rejeitada pela RLS pra qualquer staff de verdade (só não deu erro nos testes anteriores porque aquele upload usou service role, que ignora RLS) — corrigido antes de qualquer teste real. Path sempre único (uuid novo a cada upload, nunca sobrescreve) — é isso que garante que uma imagem nova enviada durante a edição não "vaza" pro publicado antes de Publicar: o arquivo já existe no Storage, mas nenhum campo publicado aponta pra ele até `publicar_vitrine()` copiar o path do rascunho.

    **Open Graph dinâmico (Parte 4)**: `generateMetadata` em `(loja)/layout.tsx` — título = nome da loja, descrição = subtítulo do banner, imagem = banner (se `tipo_fundo='imagem'`) ou logo, sempre do **publicado** (nunca rascunho — preview de link é público por natureza, WhatsApp/redes buscam sem sessão nenhuma). `produto/[slug]` ganha o próprio (produto sobrepõe completamente, não mescla campo a campo) — imagem do produto, cai pra logo da loja se o produto não tiver foto. `metadataBase` resolvido pelo host da própria requisição (cada tenant tem seu domínio) — é o que permite usar o caminho relativo da logo estática como `og:image` sem hardcodar host nenhum.

    **Bug real encontrado e corrigido no mesmo dia, achado testando o OG na HML**: o layout raiz (`src/app/layout.tsx`) tem um `title.template` fixo (`"%s | Criatório Capuã"`, pensado pra uma landing genérica antiga) que se aplicava por cima do título dinâmico da vitrine — invisível pro tenant atual (o nome bate com o template por coincidência, "Criatório Capuã | Criatório Capuã" ficava esquisito mas não óbvio o suficiente pra notar sem testar), mas **errado pra qualquer tenant futuro da Fase SaaS** com nome diferente (sufixo de marca fixa de outro tenant). Corrigido usando `title.absolute` (ignora o template do layout raiz) tanto no fallback de `(loja)/layout.tsx` quanto em `produto/[slug]` (que passou a montar o sufixo a partir de `settings.nome` de verdade, não mais hardcoded).

    **Frontend deployado ANTES da migration, de propósito (evita o incidente de contrato quebrado da migration `027`)**: rotas de `/api/painel/vitrine/*` devolvem erro amigável (RPC ainda não existe) em vez de quebrar a tela; `logo_path` novo tem fallback pro arquivo estático atual — vitrine pública inalterada. Confirmado com Chromium real contra a HML **antes** da migration: home renderizando normal (título/OG corretos, `og:image` pegando o banner já configurado na Etapa 3), `/preview` sem token e com token inválido devolvendo 404 nos dois casos (RPC ainda não existe = erro = tratado como inválido, mesmo resultado do caso "depois da migration, token errado"), as 3 rotas novas do painel (`GET /api/painel/vitrine`, `POST .../publicar`, `POST .../upload`) devolvendo 403 sem sessão.

    **Pendência real pra próxima sessão**: aguardando o usuário revisar e aplicar a migration `031`; depois disso, testar o fluxo completo (editar rascunho, "Visualizar" confirmando que só quem tem o link vê o rascunho, "Publicar", e reconferir o OG com uma ferramenta de preview de link real). Com isso, as 4 etapas do plano da Fase 1 da Vitrine estarão completas.

    **⚠️→✅ Revisado no mesmo dia (14/08/2026), ANTES de qualquer aplicação de migration** — ver item 29 logo abaixo. O usuário apontou que o projeto vai a produção comercial e um link de preview que funciona **deslogado** (token de posse, copiável/vazável) não é adequado nesse contexto — o design acima (`vitrine_preview_tokens`, `gerar_token_preview_vitrine`, `get_preview_vitrine` anon-callable, rota `/preview`) foi **descartado por completo** antes de qualquer aplicação real, substituído por preview protegido por sessão de verdade. Registro mantido aqui (não apagado) só pelo histórico da decisão original e do porquê ela mudou — mesma convenção já usada nas decisões #23/#24.

29. **📌 14/08/2026 — Revisão da Etapa 4: preview passa a ser protegido por SESSÃO de staff, não token.** Substitui por completo o mecanismo de preview do item 28 (migration `031` **editada in-place**, ainda não aplicada — não foi criada uma `032`, já que a `031` nunca chegou a ir pro banco).

    **O que muda**: migration `031` perde a tabela `vitrine_preview_tokens` e as RPCs `gerar_token_preview_vitrine`/`get_preview_vitrine` — tudo o resto (`logo_path`, `rascunho`, `get_configuracao_vitrine`, `salvar_rascunho_vitrine`, `publicar_vitrine`, extensão de `get_public_store_settings`) **inalterado**. Nova rota `src/app/vitrine-preview/page.tsx` — de propósito **fora** de `src/app/painel/**` (não herda o layout com sidebar/container estreito do painel, precisa renderizar a vitrine de verdade full-bleed), mas protegida pela mesma sessão de staff: `getStaffProfile()` (mesmo padrão de `painel/layout.tsx`, redireciona pra `/entrar?proximo=/vitrine-preview` sem sessão) **e** listada em `ROTAS_PROTEGIDAS` do `proxy.ts` (defesa em profundidade — redireciona na borda, antes até de renderizar). Lê o rascunho chamando `get_configuracao_vitrine()` com o client **autenticado** do próprio request (`@/lib/supabase/server`), nunca o client público/anon usado pelo resto da vitrine. Tenant resolvido via `getTenantSlug()` (env, o mesmo padrão "single-tenant hardcoded" já usado no resto do painel — primeiro uso real desse helper) — não por host, já que o preview roda no host do painel, não no domínio da loja.

    **Componentes reutilizáveis (Parte 2 do pedido revisado)**: nenhuma refatoração foi necessária nos componentes da vitrine (`Header`/`NavCategorias`/`BannerHero`/`SelosConfianca`/`GridCategorias`/`ProductGrid`/`Footer`/`WhatsAppFloatButton`) — todos já recebiam dado 100% via props desde a implementação original (Etapas 2/3), sem ler host/RPC/contexto internamente (só as páginas/layout fazem fetch, nunca os componentes de apresentação). `/vitrine-preview` já nasceu reaproveitando os mesmos componentes da vitrine pública, só trocando a fonte do dado (`mesclarRascunhoNoPublicado()`, já existente, sobrepõe os campos do rascunho por cima do publicado) — o preview não pode divergir da loja real por **construção** (mesmo código), não por disciplina de manter dois lugares sincronizados.

    **`VitrineForm`**: botão "Visualizar" agora só salva o rascunho e abre `/vitrine-preview` numa nova aba (`window.open`) — sem gerar link nenhum. Removidos: `useGerarLinkPreview` (hook), `POST /api/painel/vitrine/preview-link` (Route Handler), `getPreviewVitrine`/tipos de token em `database.ts`.

    **Testado contra a HML em 14/08/2026 (pré-migration, mesmo padrão de tolerância das etapas anteriores)**: `/vitrine-preview` sem sessão → redireciona pra `/entrar?proximo=%2Fvitrine-preview` (confirma a barreira de sessão funcionando mesmo antes da migration, já que o redirect é do `proxy.ts`/`getStaffProfile()`, não depende de RPC nenhuma). Rota antiga `/preview` (token) confirmada removida — 404. Home segue renderizando normal, zero regressão. `npm run build` limpo.

    **Pendência real pra próxima sessão**: aguardando o usuário revisar e aplicar a migration `031` (versão revisada, sem tokens). Depois disso, testar com sessão de staff de verdade: editar rascunho na tela, "Visualizar" abrindo `/vitrine-preview` com o rascunho correto, confirmar que uma aba **deslogada** no mesmo navegador não consegue acessar `/vitrine-preview` (vai pro login), "Publicar" refletindo na vitrine pública, e reconferir o Open Graph. Com isso, as 4 etapas do plano da Fase 1 da Vitrine estarão completas.

    **Migration `031` (revisada) aplicada pelo usuário em 14/08/2026, re-teste completo contra a HML com sessão de staff real confirmado no mesmo dia** — **as 4 etapas do plano da Fase 1 da Vitrine estão completas**. Sessão de staff obtida via a API administrativa do Supabase (`auth/v1/admin/generate_link`, mecanismo padrão — não altera senha nem sessões existentes, equivalente a clicar um link de acesso por e-mail), usada com a conta real do lojista (`adm@criatoriocapua.com.br`, tenant `capua`) pra testar o fluxo de ponta a ponta como staff de verdade, não só via `curl`/service role.

    - **(a) `/vitrine-preview` deslogado**: `curl`/Chromium sem cookie nenhum → redireciona pra `/entrar?proximo=%2Fvitrine-preview`, página nunca chega a renderizar (confirmado: HTML da resposta não contém "MODO PRÉVIA" em lugar nenhum).
    - **(b) `/vitrine-preview` logado (staff real)**: editado o título do banner pra um valor de teste bem marcado ("TESTE E2E — banner de rascunho ..."), "Salvar rascunho", depois `/vitrine-preview` numa aba autenticada — mostra a faixa "MODO PRÉVIA — mostrando o rascunho, não publicado" e o banner com o título de teste, usando os **mesmos componentes** da vitrine real (mesmo header, nav, selos, WhatsApp — só a faixa amarela no topo diferencia visualmente de a loja de verdade).
    - **(c) Vitrine pública (anônimo) nunca vê o rascunho**: com o rascunho de teste já salvo (não publicado), a home pública (`/`, sem sessão) **não continha** o título de teste em lugar nenhum do HTML — confirma que `get_public_store_settings` nunca expõe rascunho, exatamente como desenhado.
    - **(d) Publicar reflete na pública**: clicado "Publicar" (com o dialog de confirmação "isto vai ao ar para os clientes"), reconferida a home pública **sem sessão nenhuma** — agora mostrando o título de teste, banner com a foto do aquário + overlay preservados. Fluxo revertido logo em seguida (editar de novo → salvar rascunho → publicar) pro título original ("Peixes ornamentais direto do criatório"), confirmado restaurado na pública — nenhum dado de teste ficou visível pro público além da janela do teste em si.
    - **(e) Open Graph**: `og:title` = "Criatório Capuã" (nome da loja, não o título do banner — confere com o design: título = nome, não o banner), `og:description` = subtítulo do banner, `og:image` = a foto do banner já configurada na Etapa 3 (URL absoluta do Storage). Título da aba confirmado sem duplicação (`title.absolute` funcionando).

    Zero erro de console em todos os testes. Sessão de teste (magic link) descartada ao final, nenhum arquivo com token/sessão deixado no disco. **Vitrine Fase 1 (as 4 etapas do plano) está 100% completa e testada em produção/HML.**

    **Próximo passo do roadmap da Vitrine**: Fase 2 (Carrinho e Checkout — ver §4 "Carrinho e checkout"). Pré-requisito já identificado desde a Fase 0 (item 20 acima, 10/08/2026) e ainda não resolvido: a RPC pública `get_public_delivery_cities(p_tenant_slug)` não existe — leitura anônima de `delivery_cities` está completamente fechada desde a migration `028` (nem tabela nem RPC), decisão deliberada na época pra só desenhar isso junto do Carrinho (cálculo de frete/área de entrega). Sem essa RPC, o visitante anônimo não consegue ver as cidades de entrega disponíveis — bloqueia o início da Fase 2 até ser criada (mesmo padrão `SECURITY DEFINER` das outras RPCs públicas da Vitrine).

30. **📌 15/08/2026 — Decisões de produto registradas com o PO (documentação apenas, nada implementado nesta entrada).**

    **1. Duas formas de fechamento da loja, encaixadas no Carrinho (Fase 2)**: (a) Fechamento total — `loja_aberta=false` mostra só a mensagem informativa, sem catálogo nenhum (já implementado, Nível 1, decisão #5 em §5). (b) Loja aberta mas pedidos fechados — `pedidos_abertos=false`: o cliente navega e vê o catálogo inteiro normalmente, mas ao tentar **adicionar ao carrinho** é bloqueado com um aviso personalizado editável (`mensagem_pedidos_fechados`) — o bloqueio acontece **dentro** do fluxo do Carrinho (Fase 2), não é um módulo à parte nem uma tela nova. Contexto de negócio: o Cauã vende/entrega num período determinado do mês (ciclo), não continuamente — essas duas formas de fechamento cobrem esse padrão sem precisar de lógica de datas/calendário nenhuma. Hoje (Vitrine Fase 1) o Nível 2 só mostra o aviso acima do catálogo, porque ainda não existe botão de carrinho pra bloquear — ver `REGRAS_DE_NEGOCIO.md` §2 pro detalhe atualizado.

    **2. Navegação do painel — agrupamento "Configurações" (decidido, execução adiada de propósito)**: os itens de ajuste/configuração (Configurações da Vitrine, Cidades de entrega, Unidades de venda, Dados da loja, Gestão de equipe) vão ficar reunidos num agrupamento "Configurações" na sidebar, deixando o menu principal só com o operacional (Painel, Produtos, Pedidos, Clientes, Estoque) — padrão comum de SaaS, evita menu poluído conforme o painel cresce. **Execução planejada pra fase de polimento do painel, pós end-to-end — não agora.** Daqui pra frente, toda nova tela de configuração já nasce pensando nesse agrupamento futuro (nomenclatura/local consistente), mesmo sem reorganizar a sidebar hoje.

    **3. Módulo de Clientes (pendente, sequenciado)**: o lojista vai poder administrar os próprios clientes (ver quem comprou, histórico de pedidos). Entra junto com ou logo depois do módulo de Pedidos (Fase 3) — faz sentido nesse momento porque o cliente aparece no **contexto** de um pedido, não como uma tela isolada sem dado nenhum pra mostrar antes disso.

    **4. Relatórios financeiro/vendas (pendente, sequenciado)**: faturamento, produtos mais vendidos, etc. Frente futura, depois do end-to-end (só existe o que relatar depois que houver vendas de verdade rodando).

31. **📌 15/08/2026 — Decisões de produto da Fase 2 (Carrinho/Checkout), registradas com o PO ANTES de qualquer implementação (documentação apenas, nada implementado nesta entrada).** Detalhe completo das regras de negócio em `REGRAS_DE_NEGOCIO.md` §§11–14 (novas) e §5 (Entrega, complementada).

    **1. Carrinho client-side, arquitetado pra evoluir pra persistido**: o carrinho nasce no navegador (não é uma tabela no banco no MVP), pensado desde já pra migrar pro servidor (associado à conta) quando o cliente se identificar (login) no meio da navegação. Todas as regras que o carrinho aplica são sempre lidas de `store_settings` (configuração do tenant), nunca fixas no código.

    **2. Quantidade mínima por variação no carrinho**: respeita `quantidade_minima_venda` (migration `027`, já existe) ao adicionar um item.

    **3. Controle de estoque configurável, dois modos, faseado**: (a) "Bloquear pelo estoque" — revela disponibilidade real, reserva em tempo real, pensado pro cenário de pagamento no ato; **implementado só junto da integração de pagamento** (fase futura). (b) "Não bloquear" — estoque oculto, pedido vira solicitação sujeita a confirmação do lojista; **implementado no MVP**. A configuração dos dois modos é modelada agora, mesmo que só um tenha comportamento implementado de início.

    **4. Valor mínimo de pedido com flag de habilitação separada do valor**: nunca "R$0,00 = desativado" — uma flag explícita liga/desliga a regra, o valor só é considerado quando a flag está ligada.

    **5. Módulo de Configuração do Carrinho**: agrupa flag+valor mínimo (item 4) e modo de estoque (item 3) em `store_settings`. Campos modelados agora; tela de edição fica pra fase de polimento do painel, dentro do agrupamento "Configurações" (decisão #27, item 30 acima) — mesmo padrão "campo primeiro, tela depois" já usado nas Configurações da Vitrine.

    **6. Bloqueio de `pedidos_abertos=false` acontece ao adicionar ao carrinho**: reforça a decisão #26 (item 30) — vitrine 100% navegável com pedidos fechados, só a ação de adicionar ao carrinho é bloqueada, com `mensagem_pedidos_fechados` editável.

    **7. Entrega extensível, MVP só ponto de encontro por cidade**: campo "tipo de entrega" modelado pra comportar mais opções no futuro, mas o MVP implementa só o que já existe (`delivery_cities`) — via uma RPC pública **ainda a criar** (`get_public_delivery_cities`) + seletor de cidade no checkout. Frete calculado, retirada, Correios/transportadora ficam pra fases futuras. **Reforça a pendência já identificada na Fase 0 da Vitrine (item 20 acima, 10/08/2026)**: essa RPC pública nunca foi criada (a migration `028` fechou a leitura anônima de `delivery_cities` sem criar o equivalente público) — **fica confirmado que ela será criada nesta Fase 2**, não mais "quando o Carrinho for desenhado" (o Carrinho está sendo desenhado agora).

    **8. Taxa de entrega por cidade, com flag "cobrar taxa" (sim/não)**: mesmo princípio do item 4 — se a flag estiver ligada, habilita o valor da taxa, que soma ao total do pedido.

    **9. Contas de cliente nascem no MVP, reaproveitando a base de auth existente**: Supabase Auth + `handle_new_user` (já distingue destino por metadata do signup) — staff e cliente continuam distinguidos **por papel**, nunca a mesma conta. **Ponto de segurança crítico**: cliente nunca acessa o painel e vice-versa, com o mesmo rigor já aplicado ao isolamento multi-tenant (decisão #11/§10 de `REGRAS_DE_NEGOCIO.md`). Base de clientes robusta: todo cliente que gera pedido — cadastrado ou convidado — vira registro vinculado ao contato, pedidos vinculados a esse registro; fluxo cadastrado é o principal, convidado é um complemento secundário.

32. **📌 15/08/2026 — Complemento às decisões de produto da Fase 2 (item 31), registrado com o PO ANTES de qualquer implementação.** Detalhe completo em `REGRAS_DE_NEGOCIO.md` §§15–16 (novas) e pendências acrescentadas em §8.

    **10. Checkout em passo a passo, não página única**: fluxo em etapas sequenciais — Identificação → Entrega → Revisão/Confirmação. Pagamento entra como etapa a mais quando o Fluxo B (item 12 abaixo) existir. Decisão de UX pra reduzir sobrecarga numa tela só e facilitar evoluir o fluxo depois.

    **11. Identificação do cliente por e-mail**: login = e-mail, reaproveitando o Supabase Auth nativo via `handle_new_user` (mesmo mecanismo do staff), sem custo de verificação por SMS. Cadastro coleta nome + e-mail (login) + telefone/WhatsApp (**obrigatório** — canal de contato/entrega) + senha. Login por telefone fica como evolução futura, fora do MVP.

    **12. Dois fluxos de checkout conforme o pagamento**: **Fluxo A** (não integrado, pagamento na entrega) = **MVP** — finalizar cria pedido "aguardando validação", vendedor valida manualmente, notificação por e-mail+WhatsApp, pagamento na entrega; casa com o modo de estoque "não bloquear" (§12 de `REGRAS_DE_NEGOCIO.md`) e "pagamento fora do sistema" (já pendente em §8). **Fluxo B** (integrado via Asaas) = **fase futura** — pagamento na hora, confirmação por webhook, status atualiza sozinho; casa com o modo de estoque "bloquear pelo estoque". Entra junto da integração de pagamento, não no MVP.

    **13. Reserva de estoque na finalização do pedido**: ao finalizar, o estoque é reservado/consumido na hora (não fica solto) — o próximo cliente já vê esgotado, evitando frustração com item que "acabou depois". Padrão de mercado (reserva na submissão, não na validação). Usa `store_settings.baixa_estoque_na_reserva` (já existe) + baixa atômica via RPC com `FOR UPDATE` (já prevista pra Pedidos).

    **14. Reserva modelada como registro com estado e expiração, preparada pra evoluir**: nasce como um REGISTRO próprio (pedido X reservou N da variação Y), não um decremento simples de saldo. MVP só tem a reserva "firme" (na finalização, sem expiração); a estrutura já comporta, sem reescrita, a reserva "leve" durante o checkout (com expiração automática) da fase de pagamento/Fluxo B — mesmo princípio "preparar sem pagar o custo agora" do carrinho client-side (item 1).

    **15. Pendências reais, a decidir nas próximas conversas com o PO antes de implementar**: (a) política de cancelamento configurável (manual + automático por tempo, aviso ao cliente, liberação do estoque reservado); (b) Fluxo A em detalhe (o que "validar" significa, o que o vendedor pode alterar antes); (c) notificações (mecânica de e-mail+WhatsApp, WhatsApp depende de integração ainda não escolhida, ex. Evolution API); (d) Fluxo B (Asaas) completo, ainda não esboçado. Ver `REGRAS_DE_NEGOCIO.md` §8 pro registro completo.

33. **📌 18/08/2026 — Complemento às decisões de produto da Fase 2 (itens 31/32): 3 das 4 pendências do item 32/15 fechadas com o PO. Documentação apenas, nada implementado nesta entrada.**

    **1. Reserva de estoque só na finalização do pedido — carrinho nunca reserva.** Confirma e reforça a decisão já registrada em §16.1: o carrinho client-side (§11.1) não reserva estoque nenhum — a reserva só nasce quando o pedido é criado, na finalização. Consequência: um carrinho abandonado simplesmente some do navegador sem precisar liberar nada, porque nunca reservou nada. A reserva "leve" desde o próprio carrinho, com expiração automática (modelo mais sofisticado, estrutura já prevista em §16.2), fica para quando o Fluxo B/Asaas entrar — não é MVP. Ver `REGRAS_DE_NEGOCIO.md` §16.3 (novo).

    **2. Vendedor pode editar o pedido ao validar — só pra reduzir (MVP).** Detalha o que "validar" significa no Fluxo A (§15.3): o vendedor pode reduzir quantidade ou remover item (ex.: item que acabou entre o pedido e a conferência), sem precisar cancelar o pedido inteiro. **Nunca pode aumentar quantidade nem adicionar item** — isso elevaria o compromisso do cliente sem uma nova confirmação dele, fica pra fase futura. O ajuste é sempre a favor do cliente (o total só pode diminuir nessa etapa) e o estoque das reduções/remoções volta ao saldo disponível. Ver `REGRAS_DE_NEGOCIO.md` §15.4 (novo).

    **3. Política de cancelamento (MVP): manual + automático configurável.** (a) Manual — o vendedor recusa/cancela o pedido, estoque reservado é liberado. (b) Automático por tempo — pedido não validado dentro de um prazo é cancelado sozinho e libera o estoque (evita "pedido zumbi" segurando saldo); o prazo é **configurável pelo lojista** e o mecanismo automático é **desligável** (padrão de fábrica ligado, prazo conservador) — mesmo princípio de nunca esconder uma regra atrás de comportamento implícito já usado noutras flags do sistema. Ver `REGRAS_DE_NEGOCIO.md` §17 (novo).

    **4. Notificação ao cliente + Área do Cliente nasce no MVP.** Toda mudança de estado do pedido por ação do vendedor/sistema (validar, ajustar, cancelar) notifica o cliente e direciona pra uma área própria dele (**"Meus Pedidos"**, cliente logado — §14) — essa área é a **fonte de verdade** do acompanhamento, sempre mostrando o estado final/atualizado (itens, ajustes, total, status), não uma cópia estática do momento da finalização. **PDF do pedido não é o mecanismo principal** — fica como opção secundária/futura (ficaria desatualizado assim que o vendedor ajustasse o pedido). Ver `REGRAS_DE_NEGOCIO.md` §18.1–18.2 (novo).

    **5. WhatsApp em dois níveis, faseados.** Nível 1 — **notificações de saída** (sistema avisa o cliente: confirmação, validação, ajuste, cancelamento), via **Evolution API**, entra no **MVP** junto com e-mail. Nível 2 — **consultas de entrada** (cliente pergunta pelo WhatsApp, sistema responde — atendimento conversacional), via **N8N**, fica pra **fase seguinte**, não MVP. A arquitetura de notificação já nasce pensada pra múltiplos canais (e-mail + WhatsApp de saída juntos desde o início), com o conversacional como incremento posterior, não reescrita. Ver `REGRAS_DE_NEGOCIO.md` §18.3 (novo).

    **Pendência real que permanece**: só o **Fluxo B (Asaas) completo** (item (d) da lista original) segue em aberto — integração de pagamento, webhook, atualização automática de status, ainda não esboçado em detalhe, entra junto da fase de pagamento. Ver `REGRAS_DE_NEGOCIO.md` §8 (atualizado).

34. **📌 18/08/2026 (continuação, mesmo dia) — Bloco final de decisões de produto da Fase 2: detalha o Fluxo A e fecha o planejamento do MVP do Carrinho/Checkout. Documentação apenas, nada implementado nesta entrada.**

    **1. Status do pedido — quatro genéricos, servem qualquer modalidade de entrega**: `Aguardando validação` → `Confirmado` → `Concluído`, mais `Cancelado` (terminal, a partir de qualquer um dos três). Modalidade de entrega e datas são **campos** do pedido, não viram status — evita multiplicar status por modalidade quando Correios/outras entrarem no futuro. Ver `REGRAS_DE_NEGOCIO.md` §19.1 (novo).

    **2. Datas de entrega como campos, não lógica**: `data_prevista` (orienta cliente e vendedor antes da entrega) e `data_efetiva` ("entregue em", preenchida ao concluir, registro histórico). MVP trata como campos simples — sem lembretes automáticos nem janelas calculadas, isso fica pra refinamento futuro. Ver `REGRAS_DE_NEGOCIO.md` §19.2 (novo).

    **3. Tela de validação do pedido (painel do vendedor)**: mostra dados do cliente (nome, telefone/WhatsApp, e-mail), entrega (modalidade + cidade/ponto de encontro escolhido), itens (produto, variação, quantidade, preço, estoque disponível ao lado de cada item) e total. Três ações: **Validar**, **Editar** (reduzir/remover — já decidido em §15.4) e **Cancelar** (com motivo). Editar mostra o novo total na hora e deixa explícito que o cliente será notificado da mudança. Ver `REGRAS_DE_NEGOCIO.md` §20 (novo).

    **4. Dois campos de observação**: `observacao_cliente` (recado do cliente no checkout) e `observacao_interna` (anotação do vendedor — o cliente **nunca** vê). Ver `REGRAS_DE_NEGOCIO.md` §19.3 (novo).

    **5. Histórico do cliente na tela de validação — adiado pro módulo de Clientes (Fase 3), não é Relatórios**: quantos pedidos o cliente já fez, etc., é **dado individual do cliente** (natureza do módulo de Clientes — já pendente e sequenciado, ver item 30/ponto 3 acima), não da camada agregada/analítica de Relatórios (outra fase, item 30/ponto 4). A tela de validação (ponto 3) nasce sem esse histórico — entra quando o módulo de Clientes existir.

    **6. Área do cliente (vitrine) ganha edição de conta, além de "Meus Pedidos"**: editar dados cadastrais (nome, telefone, e-mail, cidade de entrega padrão) e trocar senha — os dois no MVP. Ver `REGRAS_DE_NEGOCIO.md` §18.4 (novo, complementa §18.2).

    **7. Staff também tem autogestão de conta no painel, no MVP**: editar os próprios dados cadastrais e trocar senha — mesmo princípio do ponto 6, do lado do staff. Respeita o isolamento de papéis já crítico (cliente nunca acessa o painel e vice-versa, §1/§14 de `REGRAS_DE_NEGOCIO.md`). Ver `REGRAS_DE_NEGOCIO.md` §21 (novo).

    **8. Roteiro de implementação da Fase 2 — 8 incrementos, cada um testado na URL pública antes do próximo**:
    1. RPC pública `get_public_delivery_cities` (pendência já identificada desde a Fase 0 da Vitrine — item 20). **✅ Concluído.**
    2. Contas de cliente (§14) + autogestão de conta do staff (ponto 7 acima). **✅ Concluído.**
    3. Modelo de pedidos: tabelas de pedido/itens, os 4 status (ponto 1), campos de entrega/datas/observações (pontos 2/4), reserva de estoque atômica via RPC `SECURITY DEFINER` com `FOR UPDATE` (§16). **✅ Concluído** (reserva/baixa de estoque adiada pra validação do vendedor, ponto 7 — ver §16.4).
    4. Carrinho client-side + regras (mínimo de venda §11.2, bloqueio de `pedidos_abertos` §11.3/§2, valor mínimo §11.4). **✅ Concluído.**
    5. Checkout passo a passo (identificação → entrega → revisão → finaliza, cria o pedido e reserva o estoque — §15.1/§16). **✅ Concluído em 21/08/2026** — ver item 43 abaixo e `REGRAS_DE_NEGOCIO.md` §15 pro detalhe completo.
    6. Área do cliente (Meus Pedidos + gestão de conta — §18.2/§18.4). **✅ Concluído em 21/08/2026** — ver item 44 abaixo.
    7. Painel de pedidos do vendedor (listar/validar/editar/cancelar — ponto 3 acima) + política de cancelamento (§17). **Próximo passo, não iniciado.**
    8. Notificações (e-mail + WhatsApp via Evolution API — §18.3, Nível 1). **Não iniciado.**

    **Com este bloco, o planejamento de produto do MVP da Fase 2 (Carrinho/Checkout) está fechado.** Segue em aberto só o Fluxo B (Asaas, fase futura, já registrado no item 33) — o que resta agora é seguir o roteiro acima, incremento por incremento, testando cada um na URL pública antes do próximo (mesma regra de processo já em vigor, §0).

35. **📌 18/08/2026 (continuação, mesmo dia) — Fase 2, incremento 1 do roteiro (item 34/ponto 8): RPC pública `get_public_delivery_cities` criada, aguardando revisão e aplicação manual. Nada aplicado, nada de frontend ainda.**

    Migration `032_public_delivery_cities.sql` (criada e pushada, **não aplicada**) fecha a pendência identificada desde a Fase 0 da Vitrine (item 20, 10/08/2026): a `028` fechou a leitura anônima tenant-blind de `delivery_cities` sem criar RPC pública equivalente, deliberadamente adiada pro desenho do Carrinho/Checkout.

    **Diagnóstico prévio** (regra de processo, §0 "diagnosticar antes de tocar em dados existentes" — aqui aplicada a schema, não dado): schema de `delivery_cities` conferido direto na migration `005` (nunca alterado desde então — só o `tenant_id` ganhando `DEFAULT` na `008`, sem nenhuma coluna nova) e cruzado com `src/types/database.ts` — colunas confirmadas: `id, tenant_id, nome, uf, ponto_entrega, horario, observacoes, ordem, ativo, created_at, updated_at`. Sem coluna de taxa de entrega ainda (decisão futura já registrada em `REGRAS_DE_NEGOCIO.md` §5 — "cobrar taxa" — mas ainda não modelada no banco; fora do escopo desta RPC). Ordenação do painel (`/api/painel/cidades/route.ts`) conferida como `.order('ordem').order('nome')` — reaproveitada igual na RPC.

    **RPC segue exatamente o padrão das 4 RPCs públicas da `028`**: `SECURITY DEFINER`, `stable`, `search_path` fixo, filtro de tenant resolvido internamente a partir do `p_tenant_slug` (nunca aceita `tenant_id` vindo de fora), slug inválido/tenant inativo devolvem zero linhas (não erro) — mesmo mecanismo (`join` contra `tenants` com `ativo=true`). Filtra também `delivery_cities.ativo=true` (só cidades ativas). Expõe `id, nome, uf, ponto_entrega, horario, observacoes, ordem` — `id` necessário pro checkout referenciar a cidade escolhida (mesmo padrão já usado por `customers.delivery_city_id`, migration `005`). De propósito **não** expõe: `tenant_id` (interno, sem uso pro cliente final), `ativo` (a RPC já filtra só as ativas — devolver um campo sempre `true` não agrega nada), `created_at`/`updated_at` (metadado interno). Nenhum dado sensível — é a mesma informação (nome da cidade, ponto de encontro, horário, observações) já passada manualmente por WhatsApp hoje.

    **Puramente aditiva** — cria função nova, não toca em tabela/policy/função existente nenhuma. Confirmado por leitura do SQL: nenhum `DROP`/`ALTER` em nada pré-existente. Nada quebra no que já está em produção/HML.

    **Pendência real**: aguardando revisão e aplicação manual da migration `032` pelo usuário. Depois de aplicada, o próximo passo do roteiro (item 34/ponto 8) é o incremento 2 — Contas de cliente + autogestão de conta do staff. Consumo desta RPC pelo frontend (seletor de cidade no checkout) só entra no incremento 5 (Checkout) — este incremento foi só a RPC no banco, como pedido.

    **Migration `032` aprovada e aplicada pelo usuário em 18/08/2026, testada no mesmo dia com a `anon key` (simulando a vitrine de verdade, não service role) via script Node descartável (`@supabase/supabase-js`, removido do repositório depois do teste).** Três checagens:

    - **Slug válido (`capua`)**: retornou as 2 cidades ativas do tenant (`Salvador`/BA e `Feira de Santana`/BA, cada uma com `ponto_entrega`/`horario`/`observacoes`/`ordem`), com as chaves da resposta exatamente `id, nome, uf, ponto_entrega, horario, observacoes, ordem` — nenhuma chave a mais.
    - **Filtro de `ativo=true` confirmado com dado real, não só pela leitura do SQL**: conferido direto no banco (service role, só para o diagnóstico) que o tenant `capua` tem uma 3ª cidade (`Vitória da Conquista`) com `ativo=false` — a RPC (`anon key`) devolveu só as 2 ativas, confirmando que o filtro exclui de verdade, não é coincidência de não haver cidade inativa cadastrada.
    - **Slug inválido/inexistente**: `0` linhas retornadas, sem erro — comportamento esperado (mesmo padrão das outras 4 RPCs da `028`).
    - **Nenhum campo interno vazado**: resposta conferida sem `tenant_id`, `ativo`, `created_at` nem `updated_at` em nenhuma das duas chamadas.

    **Migrations `001`–`032` estão todas aplicadas e validadas no banco — nenhuma pendente.** Consumo pelo frontend segue não iniciado (fica pro incremento 5 do roteiro, Checkout) — este incremento fechou só a RPC.

36. **📌 18/08/2026 (continuação) — Fase 2, incremento 2 (Contas de cliente): gap de segurança CRÍTICO encontrado no diagnóstico prévio (regra de processo, §0) e fechado antes de qualquer outra coisa. Migration `033` criada, aguardando revisão e aplicação manual. Nada aplicado ainda.**

    **Diagnóstico feito antes de desenhar contas de cliente** (leitura de `handle_new_user`/migration `006`, schema de `customers`, fluxo de signup/login/recuperação de senha já existente em `/cadastro`/`/entrar`/`/recuperar-senha`/`/nova-senha`, mecanismo de isolamento de papéis em `getStaffProfile()`): revelou que `handle_new_user` decidia staff-vs-cliente lendo `role` de `raw_user_meta_data` (`user_metadata`) — campo que **qualquer chamada pública a `supabase.auth.signUp()` (com a `anon key`, pública por natureza) pode preencher livremente**. Nada no servidor impedia alguém de ignorar o formulário `/cadastro` (que hardcoda `role: 'cliente'`) e chamar `signUp()` direto com `data: { role: 'admin' }`, virando staff com acesso total a `/painel`. Contradiz frontalmente o "cliente nunca acessa o painel, com o mesmo rigor do isolamento multi-tenant" (`REGRAS_DE_NEGOCIO.md` §14) — gap real, já em produção/HML, nunca antes testado.

    **Confirmado, antes de desenhar a correção, que fechar o gap não quebra o onboarding de staff atual**: consulta somente-leitura (`profiles` + `auth.users` via Admin API, service role) mostrou que os 2 staff reais hoje (`adm@criatoriocapua.com.br` e o canário `teste-isolamento@vluma.local`) foram os DOIS provisionados por canal já privilegiado — `email_confirmed_at` idêntico a `created_at` no primeiro (assinatura de `auth.admin.createUser()` com `email_confirm:true`, nunca de signup público) e ausência de `role` no metadata do segundo (staff inserido direto via SQL, bypassando a trigger). Nenhum passou pelo caminho vulnerável.

    **Migration `033_handle_new_user_role_via_app_metadata.sql` (criada e pushada, não aplicada)**: `handle_new_user` passa a ler `role` de `raw_app_meta_data` (`app_metadata`) em vez de `raw_user_meta_data` — distinção que a própria Auth API do Supabase já garante: `app_metadata` nunca é gravável pelo usuário final (nem por `signUp()`, nem por `updateUser()`), só pela Admin API com service role. Um signup público é **estruturalmente incapaz** de influenciar esse campo, não importa o payload — o gap fecha por garantia da própria API, não por convenção de código. `nome`/`whatsapp`/`delivery_city_id` continuam vindo de `raw_user_meta_data` normalmente (dado de perfil legítimo, sem risco de autorização). Caminho privilegiado de criar staff continua funcionando — ajuste mínimo: quem usa a Admin API passa `role` em `app_metadata` em vez de `user_metadata`; INSERT direto em `profiles` via SQL não muda nada (nunca passou pela trigger). Decisão confirmada com o usuário: manter esse processo manual/formalizado por enquanto, sem criar rota `is_admin()` nova (volume de staff baixíssimo, comodidade futura, não necessidade agora).

    Puramente `create or replace function` — nenhuma tabela/coluna/policy tocada, staff já cadastrado não é afetado (só vale pra `INSERT`s futuros em `auth.users`).

    **Pendência real**: aguardando revisão e aplicação manual da migration `033`. Depois de aplicada e testada, seguem as demais decisões do incremento 2 (reaproveitar `/cadastro`/`/entrar`/`/recuperar-senha`/`/nova-senha` restilizando pro tema da vitrine; e-mail `not null` em `customers`; bloquear e-mail compartilhado entre staff e cliente; `permite_autocadastro` como sub-incremento de prioridade baixa).

    **Migration `033` aprovada e aplicada pelo usuário em 18/08/2026, blindagem testada ao vivo no mesmo dia — não só por leitura de código, por um ataque de verdade simulado contra a URL pública.** Script Node descartável (removido do repositório depois), dois signups reais via `anon key` (não service role):

    - **Teste A (ataque simulado)**: `supabase.auth.signUp()` com `options.data: { role: 'admin', nome: 'Teste Blindagem Role' }` — exatamente o payload que expunha o gap antes da `033`. Resultado conferido direto no banco: **nenhuma linha em `profiles`**, uma linha em `customers` com `nome='Teste Blindagem Role'`, `email` do teste, `whatsapp=''` (default). Prova viva de que `role:'admin'` no metadata público não vira mais staff — vira cliente comum, sem exceção.
    - **Teste B (cliente normal)**: signup com `nome`, `whatsapp`, `delivery_city_id` (cidade real do tenant `capua`) — confirmado que o fluxo legítimo continua 100% funcional: `customers` preenchido corretamente (`nome`, `whatsapp`, `email`, `delivery_city_id` todos batendo com o enviado), sem linha em `profiles`.
    - **Limpeza**: os 2 usuários de teste removidos via `auth.admin.deleteUser()` (cascade apaga `customers`/`profiles` junto, por FK `on delete cascade`) — reconfirmado `0` linhas restantes em `profiles`/`customers` pros e-mails de teste.

    **Migrations `001`–`033` estão todas aplicadas e validadas no banco — nenhuma pendente.** Gap crítico fechado e comprovado. Segue o desenho do resto do incremento 2.

37. **📌 18/08/2026 (continuação) — Fase 2, incremento 2: pacote completo (1+2+3+4) aprovado. 3 migrations criadas (`034`/`035`/`036`), aguardando revisão e aplicação manual. Nada aplicado, nada de código/telas ainda — só a parte de banco deste pacote.**

    **Migration `034` — `customers.email NOT NULL` + `DROP COLUMN cidade_entrega`.** Diagnóstico prévio (regra de processo), rodado na hora de escrever a migration: as 4 linhas reais de `customers` hoje têm `0` com `email` nulo — seguro aplicar `NOT NULL` sem backfill. `cidade_entrega` (coluna morta desde que `delivery_city_id` virou o campo de verdade na migration `005`, confirmado por grep em todo `src/` sem nenhum uso) removida.

    **Migration `035` — 2 triggers `BEFORE INSERT` (uma em `profiles`, uma em `customers`) impedindo a mesma conta (`auth.users.id`) acumular linha nas duas tabelas ao mesmo tempo.** Reforça o isolamento de papéis (`REGRAS_DE_NEGOCIO.md` §1/§14). O caso "duas contas diferentes com o mesmo e-mail" já é impossível hoje (unicidade nativa de `auth.users.email` no Supabase — é por isso que `/cadastro` já trata "e-mail já cadastrado"); o que faltava fechar era a mesma conta virar as duas coisas ao mesmo tempo — só alcançável via INSERT manual privilegiado (o caminho de criar staff, ver migration `033`), não é risco externo, é defesa contra erro operacional. Escopo limitado a `INSERT` (não `UPDATE` de `id`/`auth_user_id`, vetor extremamente incomum, fora do que este pacote decidiu cobrir — registrado como limitação conhecida no cabeçalho da migration). Não interfere no fluxo normal de `handle_new_user` (nunca insere nas duas tabelas pro mesmo usuário).

    **Achado do diagnóstico rodado antes de escrever `035`**: existe **hoje** uma sobreposição real, mas só no canário de teste — `teste-isolamento@vluma.local` (tenant sintético `_teste_isolamento`) está em `profiles` **e** `customers` ao mesmo tempo (mesmo `auth.users.id`, `8c0c4252-…`). Não afeta o Cauã (dado real do tenant `capua` não tem sobreposição nenhuma). Como a trigger é `BEFORE INSERT`, aplicar `035` **não falha nem mexe** nessa linha existente — só passa a impedir a mesma sobreposição em contas **novas** dali pra frente. Decisão em aberto, sem urgência: limpar essa sobreposição do canário (remover a linha de `customers` ou de `profiles` pra ele) fica a critério do usuário, não bloqueia a aplicação da migration.

    **Migration `036` — expõe `permite_autocadastro` em `get_public_store_settings`.** Campo existe desde a `002_core.sql`, sempre foi deliberadamente excluído da RPC pública (nenhuma tela lia). Agora o `/cadastro` precisa ler essa flag pra decidir se mostra o formulário ou uma mensagem de "cadastro fechado". `drop`+`create` de novo (mesmo motivo de sempre — `RETURNS TABLE` não aceita `create or replace` com a mesma assinatura de entrada), reproduzindo as 18 colunas já existentes (lidas do estado atual da função, migration `031` — não presumidas) mais a nova. Isolamento de tenant e `security definer` inalterados.

    **Pendência real**: aguardando revisão e aplicação manual das 3 migrations. Depois de aplicadas, entra a parte de código/telas do pacote (restyle das 4 telas pro tema da vitrine + correção do dropdown de cidade vazio usando a RPC `032` + gate de `permite_autocadastro` no `/cadastro`), testada na URL pública antes de fechar o incremento 2.

    **Migrations `034`/`035`/`036` aprovadas e aplicadas pelo usuário em 18/08/2026, reconfirmadas direto no banco no mesmo dia**: `034` — `select cidade_entrega from customers` devolve erro de coluna inexistente (removida de verdade); `035` — tentativa real de `INSERT` em `profiles` reaproveitando o `auth_user_id` de um `customer` já existente foi rejeitada com a mensagem exata do cabeçalho ("Este usuario ja possui cadastro de cliente..."), sem deixar resíduo; `036` — RPC pública (`anon key`) confirmada devolvendo `permite_autocadastro`. **Migrations `001`–`036` todas aplicadas e validadas — nenhuma pendente.**

    **Parte de código do incremento 2 — implementada e testada com Playwright real contra a URL pública (`ecommercecauahml.vluma.com.br`) em 18/08/2026, não `localhost`.** Três frentes:

    **(1) Restyle das 4 telas pro tema da vitrine.** Novo `src/app/(auth)/layout.tsx` (Server Component) — casca compartilhada por `/entrar`/`/cadastro`/`/recuperar-senha`/`/nova-senha`: resolve tenant via `getTenantFromHeaders()`, busca `get_public_store_settings`, aplica `.loja-theme` + `cor_principal` (mesmo mecanismo do layout de `(loja)`). **Diferença deliberada do layout de `(loja)`**: nunca faz `notFound()` quando o tenant não resolve — staff também usa `/entrar` pra logar, e login não pode quebrar só porque o host não está em `tenant_domains` (ex. domínio bruto do deploy); sem tenant, cai pro tema neutro padrão do app, mas continua 100% funcional. Novo `AuthHeader` (logo + nome, tenant-aware) substitui o `BrandHeader` antigo (hardcoded, URL de imagem fixa de um bucket específico do Cauã, cores navy). As 4 páginas perderam o wrapper próprio (`<main>` + gradiente + `BrandHeader`/`VlumaFooter` duplicados em cada uma) e as referências a `--brand-navy`/`--brand-aqua-light` (tokens do painel) viraram `text-primary`/`bg-secondary` (tokens semânticos, respondem ao `.loja-theme`).

    **(2) Bug real corrigido: dropdown de cidade do `/cadastro` retornava sempre vazio.** Causa raiz (achada no diagnóstico do início deste incremento): `useDeliveryCities()` consultava `delivery_cities` direto pela tabela com sessão anônima — acesso fechado pela migration `028` (dropou `cities_select_anon`) sem RPC pública equivalente na época. `/cadastro` virou Server Component que busca `getPublicDeliveryCities` (RPC `032`, Fase 2 incremento 1) no servidor e passa as cidades como prop pro formulário (`cadastro-form.tsx`, client component só com a interatividade). Hook antigo (`src/hooks/use-delivery-cities.ts`) removido — ficou morto, nenhum outro consumidor.

    **(3) Gate de `permite_autocadastro`.** `/cadastro` (Server Component) busca `get_public_store_settings` e, se `permite_autocadastro=false`, mostra "Cadastro fechado no momento" em vez do formulário. `getPublicStoreSettings()` (`src/lib/loja/rpc.ts`) ganhou `permite_autocadastro: true` no objeto de defaults mesclado por cima do retorno da RPC — mesmo padrão de tolerância já usado pros campos da Etapa 3 (se algum ambiente ainda não tiver a `036` aplicada, o campo vem `undefined` da RPC antiga e o default garante `true`, formulário aberto por padrão — fail-open, nunca trava autocadastro sozinho por um campo ausente).

    **`database.ts` corrigido**: `get_public_delivery_cities` adicionada (não existia — a `032` só tinha sido consumida agora); `get_public_store_settings` ganhou `logo_path`/`permite_autocadastro`, que já faltavam desde as migrations `031`/`036` respectivamente (drift de tipo nunca corrigido a mão nas sessões daquelas migrations — corrigido de passagem aqui). `npm run build` limpo antes do teste.

    **Testado com Chromium real (Playwright headless) contra a URL pública em 18/08/2026** (script Node descartável, removido do repositório depois — mesmo rigor de sempre: URL pública após push+deploy, nunca `npm run dev`): (a) dropdown de cidade do `/cadastro` populado com as 2 cidades ativas do tenant (`Salvador - BA`, `Feira de Santana - BA`) — bug confirmado corrigido; (b) cor computada do heading "Criar conta" = `rgb(8, 145, 178)` (`#0891b2`, o teal da vitrine, não mais o navy antigo) — tema confirmado aplicado; (c) cadastro de cliente de ponta a ponta — formulário preenchido e submetido, tela "Confirme seu e-mail" apareceu; (d) login de ponta a ponta — e-mail do cliente de teste confirmado via Admin API (simulando clique no link, sem inbox real), login pelo formulário real de `/entrar`, redirecionado pra `/` com cookie de sessão presente; (e) gate de `permite_autocadastro` nos dois sentidos — com a flag `false` (setada direto no banco pra teste, revertida depois), `/cadastro` mostrou "Cadastro fechado no momento" sem o formulário; com `true` (revertido), formulário normal de volta. Usuário de teste removido ao final (`auth.admin.deleteUser`, cascade limpa `customers` junto) — zero resíduo confirmado. Zero erros de console/página em toda a sessão de teste. Screenshots de `/entrar` e `/cadastro` conferidos visualmente — logo do Criatório Capuã, tema teal consistente com o resto da vitrine, sem quebra de layout.

    **Com isso, o incremento 2 da Fase 2 (Contas de cliente) está completo** — gap de segurança fechado e comprovado (migration `033`), pacote de banco aplicado e testado (`034`/`035`/`036`), telas restilizadas e bug de cadastro corrigido, gate de autocadastro funcionando. Próximo passo do roteiro (item 34/ponto 8): incremento 3 — modelo de pedidos.

38. **📌 18/08/2026 (continuação) — Regressão reportada pelo usuário na prática ("Entrar" não abria a tela de login): investigada a fundo, NÃO reproduzida em nenhum cenário testado. Regressivo completo do fluxo de auth rodado — 12/12 checks passando.**

    **Diagnóstico do bug do "Entrar"**: 5 reproduções tentadas, todas com sucesso (nenhuma reproduziu a falha) — clique a partir da home (desktop), clique a partir da home (mobile, emulação iPhone 13), clique a partir de `/produtos`, clique a partir de uma ficha de produto, e navegação direta. Em todos os casos: `href="/entrar"` correto, clique navega pra `/entrar`, formulário de login renderiza (`#email` presente), zero erro de console/página. Headers de resposta de `/entrar` conferidos — `cache-control: private, no-cache, no-store, max-age=0, must-revalidate`, `x-vercel-cache: MISS` — descarta cache de CDN servindo versão antiga. **Causa raiz mais provável (não 100% confirmável sem acesso à sessão do navegador do usuário): artefato de timing de deploy.** O incremento 2 fez 5 pushes em sequência rápida (`68c8db3`→`13ac98c`), cada um disparando um novo deploy na Vercel; se o usuário testou com uma aba já aberta antes/durante um desses deploys, a navegação client-side do Next (`<Link>`) pode ter tentado buscar um chunk/RSC payload de uma versão de build que deixou de existir assim que o deploy seguinte substituiu os assets — isso falha silenciosamente em vez de mostrar um erro claro. **Não é um bug de código no estado atualmente publicado** — testado exaustivamente sem reprodução.

    **Regressivo completo do fluxo de auth, rodado com Chromium real (Playwright) contra a URL pública em 18/08/2026, 12/12 checks passando**: (1) home carrega; (2) listagem (`/produtos`) carrega com produtos; (3) ficha de produto carrega; (4) clique em "Entrar" abre a tela de login; (5) login de staff (`adm@criatoriocapua.com.br`) redireciona pro `/painel`; (6) sidebar do painel presente (staff reconhecido, isolamento de papéis intacto — não tratado como cliente); (7) `/painel/produtos` acessível sem redirecionar pro login; (8) `/painel/cidades` acessível; (9) tela de cadastro abre; (10) dropdown de cidade populado (2 opções); (11) tela de recuperar senha abre; (12) tela de nova senha abre. Logout executado ao final do teste de staff, sem deixar sessão presa.

    **3 erros de console encontrados, investigados e descartados como não-regressão**: `404` em `/painel/configuracoes`, `/painel/pedidos`, `/painel/clientes` — confirmado que são **prefetch automático do Next** (`_rsc=...` na query) pra itens da sidebar que apontam pra módulos ainda não construídos (Pedidos/Clientes/Configurações, roadmap futuro) — mesmo padrão já documentado como benigno no item 24 (§0), não relacionado ao incremento 2, pré-existente.

    **Nenhum código foi alterado nesta entrada** — nenhum bug reproduzível foi encontrado pra corrigir. Recomendação registrada pro usuário: dar um hard refresh (`Ctrl+Shift+R`/`Cmd+Shift+R`) na aba onde o problema apareceu e testar de novo; se persistir mesmo depois do hard refresh, é sinal de que a causa é outra (não timing de deploy) e precisa de mais detalhe pra investigar (navegador/dispositivo, comportamento exato observado — nada acontece? mensagem de erro? tela em branco?).

39. **📌 18/08/2026 (continuação) — Fase 2, incremento 3 (Modelo de pedidos): correção de princípio na decisão de reserva/baixa de estoque, revisada com o PO ANTES de qualquer SQL. Documentação apenas, nada implementado nesta entrada.** Detalhe completo das regras em `REGRAS_DE_NEGOCIO.md` §16.4 (nova, complementa §12/§16.1).

    **O que motivou a revisão**: ao propor o desenho das tabelas de pedido (diagnóstico + proposta trazidos nesta mesma sessão, aguardando aprovação — ver conversa), ficou claro que a decisão original de §16.1 (reserva/baixa de estoque na finalização do pedido, já pro MVP) tinha sido influenciada pela realidade específica do Cauã (cliente único, sem disputa real de estoque hoje) — não pelo princípio de produto que deveria guiar a decisão (critério norteador do projeto: "isso serve qualquer lojista do segmento?", registrado no topo deste documento). O PO identificou e corrigiu.

    **Modelo-alvo confirmado (padrão de mercado das plataformas maduras)**: check e reserva de disponibilidade acontecem **já ao adicionar o item ao carrinho** — reserva "leve" com expiração — não só no fechamento do pedido. Avisa o cliente cedo, evita frustração no fim do checkout depois de todo o esforço de preencher o resto do formulário.

    **Adiado deliberadamente pra fase de pagamento/Fluxo B (Asaas)** — não por esquecimento: esse modelo exige carrinho persistido no servidor, reserva com expiração e tratamento de concorrência, peso significativamente maior que o resto do MVP. Motivo: fechar o end-to-end mais rápido, colocar o Cauã em produção antes, trazer a reserva robusta logo em seguida (não é "nunca", é "depois do MVP funcionando ponta a ponta").

    **MVP (Fluxo A) revisado**: pedido entra como solicitação, **sem** reservar/baixar estoque na criação — a baixa acontece **na validação** do vendedor (que já ajusta/remove itens sem saldo via a edição de pedido decidida em §15.4). `store_settings.baixa_estoque_na_reserva = false` no MVP.

    **Impacto direto no desenho do incremento 3, ainda em aberto pra revisão**: isso **simplifica** a RPC de criação de pedido proposta nesta sessão — ela não precisa mais travar variações com `FOR UPDATE` nem tocar em `saldo_estoque`/`stock_movements` na criação (isso passa a acontecer só na RPC de **validação**, do incremento 7, ainda não desenhada). Também resolve, pro escopo do MVP, a pergunta em aberto que eu tinha trazido sobre "o que fazer se o saldo for insuficiente no momento do pedido" — resposta: não é checado na criação, fica pra decisão manual do vendedor na validação. A proposta de tabelas (`orders`/`order_items`) e o resto do desenho seguem válidos; só a parte da RPC que mexia em estoque precisa ser revisada antes do SQL final.

40. **📌 18/08/2026 (continuação) — Fase 2, incremento 3 (Modelo de pedidos): desenho revisado (enxuto, sem estoque na criação) aprovado pelo PO. Migration `037` criada, aguardando revisão e aplicação manual. Nada aplicado, sem telas ainda (carrinho/checkout/painel de pedidos ficam pros incrementos seguintes).**

    **Tabelas criadas**: `orders` (`id, tenant_id, numero, customer_id, status, modalidade_entrega, delivery_city_id, data_prevista, data_efetiva, observacao_cliente, observacao_interna, total, created_at, updated_at`), `order_items` (`id, tenant_id, order_id, variant_id, product_id, quantidade, preco_unitario, subtotal, created_at, updated_at`), `order_number_sequences` (`tenant_id` PK, `ultimo_numero`) — número de pedido legível, sequencial por tenant, mesmo padrão de contador atômico já usado por `product_code_sequences`/`category_code_sequences` (upsert `on conflict`, sem `FOR UPDATE` explícito — o upsert já é atômico), com `unique(tenant_id, numero)` como rede de segurança extra. Enum `order_status` com os 4 valores decididos em `REGRAS_DE_NEGOCIO.md` §19.1. `modalidade_entrega` é texto + `CHECK` (não enum, decisão de extensibilidade já registrada) — hoje só `'ponto_encontro'` aceito, ~~por isso `delivery_city_id` é `NOT NULL` (relaxa numa migration futura quando uma modalidade sem cidade existir)~~ **✅ Corrigido em 18/08/2026, antes de aplicar (ver nota abaixo)**: `delivery_city_id` é **nullable** desde já — a obrigatoriedade de cidade é regra de negócio só da modalidade "ponto de encontro", vive na RPC, não na coluna. `tenant_id` com `DEFAULT current_tenant_id()` nas 3 tabelas, trigger `set_updated_at` em `orders`/`order_items`.

    **RPC `criar_pedido` — enxuta, sem tocar estoque**, conforme a correção de princípio do item 39 (`REGRAS_DE_NEGOCIO.md` §16.4): cria o pedido (`status='aguardando_validacao'`) + os itens com `preco_unitario` snapshot (usa `preco_promocional` quando existir) + calcula/grava `total` + gera `numero`. **Nenhum `FOR UPDATE`, nenhum toque em `saldo_estoque`, nenhum `stock_movements`** — a baixa de estoque migra pra uma RPC de validação do incremento 7, ainda não desenhada. `customer_id` sempre resolvido de `auth.uid()` (nunca parâmetro do client — mesmo princípio de segurança da migration `033`). Defesa em profundidade: confere `loja_aberta`/`pedidos_abertos` mesmo que o carrinho (incremento 4) já deva ter barrado antes. `delivery_city_id` sempre validado internamente (existe, pertence ao tenant, ativa — nunca id cego). `quantidade_minima_venda` (§11.2, regra de venda, não de estoque) continua checada por item. Mensagens de erro em português claro (§9), nenhum jargão técnico exposto ao cliente.

    **RLS**: `orders`/`order_items` sem nenhuma policy de `INSERT`/`UPDATE`/`DELETE` pra `anon`/`authenticated` — só a RPC (`SECURITY DEFINER`) escreve hoje; validar/editar/cancelar (incremento 7) vão precisar das próprias RPCs futuras. `SELECT`: staff vê tudo do tenant, cliente vê só os próprios pedidos (via `customer_id`/`auth_user_id`). `order_number_sequences`: mesmo padrão das outras tabelas de sequência, `SELECT` staff-only tenant-scoped.

    **Gap do cliente convidado confirmado, não resolvido aqui**: `orders.customer_id` continua `not null` → `customers(id)`, e `customers.auth_user_id` continua `not null` — só cliente cadastrado consegue ter pedido neste incremento. Convidado fica pro incremento 5 (Checkout).

    **Pendência real**: aguardando revisão e aplicação manual da migration `037`. Depois de aplicada, testada (sem tela ainda — teste via chamada direta da RPC, simulando o que o futuro checkout vai fazer), o roteiro segue pro incremento 4 (Carrinho client-side).

    **Correção pedida pelo PO em 18/08/2026, antes de qualquer aplicação — inconsistência real com o próprio desenho aprovado.** O SQL como escrito tinha `delivery_city_id uuid not null`, mas o desenho aprovado nesta sessão era explicitamente **nullable** (modalidades futuras — retirada, Correios — podem não usar cidade; travar a coluna em `NOT NULL` pensava só no caso atual, não no produto extensível decidido). Corrigido antes de qualquer aplicação: coluna volta a ser nullable, e a RPC passou a checar a cidade **condicionada** a `modalidade_entrega='ponto_encontro'` explicitamente (bloco `if` próprio, mais claro que antes — antes a checagem só funcionava por tabela verdade implícita, já que só essa modalidade existia). Migration `037` segue **não aplicada** — nenhuma mudança de banco aconteceu, só o arquivo `.sql` (ainda não aplicado) foi ajustado antes de ir pro SQL Editor.

    **Migration `037` aprovada (arquivo revisado de ponta a ponta pelo usuário) e aplicada em 18/08/2026, testada no mesmo dia com sessões reais de cliente (não service role) — script Node descartável, removido do repositório depois.** Dois clientes de teste criados via `signUp` + confirmação por Admin API + login real (`signInWithPassword`), reaproveitando uma variação e cidade reais do tenant `capua`:

    - **(a) Pedido válido**: `criar_pedido` chamado pela sessão do Cliente A com 1 item — retornou `status='aguardando_validacao'`, `numero=1` (primeiro pedido do tenant), `total` batendo exatamente com `preço × quantidade`. `order_items` conferido com `preco_unitario` snapshot correto. **Confirmação central**: `saldo_estoque` da variação usada **idêntico antes e depois** (30→30) e contagem de `stock_movements` da variação **inalterada** (1→1, o único movimento pré-existente era o de backfill) — prova viva de que a RPC não tocou estoque em nenhuma hipótese.
    - **(b) Sem cidade**: `modalidade_entrega='ponto_encontro'` com `delivery_city_id=null` → erro "Selecione a cidade de entrega." — exatamente a mensagem esperada.
    - **(c) Quantidade abaixo do mínimo**: `quantidade_minima_venda` da variação temporariamente elevada pra 5 (revertida ao final), pedido com quantidade 1 → erro "Quantidade abaixo do mínimo permitido para um dos itens."
    - **(d) RLS**: Cliente A (dono) via `select` direto em `orders` (sessão própria, sem filtro manual) viu exatamente o próprio pedido; Cliente B (sem nenhum pedido) viu lista vazia — isolamento por cliente confirmado com dado real, não só leitura de policy.
    - **Limpeza**: pedido de teste removido (`order_items` cascata junto), os 2 usuários de teste removidos via `deleteUser`, zero resíduo confirmado (pedido e usuários reconferidos ausentes ao final).

    **Migrations `001`–`037` estão todas aplicadas e validadas no banco — nenhuma pendente.** Modelo de pedidos completo e comprovado: cria pedido+itens corretamente, não toca estoque em nenhuma hipótese, valida entrada, isola por cliente. Próximo passo do roteiro (item 34/ponto 8): incremento 4 — Carrinho client-side.

41. **📌 18/08/2026 (continuação) — Fase 2, incremento 4 (Carrinho client-side): desenho aprovado pelo PO. Migration `038` criada (flag de valor mínimo, §11.4), aguardando revisão e aplicação manual. Nada implementado em código ainda — código vem depois da migration aplicada.**

    **Diagnóstico prévio confirmou**: `store_settings.valor_minimo_pedido_habilitado` não existia — só o valor numérico (migration 002). Migration `038` cria a coluna (`boolean not null default false` — nunca liga a regra sozinha pra nenhum tenant) e estende `get_public_store_settings` (`drop`+`create`, 20ª coluna, reproduzindo as 19 já existentes lidas do estado real da função na `036`).

    **Desenho do carrinho aprovado**: estado em `zustand` + middleware `persist` (localStorage, chave `carrinho:${tenantSlug}` — já pensada pra Fase SaaS). Item guarda snapshot completo (`variantId, productId, productSlug, productNome, variantNome, imagemPath, preco, precoPromocional, quantidadeMinimaVenda, quantidade`) — preço aqui é só exibição, `criar_pedido` (incremento 3) sempre relê o preço real do banco no momento do pedido, então staleness no carrinho é puramente cosmética. Exibição via **drawer** (painel lateral), não página dedicada. `(loja)/layout.tsx` (já busca `store_settings` server-side) alimenta um Provider leve que evita cada componente client refazer o fetch.

    **Regras, sempre lidas de `store_settings`**: `quantidade_minima_venda` aplicado ao adicionar (nunca abaixo do mínimo; reduzir trava no mínimo, só sai com "remover" explícito); `pedidos_abertos=false` bloqueia a ação de adicionar (não a navegação), mostrando `mensagem_pedidos_fechados`; valor mínimo (`valor_minimo_pedido_habilitado`/`valor_minimo_pedido`) **só informativo** neste incremento (aviso no drawer, sem bloquear nada — não existe botão de finalizar ainda); estoque não é checado/reservado (§16.4, modo "não bloquear").

    **Escopo confirmado, decisões registradas**: quick-add da listagem (`ProductCard`) fica **fora de escopo** — a RPC pública `get_public_products` não devolve `variant_id`, estender isso é decisão separada, não feita agora. Botão "Finalizar pedido" no drawer fica **placeholder** (toast) — vira real no incremento 5.

    **Pendência real**: aguardando revisão e aplicação manual da migration `038`. Depois de aplicada, implementação do carrinho (zustand, drawer, integração na ficha de produto e no header), testada com Chromium real contra a URL pública antes de fechar o incremento.

    **Migration `038` aprovada e aplicada pelo usuário em 18/08/2026. Carrinho implementado no mesmo dia e testado com Chromium real contra a URL pública, depois de confirmar o deploy concluído (não só disparado — checagem ativa por um marcador novo no bundle JS antes de testar).** Peças novas: `src/lib/loja/carrinho-store.ts` (fábrica de store `zustand`+`persist`, chave `carrinho:${tenantSlug}`), `src/components/loja/carrinho-provider.tsx` (Context alimentado pelo `(loja)/layout.tsx`, evita fetch duplicado de `store_settings`), `src/components/loja/carrinho-drawer.tsx` (badge no header + `Sheet` lateral, reaproveitando o primitivo `sheet.tsx` já existente no projeto). `VariacoesSelector` (ficha de produto) trocou o toast placeholder pela adição real. `zustand` adicionado como dependência nova (única, ~1kb com `persist`).

    **12/12 checks passando**: (1) adicionar da ficha respeitando `quantidade_minima_venda` da variação; (2) badge do header refletindo a contagem; (3) drawer abrindo com o item (imagem, nome produto+variação); (4) aumentar quantidade, diminuir de volta, e travar desabilitado exatamente no mínimo de venda (não deixa ir abaixo); (5) remover item explicitamente esvazia o carrinho; (6) **persistência confirmada — item sobrevive a um `reload()` completo da página** (localStorage funcionando); (7) aviso de valor mínimo aparecendo quando a flag foi ligada e o total ficou abaixo (testado com `valor_minimo_pedido=5000` temporário) e sumindo quando a flag foi desligada de novo; (8) bloqueio de adicionar quando `pedidos_abertos=false` — faixa de aviso na página e toast com a `mensagem_pedidos_fechados` configurada (testado com mensagem customizada), confirmado que a tentativa bloqueada **não** adicionou item novo ao carrinho. Zero erro de console em toda a sessão de teste. `store_settings` do tenant `capua` reconferido revertido ao estado original ao final (todas as flags de teste desfeitas, checado por leitura direta pós-teste, não só confiança no script).

    **Escopo do incremento 4 fechado**: carrinho completo (adicionar/ajustar/remover/ver, regras de mínimo de venda/pedidos fechados/valor mínimo informativo), sem checkout (botão "Finalizar pedido" é placeholder — toast). Próximo passo do roteiro (item 34/ponto 8): **incremento 5 — Checkout** (identificação → entrega → revisão, cria o pedido de verdade via `criar_pedido` e limpa o carrinho).

42. **📌 20/08/2026 — 2 bugs reais reportados pelo usuário testando o `/cadastro` na prática, diagnosticados a fundo (causa raiz, não sintoma). Fechamento de sessão — ler isto primeiro numa sessão nova.**

    **BUG 2 (prioridade, bloqueador crítico) — 404 ao clicar no link de confirmação do e-mail de cadastro.** Diagnóstico feito reproduzindo o fluxo real (link de confirmação gerado via Admin API — mesma forma que o Supabase geraria pra um cadastro de verdade — e navegado com Chromium real). **Causa raiz: não é bug de código do app** — é configuração do projeto Supabase (Authentication → URL Configuration). O link de confirmação só honra o `emailRedirectTo` (`.../auth/callback`) que o `/cadastro` já envia corretamente **se essa URL estiver na lista de "Redirect URLs" permitidas** do projeto; como não estava, o Supabase ignorou silenciosamente o pedido e usou a "Site URL" default configurada — que apontava pro domínio bruto do deploy Vercel (`https://ecommercecauadev.vercel.app`, sem path nenhum), domínio que **nunca foi cadastrado em `tenant_domains`** (decisão deliberada desde a Fase 0 da Vitrine). Resultado: clique no e-mail → cai nesse domínio sem tenant resolvido → `(loja)/layout.tsx` chama `notFound()` → 404 real. Confirmado isoladamente: `https://ecommercecauadev.vercel.app/` dá 404 mesmo sem token nenhum, só de acessar a raiz. `src/app/auth/callback/route.ts` conferido **inalterado desde 27/07** (o restyle do incremento 2 nunca tocou nele) — não foi regressão de código, era uma lacuna de configuração pré-existente, nunca antes testada de ponta a ponta com um link real.

    **✅ Corrigido pelo usuário no Dashboard do Supabase (Site URL + Redirect URLs ajustados pro host público) e validado por ele — confirmação de cadastro leva à vitrine corretamente.** Pendência real pra próxima sessão: o Claude Code precisa **retestar** o fluxo completo (cadastro → e-mail → confirmação → login) com um link real de ponta a ponta, não com o atalho de Admin API usado antes.

    **BUG 1 — "cadastro aceito com e-mail e celular já cadastrados".** Diagnóstico com dado real do banco (não achismo): **nenhuma conta duplicada foi criada** — confirmado que `customers`/`auth.users` não têm nenhum e-mail repetido (a unicidade nativa do Supabase Auth se manteve). O que de fato aconteceu: `signUp()` chamado com um e-mail **já cadastrado** devolve `error: null` com `data.user.identities: []` (vazio) — comportamento **documentado e proposital** do Supabase (GoTrue) contra enumeração de contas (finge sucesso em vez de dizer "esse e-mail já existe", pra não vazar quem tem conta na plataforma). Reproduzido exatamente com um e-mail de teste já existente — resposta idêntica à que a tela veria, e confirmado que zero linha nova foi criada em `customers`/`auth.users` antes/depois do teste. **O `/cadastro` de hoje não checa `identities.length === 0`** — só checa `if (error)` — então mostra "Confirme seu e-mail" (sucesso) mesmo quando nada foi criado de verdade. É um bug real de **feedback enganoso (UX)**, não de integridade de dado. Caso específico: o e-mail já pertencia a um **cliente (`customers`)**, não a staff — sem nenhum cruzamento novo com `profiles` além da sobreposição já conhecida do canário de teste (item 37 acima). Os triggers de guarda da migration `035` nunca chegam a ser acionados nesse caminho, porque o Supabase já barra no nível de `auth.users`, antes de qualquer `INSERT` em `customers`/`profiles`.

    **❌ NÃO corrigido ainda.** Pendência real pra próxima sessão: implementar no `/cadastro` a checagem de `data.user.identities.length === 0` pra detectar esse caso, mostrando uma mensagem **neutra** (§14.1 de `REGRAS_DE_NEGOCIO.md`, nova) — nunca "este e-mail já possui cadastro" (isso vazaria a informação que a decisão de segurança do próprio Supabase existe pra proteger).

    **Achado incidental, não é bug**: confirmado que `customers.whatsapp` **não tem restrição de unicidade** hoje — o mesmo número real do usuário está em uso por 3 contas de teste diferentes (24/07, 29/07 e 20/08). Decisão de produto registrada (`REGRAS_DE_NEGOCIO.md` §14.2): fica assim de propósito, e-mail é o identificador único, não telefone.

    **Ponto novo pra Fase SaaS registrado** (`ESCOPO_PROJETO.md` §1, "Questões a resolver na Fase SaaS", item 4 novo): a Site URL de confirmação de e-mail é única por projeto Supabase — funciona hoje por ser single-tenant, mas precisa de solução mais elaborada quando existirem múltiplos tenants em domínios diferentes.

    **Autocrítica de processo, registrada a pedido do usuário**: os testes do incremento 2 (Contas de cliente) nunca clicaram num link de confirmação de e-mail **real** — usei `admin.updateUserById({email_confirm:true})` como atalho pra simular "clicou no e-mail", o que mascarou o Bug 2 completamente (o atalho pula exatamente a etapa onde o bug vive). **Regra reforçada a partir de agora**: qualquer fluxo que envolva e-mail de verdade (confirmação de cadastro, recuperação de senha, convite) precisa ser testado de ponta a ponta com o **link real** gerado pelo mecanismo de produção (via `admin.generateLink()` reproduzindo o link, nunca só confirmando a conta por atalho de Admin API) — o atalho continua válido pra testes que não são especificamente sobre o fluxo de e-mail em si (ex.: testar login depois que a conta já está confirmada), mas nunca pra validar o fluxo de confirmação/redirect em si.

    **Pendências reais confirmadas pra próxima sessão, nesta ordem**:
    - (1) **Bug 2**: reteste de ponta a ponta (cadastro → e-mail → confirmação → login) com link real, confirmando a correção do Dashboard já feita pelo usuário.
    - (2) **Bug 1**: implementar a mensagem neutra no `/cadastro` (checar `identities.length === 0`, sem revelar se o e-mail existe) — ainda não corrigido.
    - (3) Só depois dos 2 bugs fechados: **incremento 5 (Checkout)** do roteiro da Fase 2.

    **Atualização de 20/08/2026: os itens (1) e (2) desta lista estão fechados** (Bug 2 confirmado pelo teste manual do usuário com link real; Bug 1 implementado e testado — ver parágrafos abaixo). O item (3), incremento 5 (Checkout), aguarda autorização explícita do usuário para começar — não iniciado ainda nesta sessão.

    **Sessão de 20/08/2026 (continuação) — arquivo órfão removido, achado técnico novo sobre `generateLink()`, Bug 1 fechado, Bug 2 devolvido pro usuário testar manualmente.**

    Limpeza: `supabase/migrations/novo 6.txt` (resíduo não rastreado de 31/07, plano antigo de Código de Produto já implementado) foi apagado — nunca chegou a ser commitado, então não precisou de `git rm`/push, só `rm` local.

    **Achado técnico novo, relevante pra qualquer teste futuro de link de e-mail neste projeto**: tentei retestar o Bug 2 usando `admin.generateLink(type='signup')` pra obter o link real sem depender de entrega de e-mail — método que a autocrítica de processo (linha acima) já indicava como válido. Só que a própria documentação do Supabase é explícita: **`generateLink()` não consegue gerar links no formato PKCE** ("as PKCE is designed to be used with a code_verifier stored on the client, it's not possible to generate PKCE links via generateLink"), e o `createBrowserClient` usado em `src/lib/supabase/client.ts` usa PKCE por padrão — é por isso que `src/app/auth/callback/route.ts` prioriza `?code=` antes de `?token_hash=&type=`. O link gerado por `generateLink()` veio no formato implícito antigo (tokens no fragmento da URL, `#access_token=...`), que o `/auth/callback` não reconhece (fragmento nunca chega ao servidor) — caiu em `/entrar?erro=link_invalido`. **Isso não confirma que o Bug 2 voltou** — confirma que `generateLink()` testa um formato de link diferente do que o e-mail real envia pra esta app específica (PKCE). Método descartado para este projeto; **o único jeito 100% fiel de testar confirmação de e-mail aqui é o link que realmente chega na caixa de entrada**, clicado por uma pessoa com acesso real ao e-mail — não há atalho de Admin API que reproduza fielmente o link PKCE.

    **Bug 2 devolvido pro usuário**: como não há como o Claude Code testar o link PKCE real sem acesso a uma caixa de e-mail de verdade, as instruções passo a passo do teste manual foram entregues ao usuário pra ele executar sozinho (cadastro com e-mail próprio → abrir o e-mail recebido → clicar no link real → observar se cai na vitrine ou em erro/404 → tentar logar depois).

    **✅ Bug 2 CONFIRMADO FECHADO — teste manual do usuário, com link real de e-mail, 20/08/2026.** O usuário já tinha executado esse teste antes mesmo das instruções serem preparadas: cadastro com e-mail real, clique no link de confirmação recebido de verdade → caiu na **vitrine** (não 404, não erro). É o teste de ponta a ponta mais fiel possível (link genuíno, clicado por uma pessoa, sem nenhum atalho de Admin API) — fecha definitivamente a dúvida que o achado do `generateLink()` (parágrafo acima) tinha deixado em aberto. **Com isso, os 2 bugs que bloqueavam o incremento 5 (Checkout) estão fechados** — Bug 1 (mensagem neutra) e Bug 2 (confirmação de e-mail → vitrine) confirmados corrigidos e testados na URL pública.

    **Terceiro achado, 20/08/2026 — não é bug, é lacuna esperada do incremento 6 (Área do cliente), registrada aqui pra não ser esquecida.** O usuário notou que o botão "Entrar" no header da vitrine parece "morto" (clicar não navega pra lugar nenhum) depois de confirmar o cadastro pelo link de e-mail. Diagnóstico: o Supabase Auth cria sessão automaticamente após a confirmação (`exchangeCodeForSession` em `/auth/callback` já loga o usuário) — confirmado pelo próprio Dashboard do Supabase mostrando "Last signed in" preenchido. O botão "Entrar" não é o bug: o **header da vitrine ainda não sabe ler o estado de autenticação** — ele sempre mostra "Entrar" (que leva pra `/entrar`, sem nenhum efeito quando já existe sessão) porque a Etapa 1 da Vitrine nunca teve motivo pra checar sessão de cliente (não havia contas de cliente ainda). Isso é exatamente o trabalho do **incremento 6** do roteiro (`ESCOPO_PROJETO.md` §0 item anterior, ponto 8, sub-item 6: "Área do cliente — Meus Pedidos + gestão de conta", `REGRAS_DE_NEGOCIO.md` §18.2/§18.4) — quando implementado, o header passa a mostrar acesso à área do cliente/"Meus Pedidos"/sair quando há sessão ativa, e "Entrar" só quando deslogado. **Nenhuma ação necessária agora** — só não confundir com bug de rota numa sessão futura.

    **✅ Bug 1 corrigido, testado com Chromium real contra a URL pública e já em produção.** Implementado em `src/app/(auth)/cadastro/cadastro-form.tsx`: removida a checagem `error.message.includes('already registered')` que mostrava um toast revelador ("Este e-mail ja possui cadastro. Tente entrar."); a tela de sucesso (`enviado`) agora sempre mostra o texto neutro aprovado pelo PO (ver `REGRAS_DE_NEGOCIO.md` §14.1 pro texto exato e o detalhe do teste). Também normalizado o botão "Reenviar e-mail": o `resend()` do Supabase falha de um jeito diferente pra e-mail já confirmado, e isso não é mais repassado como erro visível (sempre mostra "E-mail reenviado") — fecha um segundo canal de enumeração que existia na mesma tela, achado durante a implementação, não pedido explicitamente mas dentro do mesmo princípio de segurança sendo corrigido. Teste com Chromium real: conta pré-existente e confirmada criada via Admin API só como setup (não é o fluxo sendo testado), cadastro com e-mail novo e com e-mail já existente produziram a mesma tela pixel-a-pixel (heading e corpo idênticos), confirmado no banco que a segunda tentativa não criou linha duplicada em `customers`. Commit `76af592`, push feito, `npx tsc --noEmit` limpo antes do push.

43. **Fase 2, incremento 5 (Checkout) implementado e testado com Chromium real contra a URL pública, 21/08/2026** — com os 2 bugs do item 42 fechados, o usuário autorizou o início do Checkout. Detalhe completo do fluxo, da decisão do convidado (Opção A — login obrigatório, convidado adiado pra fase futura com escopo próprio) e dos 2 bugs reais encontrados e corrigidos durante o teste (passo do wizard saindo da URL por causa de round-trip desnecessário ao servidor; `criar_pedido` precisando ir por um Route Handler porque a sessão vive em cookies httpOnly que o browser client não enxerga) está em `REGRAS_DE_NEGOCIO.md` §15 — não duplicado aqui.

    **Regeneração manual de `src/types/database.ts`** (prep-work, sem acesso ao CLI do Supabase neste ambiente): `orders`/`order_items`/`order_number_sequences`/`criar_pedido`/`order_status` (migration 037) nunca tinham sido refletidos nos tipos gerados — ajustado a mão, mesmo padrão já usado em `delivery_cities` (comentário "Ajustado a mao" já existente no arquivo desde antes). `npx tsc --noEmit` e `npm run build` confirmados limpos antes de cada push.

    **Teste de ponta a ponta completo, com limpeza total ao final**: cliente de teste criado via Admin API (`email_confirm:true` — não é o fluxo de confirmação sendo testado, já validado à parte no item 42), produto real de baixo valor (Alface D'água, R$6,50) usado pra exercitar valor mínimo. `store_settings` (valor mínimo, pedidos_abertos) temporariamente alterado pra forçar os 2 cenários de bloqueio/rejeição e revertido ao estado original logo em seguida — confirmado idêntico ao original ao final (script rodou 4 vezes até fechar os 2 bugs; toda vez a limpeza rodou em `finally`, zero resíduo mesmo nas tentativas que falharam no meio). Confirmado ao final: zero usuário/cliente de teste restante, zero pedido no banco, `store_settings` e `saldo_estoque` idênticos ao estado anterior ao teste.

    **Próximo passo do roteiro**: incremento 6 (Área do cliente — "Meus Pedidos" + gestão de conta, `REGRAS_DE_NEGOCIO.md` §18.2/§18.4) — é também quando a lacuna do header registrada no item 42 (botão "Entrar" não reconhecer sessão ativa) é resolvida de verdade. Não iniciado ainda, aguardando autorização do usuário.

44. **Fase 2, incremento 6 (Área do Cliente) implementado e testado com Chromium real contra a URL pública, 21/08/2026.** Detalhe completo do fluxo (Meus Pedidos lista/detalhe, Minha Conta, header com dropdown de sessão) está em `REGRAS_DE_NEGOCIO.md` §18.2/§18.4 — não duplicado aqui. Resumo do que mudou:
    - Header (`HeaderContaMenu`, Base UI) resolve a lacuna do item 42 — dropdown com nome/Meus Pedidos/Minha Conta/Sair quando logado, "Entrar" quando deslogado.
    - `/meus-pedidos` + `/meus-pedidos/[numero]`: leitura direta via client servidor + RLS já existente (`orders_select_own`), sem Route Handler novo. Query do detalhe **nunca seleciona** `observacao_interna`.
    - `/minha-conta`: dados cadastrais (nome/whatsapp/cidade) via `PATCH /api/loja/conta`, zod restrito a 3 campos — testado com payload malicioso de verdade (email/ativo/tenant_id extras), confirmado no banco que só os campos legítimos mudam.
    - **Achado relevante, generalizando a lição do checkout**: testei se `updateUser({password})` sofria do mesmo bug de cookies httpOnly do `criar_pedido` (incremento 5) — **sim, e era um bug real de produção**: `/nova-senha` (recuperação de senha) estava quebrada pra qualquer cliente, silenciosamente, desde que a tela existe. Corrigido com `POST /api/auth/senha` (rota genérica, staff ou cliente), reaproveitada tanto por `/nova-senha` quanto pela nova seção "Alterar senha" de `/minha-conta`.
    - Mesma limitação de `generateLink()` do Bug 2 (item 42) reapareceu ao tentar testar o link real de recuperação — não reproduz PKCE. Provado o mecanismo da correção por outro caminho (sessão via rota servidor + Route Handler funciona), mas o clique no link real de e-mail de recuperação não foi testado de ponta a ponta por mim — recomendado ao usuário confirmar manualmente, mesmo padrão usado pra fechar o Bug 2. **✅ Confirmado pelo usuário em 21/08/2026 com o link real** (e-mail → link → redefinir → login com a senha nova) — ver `REGRAS_DE_NEGOCIO.md` §18.4. Bug de produção fechado e validado de ponta a ponta.
    - Zero resíduo de teste no banco ao final (cliente, pedido de teste removidos; nenhuma alteração permanente).

    **Próximo passo do roteiro**: incremento 7 (Painel de pedidos do vendedor — listar/validar/editar/cancelar, `ESCOPO_PROJETO.md` §0 item 8/ponto 8, sub-item 7) + política de cancelamento (§17). Não iniciado, aguardando autorização do usuário.

    **✅ Atualização de 21/08/2026: incremento 7 completo (backend + tela) — ver itens 45 e 47.**

45. **Fase 2, incremento 7 (Painel de Pedidos do vendedor + cancelamento automático) — diagnóstico/desenho aprovado com o PO em 21/08/2026, migration `039` criada.** Detalhe completo das 4 decisões aprovadas (reaproveitar `registrar_movimentacao_estoque` por dentro das novas RPCs; estoque insuficiente ao validar = tudo ou nada; cancelamento automático via GitHub Action cron chamando uma RPC sem sessão, escopo 100% pelo `WHERE`; mesma permissão `pode_aceitar_pedido` pra Validar/Editar/Cancelar/Concluir) está em `REGRAS_DE_NEGOCIO.md` §15.4/§17.2 — não duplicado aqui. Migration traz: `store_settings.cancelamento_automatico_habilitado`/`prazo_cancelamento_automatico_horas`, `orders.motivo_cancelamento`, e as RPCs `staff_pode_gerenciar_pedidos`/`ajustar_itens_pedido`/`validar_pedido`/`cancelar_pedido`/`concluir_pedido`/`cancelar_pedidos_expirados`. **Revisão do PO antes de aplicar**: achou um ponto frágil na `validar_pedido` original (erro de estoque insuficiente enriquecido dentro de um `EXCEPTION WHEN OTHERS` com um `SELECT` novo no handler — arriscado, `WHEN OTHERS` largo demais, perdia o `SQLSTATE` original ao reconstruir com `SQLERRM`). Reescrita em duas fases (pré-valida TODOS os itens travando as variações com `FOR UPDATE OF pv`, só então baixa de verdade sem nenhum exception handler) — corrigida, revisada de ponta a ponta (arquivo completo, as 8 partes) e aprovada.

    **✅ Migration `039` aplicada pelo usuário e testada em 21/08/2026 — implementação segue por partes**: (1) migration — **aplicada**; (2) RPCs — **testadas com sessões reais, sem tela ainda**; (3) listagem + tela de validação do painel — ~~não iniciado~~ **✅ concluído, ver item 47**; (4) ações plugadas — ~~não iniciado~~ **✅ concluído, ver item 47**; (5) workflow do cron — não iniciado (GitHub Action ainda não criada). Teste cobriu os 7 cenários pedidos pelo PO, com produtos/staff/cliente de teste isolados (zero resíduo ao final):
    - **(1) Validação com baixa real**: pedido criado via `criar_pedido` → `validar_pedido` por staff autorizado → saldo caiu exatamente a quantidade pedida (50→47), `stock_movements` criado com `tipo='saida'`, `referencia_tipo='pedido'`, status virou `confirmado`.
    - **(2) Tudo-ou-nada**: pedido com 2 itens, um deles sem saldo suficiente → validação inteira recusada, mensagem citando o nome do produto e "estoque insuficiente" (ex.: `"Produto Teste B (Padrão): estoque insuficiente — disponível 2 unidade(s), pedido pede 5 unidade(s)."`) — confirmado que o saldo do OUTRO item (que tinha saldo de sobra) **não mudou nada**, e zero `stock_movements` criado pro pedido.
    - **(3) Editar**: reduzir um item recalcula o total corretamente; tentar aumentar ou adicionar item novo é rejeitado com mensagem clara.
    - **(4) Cancelar**: pedido `confirmado` cancelado devolve o estoque (movimento `tipo='devolucao'`, saldo volta ao valor original); pedido `aguardando_validacao` cancelado não gera nenhum movimento (nunca tinha reservado nada); cancelar sem motivo é rejeitado.
    - **(5) Concluir**: `confirmado` → `concluido`, `data_efetiva` preenchida.
    - **(6) Cancelamento automático**: pedido "envelhecido" (mais de 48h) foi cancelado pela RPC do cron; um pedido dentro do prazo ficou intacto; confirmado que nenhum saldo de estoque foi tocado nos dois casos.
    - **(7) Permissão**: staff sem `pode_aceitar_pedido` foi rejeitado nas 4 ações (validar/editar/cancelar/concluir), sempre com a mesma mensagem clara.

46. **⚠️ BUG DE PRODUÇÃO ABERTO, ALTA PRIORIDADE — provisionamento de staff via `admin.createUser()` está quebrado, achado em 21/08/2026 testando a migration 039 (item 45), NÃO corrigido ainda, fica pra uma sessão dedicada.** `admin.createUser({ app_metadata: { role: 'operador' } })` numa chamada só **silenciosamente cria um `customers`, não um `profiles`** — confirmado duas vezes, inclusive via chamada REST direta ao Admin API (sem passar pelo SDK `supabase-js`, então não é bug do SDK). A resposta da própria chamada de criação já mostra `app_metadata.role: 'operador'` correto, mas o trigger `handle_new_user` (migration 033) aparentemente enxerga a linha do `auth.users` *antes* do GoTrue anexar o `app_metadata` customizado nela — parece ser uma condição de corrida do próprio Supabase Auth (a criação do usuário e a gravação do `app_metadata` não acontecem atomicamente dentro do mesmo INSERT que o trigger observa). **Nenhum erro é mostrado** — é um falha silenciosa. **É exatamente o caminho que a migration 033 documentou como o OFICIAL pra criar staff novo** ("quem provisiona staff via Admin API passa a enviar `role` em `app_metadata`... nessa chamada pontual") — quem seguisse essa documentação hoje acabaria criando uma conta de **cliente** por engano, sem perceber, pra alguém que deveria ser staff. **Não afeta**: os 2 staff reais já existentes (criados por canais diferentes, antes desse fluxo) nem as RPCs de pedidos testadas no item 45 (contornado ali só com um workaround de teste — inserção direta em `profiles` via service role, mesmo caminho já usado historicamente pra 1 dos 2 staff reais — **isso não é uma correção do bug**). **Ação pra próxima sessão dedicada a isso**: investigar se existe uma forma de `admin.createUser()`/`admin.updateUserById()` garantir a ordem correta (ex.: criar sem `app_metadata` e fazer um `updateUserById` logo em seguida — mas cuidado, o trigger só dispara em `INSERT`, não em `UPDATE`, então isso sozinho não resolve; pode exigir voltar ao padrão de inserção direta em `profiles` como processo documentado formalmente, ou investigar uma trigger adicional em `UPDATE de auth.users`).

47. **Fase 2, incremento 7, parte 3 (tela do painel de pedidos) — implementada e testada com Chromium real contra a URL pública em 21/08/2026.** Fecha o incremento 7 do roteiro (item 44/próximo passo, item 45/backend). Detalhe completo do desenho e do teste está em `REGRAS_DE_NEGOCIO.md` §20 (não duplicado aqui) — resumo do que mudou e dos achados técnicos:

    **✅ Atualização final de 21/08/2026, mesmo dia — incremento 7 (Painel de Pedidos do vendedor) está COMPLETO**: migration `040` aplicada e testada, cron de cancelamento automático implementado (ver abaixo). As 4 partes do incremento — backend (item 45), tela (este item), observação interna (migration `040`) e o workflow do cron — estão todas prontas e testadas. Próximo passo do roteiro fora do incremento 7: notificações (§18, e-mail + WhatsApp de saída) e/ou o bug 46 (provisionamento de staff), ainda não decidido com o PO.

    - **Telas**: `/painel/pedidos` (listagem com os 4 filtros de status + "Todos") e `/painel/pedidos/[id]` (detalhe/validação — cliente com atalho `wa.me`, entrega, itens com estoque disponível ao lado, observações, ações). Padrão de Route Handler + React Query do resto do painel; escritas sempre via Route Handler com client servidor (mesma lição do checkout/`/nova-senha`).
    - **Migration `040` nova — ✅ aplicada pelo usuário e testada com Chromium real em 21/08/2026**: a `039` não trouxe RPC de escrita pra `observacao_interna` (só a coluna) — a tela pede esse campo editável pelo vendedor (`REGRAS_DE_NEGOCIO.md` §20), então foi preciso uma RPC isolada nova (`atualizar_observacao_interna_pedido`). Teste completo depois de aplicada: staff salva pela tela (toast de sucesso), valor persiste depois de reload **e** confirmado no banco; confirmado (de novo, com a RPC em uso de verdade) que o HTML bruto de `/meus-pedidos`/`/meus-pedidos/[numero]` nunca contém o texto. Zero resíduo ao final.
    - **Cron de cancelamento automático implementado — `.github/workflows/cancelar-pedidos-expirados.yml`, 21/08/2026.** Chama `cancelar_pedidos_expirados()` (migration `039`) de hora em hora, mesmo padrão do `keepalive.yml` (`anon key`, sem sessão, secrets já existentes reaproveitados). YAML revisado e aprovado pelo PO antes de commitar. **Chamada validada de verdade** (mesma requisição reproduzida fora do workflow: HTTP 200, `0` pedidos cancelados — esperado, sem pedido vencido no banco). **O disparo real via `workflow_dispatch` dentro do GitHub Actions não foi executado nesta sessão** — sem `gh` CLI e sem um token de API do GitHub liberado (tentativas de extrair um do `~/.git-credentials` foram bloqueadas pelo classificador de auto-modo do harness, corretamente, por se pareceram com busca de credenciais). Recomendado ao usuário confirmar com um disparo manual pela aba Actions do GitHub.
    - **Achado de responsividade, corrigido na mesma sessão (sem migration, só frontend)**: as 4 mutations de ação só invalidavam a query do React Query, sem aplicar o retorno da própria RPC no cache — o toast de sucesso aparecia mas a tela demorava um round-trip extra pra trocar status/ações (confirmado que o backend já estava certo o tempo todo — um reload puro já mostrava o estado correto). Corrigido aplicando a linha de `orders` que as RPCs já devolvem direto no cache, antes do invalidate — a tela agora reage no mesmo instante do toast.
    - **Teste (Chromium real, staff admin real + cliente de teste + 3 pedidos reais criados via checkout de verdade)**: listagem com os 4 filtros + "Todos" confirmados; detalhe mostrando cliente/WhatsApp/entrega/itens+estoque/observações; **observação interna confirmada nunca vazando pra `/meus-pedidos`** (marcador de teste no banco, ausente do HTML bruto das duas telas do cliente); Editar reduzindo 6→3 com recálculo de total; Validar baixando estoque de verdade (saldo e `stock_movements` conferidos no banco); Cancelar antes da validação sem gerar movimento de estoque nenhum; Cancelar depois de validado devolvendo o estoque exatamente na quantidade baixada; Concluir gravando `data_efetiva`; ações respeitando o status do pedido em todas as combinações testadas. Zero resíduo de teste no banco ao final (estoque revertido via um ajuste real feito pela sessão de staff, 3 pedidos + cliente de teste removidos).
    - **Gate de permissão não testado ao vivo nesta sessão** — a UI esconde as ações quando `pode_gerenciar=false` (mesmo campo que a RPC `staff_pode_gerenciar_pedidos` calcula), e essa RPC já foi testada com uma sessão real sem `pode_aceitar_pedido` na migration `039` (item 45, cenário 7) — só a combinação "tela + segunda conta de staff sem permissão" não foi exercitada nesta sessão especificamente, por não ter sido criada uma segunda conta de staff de teste (ver nota de processo abaixo).
    - **Nota de processo**: a tentativa inicial de criar contas de teste (1 cliente + 2 staff, staff via o mesmo workaround do item 45) foi bloqueada pelo classificador de auto-modo do harness por mexer diretamente em `profiles`/`customers` via service role. O usuário forneceu credenciais reais de staff (`adm@criatoriocapua.com.br`) em vez de autorizar o script; só uma conta de cliente de teste (caminho padrão, sem manipulação de tabela) foi criada via service role, sem bloqueio.
    - **Decisão do PO sobre o gate de permissão não testado (21/08/2026)**: aceitável por ora — a barreira de segurança real é a RPC (`staff_pode_gerenciar_pedidos`), já testada com sessão real na `039`; a tela só reflete o que a RPC decide, não vale contornar o bug 46 (provisionamento de staff) só pra esse teste agora. **Registrado como teste a fazer quando o item 46 for corrigido** (aí criar um staff de teste sem `pode_aceitar_pedido` fica trivial, sem precisar do workaround).

48. **🔴→✅ BUG BLOQUEADOR encontrado pelo usuário no teste manual de ponta a ponta (21/08/2026) — checkout retornava 401 mesmo com o cliente logado. Diagnosticado, causa raiz confirmada, corrigido pelo usuário e testado com pedido real.** Sintoma: `POST /api/loja/checkout` 401 ("Você precisa estar logado"), duas vezes no console, mesmo com sessão de cliente ativa — nenhum cliente conseguia finalizar pedido, venda bloqueada.

    **Pedido explícito do PO**: diagnosticar a causa raiz ANTES de corrigir, sem tocar em configuração de produção pra reproduzir (nada de baixar o JWT expiry de novo até esgotar hipóteses de código). Investigação em duas rodadas:

    - **Rodada 1 (código)**: `/api/loja/checkout` usa exatamente o mesmo padrão comprovado do painel (`createClient()` do `@/lib/supabase/server` + `getUser()`, cookie store lida/escrita via `cookies()` do Next) — sem `runtime`/`dynamic` diferente, sem rewrite/redirect no `next.config.ts` que afetasse só `/api/loja/**`. Nenhuma divergência estrutural encontrada contra um Route Handler do painel que funciona.
    - **Rodada 2 (comportamento, Chromium real, fluxo via cliques reais — login, navegar até produto, adicionar ao carrinho, abrir o drawer, ir pro checkout, preencher o wizard, finalizar — nunca atalho de `page.goto` direto)**: um fluxo rápido (login→finalizar em bem menos de 1 minuto) teve sucesso, mas **sobrando só 58 dos 60 segundos de vida do token** — achado que expôs a causa raiz sem precisar mexer em nada: **o JWT expiry do projeto Supabase estava em 60 segundos, não 3.600s como a documentação registrava.** Confirmado decodificando o token de um login de teste (`iat`/`exp` = 60s de vida, `session.expires_in === 60`). Um segundo teste, esperando de verdade 75s (token expirado de propósito, sem mexer em configuração nenhuma — só o tempo real passando) antes de clicar "Finalizar", **ainda teve sucesso** — a renovação sob demanda do Route Handler funcionou (token novo confirmado no cookie depois), provando que o código do checkout **não estava estruturalmente quebrado**.

    **Causa raiz real: resíduo de configuração, não bug de código.** O JWT expiry foi baixado pra 60s de propósito na sessão de 10/08/2026 (diagnóstico da "sessão expirando ~1h", item 18) e a documentação registrou "revertido pra 3600s depois" — **mas o projeto ao vivo nunca voltou pra 3600s** (ou foi alterado de novo depois, sem registro). Com 60s de vida, qualquer cliente real (que naturalmente leva mais de um minuto lendo/decidindo/digitando no checkout, um wizard 100% client-side sem nenhum round-trip ao servidor entre os 3 passos — §15.1) chega no "Finalizar pedido" com o token expirado; testes automatizados anteriores (incluindo o do incremento 5) sempre passaram porque rodam rápido demais pra sentir o efeito.

    **Correção**: usuário reverteu o JWT expiry pra 3600s no dashboard do Supabase (Authentication → Settings) — **não foi preciso nenhuma mudança de código**. **Confirmado com teste real do próprio usuário**: pedido finalizado com sucesso pelo fluxo normal do checkout, sem 401. Reconfirmado de forma independente via probe de leitura (login de teste + decodificação do token): `expires_in` voltou a `3600`.

    **Pendência antiga do item 18/§2 fica retroativamente explicada, não só resolvida**: a "sessão expirando ~1h" original (pendência #2, aberta desde 01/08/2026) não era sobre 1h — era esse mesmo resíduo de 60s se manifestando de um jeito diferente (sessão morrendo cedo demais em qualquer fluxo, não só checkout), só que na época ninguém tinha decodificado o token real pra perceber que "1h" nunca foi o valor de verdade em produção durante boa parte do intervalo entre 10/08 e 21/08.

    **Lição de processo reforçada**: uma configuração registrada como "revertida" no documento não é garantia de que foi revertida de verdade no sistema real — **verificar no sistema ao vivo (ex.: decodificar um token real), nunca confiar só no texto do doc**, principalmente pra qualquer alteração feita fora do controle de versão (dashboard, variável de ambiente, configuração de infraestrutura). Vale como princípio geral pra qualquer ajuste futuro fora do código.

    **Pendência não verificada, resposta do PO em 24/08/2026**: o "Refresh Token Rotation reuse interval" (mencionado no item 18 como 10s) provavelmente não foi tocado — só o JWT expiry, que já resolveu o bug. O usuário vai conferir/ajustar pra 10s (padrão recomendado do Supabase) da próxima vez que passar pelo dashboard, como precaução extra — sem urgência, não bloqueia nada.

49. **Nova frente de trabalho definida com o PO em 24/08/2026 — sequência de ajustes operacionais ANTES do incremento 8 (Notificações).** Ordem: (1) ocultar cidade do cadastro; (2) bug 46 (provisionamento de staff); (3) gestão de usuários no painel; (4) módulo de Configuração; (5) notificações. Um por vez, com autorização entre cada um.

    **Item (1) — cidade sai do cadastro de cliente. ✅ Implementado e testado com Chromium real contra a URL pública em 24/08/2026.** Detalhe completo da decisão de produto e do teste em `REGRAS_DE_NEGOCIO.md` §14.3 (não duplicado aqui). Resumo técnico: `cadastro-form.tsx` perde o campo/estado de cidade e a validação que o exigia; `cadastro/page.tsx` para de buscar `get_public_delivery_cities` (não tinha mais uso ali). Nenhuma migration — `customers.delivery_city_id` já era nullable (`005`) e `handle_new_user` (`033`) já tratava o campo ausente como `null` de propósito (`nullif` + `try/catch`). Checkout (`/checkout`, passo Entrega) e `/minha-conta` continuam pedindo/editando a cidade normalmente, sem nenhuma mudança neles. Teste: formulário sem campo/texto de cidade; cadastro real via UI confirmado no banco com `delivery_city_id = null`; checkout do mesmo cliente pedindo cidade normalmente (Continuar desabilitado até escolher, habilitado depois); `/minha-conta` confirmado intacto. Zero resíduo de teste.

    **Item (2) — bug 46 (provisionamento de staff), diagnóstico aprofundado com evidência — 24/08/2026. Causa raiz CONFIRMADA (não mais hipótese), nada corrigido ainda (pedido explícito do PO: diagnóstico antes do código).** Detalhe técnico completo, causa raiz e as opções de correção com prós/contras estão em §2 "Padrão de autenticação" mais abaixo (não duplicado aqui, pra manter esta entrada como índice). Resumo: a hipótese original (condição de corrida — `handle_new_user`, `AFTER INSERT` em `auth.users`, lendo `NEW.raw_app_meta_data` antes do GoTrue terminar de gravar o `app_metadata`) foi **confirmada com evidência direta**, não só reproduzida de novo. Teste decisivo: mesma chamada de `admin.createUser()`, com `user_metadata` (nome/whatsapp) **e** `app_metadata.role` juntos — o `customers` criado tem o nome/whatsapp corretos (a trigger viu `raw_user_meta_data` certinho), mas foi pra `customers`, não `profiles` (a trigger não viu `role` em `raw_app_meta_data`) — **a mesma chamada, os dois metadados tratados de formas diferentes pelo GoTrue**, prova de que `app_metadata` chega à linha por um caminho/momento distinto de `user_metadata`, depois que a trigger (que só roda uma vez, no INSERT) já decidiu. Sem acesso a SQL direto neste ambiente (só o client de dados via service role) — não foi possível instrumentar a trigger com log; a evidência é 100% comportamental/reprodutível, mas conclusiva pela semântica de trigger `AFTER INSERT` do Postgres (o `NEW` só reflete o que aquele INSERT específico gravou). Decisão de correção **ainda pendente com o PO** — aguardando escolha entre as opções levantadas.

---

## 0. Regra de processo (definition of done)

> **Este documento e `docs/REGRAS_DE_NEGOCIO.md` DEVEM ser atualizados a cada nova decisão de produto ou feature entregue, ANTES de considerar a tarefa concluída.** Uma feature só está "pronta" quando: (1) o código está commitado, (2) o build passa, e (3) estes dois documentos refletem a mudança. Isso vale tanto para o assistente quanto para qualquer humano trabalhando no projeto depois.

### Rotina de fim de feature

Ao concluir cada feature, **antes de considerá-la pronta**:

1. Código commitado e build passando.
2. `ESCOPO_PROJETO.md` e `REGRAS_DE_NEGOCIO.md` atualizados — **adicionar/complementar, nunca remover histórico**.
3. Commit dos docs (separado ou junto do commit de código).
4. **Toda feature com UI testada com Chromium real (headless) contra o ambiente**, não só `npm run build`/typecheck — ver padrão detalhado abaixo.

Se a sessão estiver longa, encerrar com os docs atualizados e commitados, para que uma sessão nova possa retomar lendo só os documentos.

### Convenção de comunicação com o consultor de PO/UX/Engenharia

**📐 Registrada em 01/08/2026.** Ao final de **toda** resposta do assistente, incluir um bloco delimitado por `=== PARA O PO/UX/ENGENHEIRO ===` e `=== FIM ===`, contendo **apenas** o que precisa de decisão de produto/arquitetura ou revisão de SQL/segurança (perguntas em aberto, SQL a revisar, decisões de design). **Não** incluir explicações longas, logs de build, nem detalhes operacionais nesse bloco. Se a tarefa foi puramente operacional, escrever apenas "Nada para o PO/UX/Engenheiro nesta [resposta/tarefa]". Este bloco é o que Sérgio repassa ao consultor — mantê-lo enxuto reduz o consumo de tokens da revisão.

### Padrão de teste: Chromium real antes de considerar pronto

**✅ Formalizado em 31/07/2026** — já vinha sendo seguido informalmente desde Características de categoria, o fix do bug de slug e a cascata de inativação/reativação (todas pegaram bug real assim, antes da revisão humana). Agora é regra obrigatória, não boa prática opcional.

- Toda feature que envolve UI (tela nova, formulário, listagem, fluxo do painel) é testada com **Chromium real em modo headless** contra o ambiente rodando, antes de ser considerada pronta — `npm run build` (compilação/typecheck) sozinho não é suficiente, ele não pega bug de comportamento em runtime.
- O teste cobre os **fluxos principais** da feature (ex.: criar, editar, listar) e os **casos de borda relevantes** (ex.: validação, mensagem de erro, estado vazio).
- Quando a feature grava no banco, o teste **confirma a persistência real** (reconsultando o dado após a ação, não só confiando no retorno visual da tela).
- O resultado do teste (o que foi testado, o que passou, qualquer ajuste feito por causa dele) é **reportado junto com a entrega da feature** — mesmo padrão de transparência já usado nas entregas anteriores ("Testado com Chromium real: ...").

**⚠️ Correção crítica, 01/08/2026:** "ambiente rodando" acima ficou ambíguo e foi mal interpretado numa sessão — o teste da Etapa 1 de Produtos rodou o Chromium contra `npm run dev` (`localhost:3000`), nunca pushado nem deployado. Resultado: a feature foi reportada como "testada e pronta", mas os commits nunca chegaram no `origin/main` nem na Vercel — usuário real (Sérgio) bateu 404 em produção. **Essa regra já existia antes**, registrada em `VISAO_CAUA.md` ("sem execução de `npm run dev`... homologação na Vercel"), mas não foi carregada pra este documento nem consultada antes de montar o teste. Regra reforçada e explícita a partir de agora: **"ambiente rodando" significa a URL pública após `git push` + deploy da Vercel concluído — nunca só `npm run dev` local.** `npm run dev`/`localhost` só serve para iteração rápida durante o desenvolvimento, nunca como validação final de "pronto". Antes de reportar qualquer feature como testada, confirmar: (1) commit pushado pro `origin/main` (`git log origin/main` bate com o local), (2) deploy da Vercel concluído (não só disparado), (3) só então testar contra a URL pública.

### Rastreabilidade de migrations ajustadas ou descartadas

Migration criada e commitada, mas **ainda não aplicada**, pode precisar mudar antes de ir pro banco (ex.: dependência de uma decisão posterior, como o isolamento por tenant — decisão #21). Quando isso acontecer: **nunca apagar o registro da decisão original em silêncio**. Documentar aqui (tabela de migrations em §6, e no ponto onde a feature é descrita): o que foi descartado/ajustado, por quê, e o que substituiu — mantendo visível o histórico de que a versão original existiu e a razão da mudança. O arquivo `.sql` em si pode ser removido/renomeado do `supabase/migrations/` (não faz sentido manter SQL morto versionado), mas a **decisão e o motivo continuam registrados nos docs**.

**Ao iniciar qualquer sessão nova**, o primeiro passo é ler `docs/ESCOPO_PROJETO.md` e `docs/REGRAS_DE_NEGOCIO.md` para recuperar o contexto.

### Aprendizado de processo: diagnosticar antes de tocar em dados existentes

Registrado em 31/07/2026, a partir da experiência de montar o teste de isolamento (`supabase/tests/isolamento_test.sql`): a primeira tentativa de script assumiu que o usuário de teste já teria um `profile` de staff, sem checar antes — só depois de o teste falhar (e de mais uma rodada de causa raiz sobre transação/commit) é que ficou claro que ele existia apenas como `customer`. **Regra adotada daqui pra frente**: antes de montar qualquer script ou feature que crie, mova ou modifique dados já existentes (usuários, registros de teste, dados de produção), rodar primeiro um diagnóstico somente-leitura (`SELECT` simples confirmando o estado atual) e só então desenhar a lógica de escrita em cima do que foi confirmado — não do que se assume que deveria estar lá. Reduz retrabalho e evita lógica condicional (tipo "se existir, faz X, senão faz Y") escrita às cegas, sem validar qual dos dois casos é o real antes de programar os dois.

### Registro de tempo por feature

| Feature | Início | Conclusão | Nº de sessões |
|---|---|---|---|
| Características de categoria | 30/07/2026 15:31 | 30/07/2026 17:12 | 1 |
| CRUD de Produtos — Etapa 1 (núcleo, incluindo isolamento por tenant como pré-requisito) | 30/07/2026 | 01/08/2026 | 3 |

---

## 1. Visão do produto

E-commerce para o **Criatório Capuã** (peixes ornamentais e animais exóticos), substituindo um processo hoje manual (tabela de preços por WhatsApp → cliente monta lista → conferência manual de estoque → pedido em planilha Excel).

O produto é **arquitetado como SaaS multi-tenant desde a primeira tabela**, mas roda hoje em modo **single-tenant** (um único tenant, `capua`, com `tenant_id` fixo). Isso significa: toda tabela de domínio já tem `tenant_id`, toda escrita passa por RLS que valida `is_staff()`/`is_admin()`, e não há atalhos client-side que hardcodem o tenant — colunas `tenant_id` recebem `DEFAULT public.current_tenant_id()` no banco (não o client que descobre/envia o tenant). Quando o segundo cliente chegar, a evolução para multi-tenant não deve exigir reescrever o modelo de dados, só resolver o tenant a partir do subdomínio (`src/lib/tenant.ts` já isola essa lógica, hoje retornando uma constante).

**Cliente piloto:** Criatório Capuã. **Desenvolvimento:** VLUMA Tecnologia.

### Estratégia de evolução para SaaS (roadmap arquitetural)

**📐 Decidida em 31/07/2026 — ver decisão #22 em §5.** Define a ordem macro do projeto daqui pra frente:

1. **Isolamento total por tenant, agora** — antes de seguir com qualquer feature nova (a começar por Produtos), fechar a base de RLS/RPCs pra garantir que o modelo multi-tenant já declarado (§1) funcione de verdade, não só na estrutura das tabelas. Ver decisão #21.
2. **Desenvolvimento do produto para o Cauã continua sobre essa base isolada** — Produtos, Vitrine, Carrinho/Checkout, Pedidos etc., seguindo o roadmap de §4.
3. **Cauã fechado (produto completo) → vai para produção como tenant único.** Primeiro cliente real, ambiente de produção, mas ainda um deploy essencialmente single-tenant em uso (mesmo com a arquitetura multi-tenant por baixo).
4. **Fase SaaS**: nesse ponto, **clona-se código + banco** para um novo projeto/ambiente SaaS. A partir da clonagem, **o projeto do Cauã congela** — só recebe correções de bug que façam sentido replicar pros dois lados (Cauã e SaaS), nenhuma feature nova. Todo o trabalho de SaaS de verdade (Super Admin VLUMA, cadastro automático de novo cliente/tenant, cobrança, resolução de tenant por subdomínio real, etc.) evolui **só no clone**, sem arriscar o ambiente de produção do cliente pagante.
5. **SaaS pronto** → decisão futura (não tomada agora): migrar o Cauã pra rodar dentro da plataforma SaaS unificada, ou mantê-lo standalone indefinidamente.

**Por quê clonar em vez de evoluir o mesmo ambiente direto pra SaaS:** o Cauã é cliente pagante rodando em produção nesse ponto — mudanças estruturais de SaaS (novo onboarding, billing, multi-tenant real) têm risco de regressão alto demais pra testar direto contra o ambiente de quem já depende do sistema no dia a dia. Clonar isola o risco.

#### Questões a resolver na Fase SaaS (decididas em 01/08/2026 — a construir no clone, não agora)

**📐 Registradas em 01/08/2026, sem implementação nenhuma ainda — fica para a Fase SaaS (item 4 acima), depois do Cauã congelar.** Três frentes fechadas como escopo da fase, para não perder o fio da meada quando essa fase começar:

1. **Onboarding de comerciantes**: fluxo self-service de cadastro de lojista — o comerciante se cadastra, cria a loja, o sistema provisiona o tenant automaticamente (linha em `tenants`, configuração inicial de `store_settings`, etc.), sem intervenção manual da VLUMA. Hoje `handle_new_user()` (migration `006`) hardcoda `tenant = 'capua'` no signup (ver nota já registrada acima, no início de §2) — na Fase SaaS isso vira dinâmico, resolvido pelo contexto do cadastro (qual loja o comerciante está criando), não mais uma constante fixa no código.
2. **Acesso por subdomínio nosso OU domínio próprio do cliente**: toda loja nova ganha um subdomínio padrão (ex.: `loja.vluma.com.br`) automaticamente no onboarding (item 1); lojistas mais avançados podem apontar um domínio próprio via CNAME (padrão de mercado — Shopify, Nuvemshop). Isso exige **resolução de tenant por host** (subdomínio ou domínio → `tenant_id`) no servidor, uma peça de infraestrutura nova — `src/lib/tenant.ts` já isola esse ponto de decisão no código (hoje retorna uma constante), mas a lógica real de resolver por host ainda não existe.
3. **Acesso do cliente-final da loja**: o consumidor final de cada loja acessa a vitrine pública através do subdomínio ou domínio daquela loja específica (uma das duas opções do item 2) — é a camada de storefront público, container onde o cliente-final efetivamente compra.
4. **Confirmação de e-mail por tenant — Site URL única por projeto Supabase.** Registrado em 20/08/2026, achado ao diagnosticar o Bug 2 (404 na confirmação de cadastro, ver §0 item 42). O Supabase Auth tem **uma única** "Site URL" (Authentication → URL Configuration) por projeto — hoje aponta pro host do Cauã (`https://ecommercecauahml.vluma.com.br`) e funciona porque o projeto é single-tenant. Quando a Fase SaaS trouxer múltiplos tenants em domínios/subdomínios diferentes (item 2 acima), essa configuração única deixa de fazer sentido — cada loja teria seu próprio domínio, mas o Supabase só permite uma Site URL padrão (a lista de "Redirect URLs" aceita múltiplas entradas/wildcards, então tecnicamente dá pra listar todos os domínios ali, mas o **e-mail de confirmação em si** — template, link — ainda parte de uma configuração central do projeto). Precisa de solução mais elaborada nessa fase: candidatos a investigar incluem customizar o link de confirmação por tenant via `emailRedirectTo` combinado com uma lista de Redirect URLs cobrindo todos os domínios ativos (com wildcard, se o Supabase suportar), ou mover a lógica de confirmação pra fora do fluxo padrão do Supabase (RPC própria + link customizado). Não resolvido agora — só registrado pra não ser esquecido quando a Fase SaaS começar.

**Vínculo com a Vitrine (não é só Fase SaaS — chega antes):** a resolução de tenant por host do item 2 **não é exclusiva da Fase SaaS** — já é pré-requisito da fase da **Vitrine** (catálogo público, ver §4 "Planejadas"), bem antes do Cauã congelar. Essa é exatamente a "Opção C" já registrada em §2 "Padrão de multi-tenant nas escritas" (leitura pública via RPC/view parametrizada por tenant, resolvido no servidor a partir do subdomínio) — ou seja, parte da infraestrutura de resolução de tenant por host que a Fase SaaS precisa (item 2) já vai existir, em versão simplificada (só subdomínio nosso, sem domínio próprio de cliente ainda), desde a construção da Vitrine. A Fase SaaS estende essa mesma peça pra também aceitar domínio próprio do cliente (CNAME) e pro fluxo completo de onboarding (item 1).

---

## 2. Stack e arquitetura

### Stack

| Camada | Tecnologia |
|---|---|
| Framework | Next.js 16.2.11 (App Router, Turbopack) + TypeScript 5 |
| UI | React 19.2.4, Tailwind CSS v4, shadcn/ui (preset **Base UI**, não Radix) |
| Ícones/fonte | Lucide React; Geist (corpo) + Syne (`font-display`, títulos) |
| Estado de servidor | TanStack Query v5 |
| Formulários | React Hook Form + Zod (`@hookform/resolvers`) — **sem** o componente `Form` do shadcn, que é acoplado a Radix/Slot e incompatível com o preset Base UI deste projeto; forms usam `register`/`Controller` direto sobre `Input`/`Select`/`Switch`/`Combobox` |
| Backend/dados | Supabase (Postgres + Auth + RLS), `@supabase/ssr` 0.12.3 |
| Drag-and-drop | `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` — Base UI não tem primitivo de sortable; dnd-kit é o padrão de mercado, acessível, sem dependência de Radix |
| Deploy | Vercel |

### Padrão de autenticação (importante, decisão custosa de descobrir)

A sessão Supabase é gravada em cookie **`httpOnly: true`** (proteção contra roubo de token via XSS). Consequência arquitetural que se propaga por todo o painel:

- **O client Supabase do navegador (`src/lib/supabase/client.ts`) nunca enxerga a sessão** (JS não lê cookie httpOnly). Qualquer chamada feita direto do browser roda como usuário **anônimo**.
- Por isso, **login e logout são Route Handlers** (`/api/auth/login`, `/sair`), não Server Actions nem client calls — só o servidor lê o cookie.
- Login usa o padrão **POST → 303 See Other** com o `Set-Cookie` anexado à **mesma resposta de redirect** (`NextResponse.redirect` construído *antes* do `signInWithPassword`, cookies gravados via `response.cookies.set` dentro do `setAll`). Anexar cookie a um `NextResponse.redirect` criado separadamente do `cookieStore` **não propaga o `Set-Cookie`** — bug real já caçado e corrigido.
- **Logout precisa ser POST, nunca GET.** O Next.js faz *prefetch* automático de `<Link>` visíveis na viewport; um `GET /sair` que efetua `signOut()` é derrubado pelo próprio prefetch do framework, matando a sessão sem o usuário clicar em nada. Bug real, também já caçado.
- **Toda tela do painel que precisa ler dados além do que a RLS libera para `anon`/leitura pública (ex.: categorias inativas, criar/editar registros) passa por Route Handlers em `/api/painel/<entidade>`**, que usam o client server-side (`src/lib/supabase/server.ts`, lê o cookie via `cookies()` do `next/headers`, autenticado de verdade). Os hooks TanStack Query do painel fazem `fetch()` contra essas rotas — **não** chamam `supabase.from(...)` direto do browser. Esse é o padrão fixado para todo CRUD administrativo daqui pra frente.
- `src/proxy.ts` (renomeado de `middleware.ts` na convenção do Next 16) é **somente leitura**: nunca grava cookie, só decide redirect (`/entrar` ↔ `/painel`) a partir da sessão já existente.

**⚠️ Pendência reportada em 01/08/2026, não confirmada por teste ainda — sessão expirando (~1h) sem renovação, impacto no Cauã em produção.** Achado ao reler o código (não reproduzido ao vivo nesta sessão — a suspeita inicial de expiração durante o teste de Produtos se mostrou ser outra causa, um bug no script de teste clicando no botão errado, não isso). O que o código mostra:
- `proxy.ts` tem `setAll()` **intencionalmente vazio** ("proxy nao renova nem grava cookies" — comentário no próprio arquivo).
- `src/lib/supabase/server.ts` tem um comentário que diz o oposto: "Server Component: ignorado, middleware cuida da renovação" — mas o middleware (`proxy.ts`), como acabamos de confirmar, **não cuida** de renovação nenhuma. Os dois comentários se contradizem.
- Combinado, isso sugere que **nenhuma camada do app renova o token proativamente** fora de um Route Handler específico que já esteja fazendo outra operação Supabase. Um staff que navega só por Server Components (sem disparar POST/PATCH) por mais tempo que o access token dura pode, em tese, cair sem aviso.
- **Não investigado**: se o refresh token do Supabase está configurado com rotação (nesse caso, tentativas de refresh "descartadas" — nunca persistidas de volta no cookie pelo `setAll` vazio — podem invalidar o refresh token depois de algumas tentativas, tornando o problema pior). Precisa checar a configuração de Auth do projeto Supabase e, idealmente, reproduzir o logout de verdade (deixar uma aba do painel aberta e parada por >1h, ou ajustar a duração do JWT em ambiente de teste pra acelerar) antes de decidir a correção.

**✅ Resolvido em 10/08/2026 — diagnóstico completo, causa raiz confirmada, corrigido e testado de verdade (não só por leitura de código). Ver item 18 em §0 e a tabela "Concluídas" em §4 pro relato completo**: confirmado no dashboard Supabase que rotação de refresh token está **ativa** (reuse interval 10s) — o pior cenário cogitado acima se confirmou. Achado adicional relendo `git log -- src/proxy.ts`: o `setAll` vazio não é a versão original, é o resultado de um incidente de debug real em 27-28/07/2026 (o padrão oficial da Supabase para middleware chegou a ser implementado e revertido de propósito, "não sobrescreve cookie de sessão", sem causa raiz documentada na época). `proxy.ts` corrigido pra persistir a renovação de novo, com as mesmas opções de cookie do resto do app e um log só quando o refresh acontece de verdade — testado ao vivo baixando o JWT expiry pra 60s no dashboard (temporário, revertido depois do teste) e confirmando 2 ciclos completos de expiração+renovação silenciosa sem derrubar a sessão.

**🔒 Regra de segurança fixada em 18/08/2026 (migration `033`) — criação de staff exige `app_metadata`, nunca `user_metadata`.** Gap crítico encontrado no diagnóstico do incremento 2 da Fase 2 (Contas de cliente, ver §0 item 36 pro relato completo): `handle_new_user()` decidia staff-vs-cliente lendo `role` de `raw_user_meta_data`, campo que qualquer `signUp()` público (`anon key`) preenche livremente — nada impedia alguém de virar staff mandando `role:'admin'` fora do formulário `/cadastro`. Corrigido lendo `role` de `raw_app_meta_data` em vez disso — campo que a Auth API do Supabase **nunca** deixa o próprio usuário gravar (nem `signUp()`, nem `updateUser()`), só a Admin API com `service role key`. **Regra daqui pra frente, vale pra qualquer sessão futura que precise criar staff**: usar a Admin API (`auth.admin.createUser()`) passando `role` em `app_metadata` — **nunca** em `user_metadata`/`options.data` do jeito que o `/cadastro` público usa. Passar `role` em `user_metadata` na Admin API também "funcionaria" tecnicamente pro caso descuidado (a função simplesmente ignora esse campo e cria `customers` em vez de `profiles`), mas silenciosamente — o efeito seria "criei um staff e na verdade virou cliente", confuso de diagnosticar se alguém tentar o caminho antigo por hábito. INSERT direto em `profiles` via SQL/service role continua funcionando sem nenhuma mudança (nunca passou pela trigger).

**🔴 Bug 46 (provisionamento de staff via `admin.createUser()` quebrado) — diagnóstico aprofundado e CAUSA RAIZ CONFIRMADA em 24/08/2026, ainda não corrigido (pedido explícito do PO: diagnóstico completo antes de qualquer código).** Achado original em 21/08/2026 (item 46 em §0): `admin.createUser({ app_metadata: { role: 'operador' } })` numa chamada só cria silenciosamente um `customers`, não um `profiles`, sem erro nenhum — reproduzido de novo nesta rodada, com evidência direta, não só a hipótese registrada antes.

*Diagnóstico instrumentado (sem acesso a SQL direto neste ambiente — só o client de dados via service role, sem forma de instrumentar a trigger com log; evidência é 100% comportamental/reprodutível):*

1. **Reprodução controlada, 3 caminhos**: `admin.createUser()` via SDK, o mesmo via chamada REST pura (sem SDK) e um `getUserById()` logo depois — os três confirmam: o `customers` é criado (nunca `profiles`), e o `app_metadata.role` **está presente e correto** quando relido pela Admin API imediatamente depois. Ou seja, o valor existe de verdade na linha — só não estava lá no instante em que a trigger rodou.
2. **Teste decisivo (o que faltava pra virar evidência, não só hipótese)**: mesma chamada de `createUser()`, passando `user_metadata` (nome/whatsapp) **e** `app_metadata.role` juntos. Resultado: o `customers` criado tem o **nome/whatsapp corretos** (a trigger viu `raw_user_meta_data` certinho, no mesmo INSERT) — mas foi pra `customers`, não `profiles` (a trigger não viu `role` em `raw_app_meta_data`, na mesma chamada, no mesmo INSERT). **A mesma chamada, os dois metadados tratados de formas diferentes pelo GoTrue** — prova de que `app_metadata` chega à linha por um caminho/momento distinto de `user_metadata`, depois que a trigger (que só roda uma vez, `AFTER INSERT`) já tomou sua decisão.
3. **Teste de timestamp inconclusivo, registrado por transparência**: tentei comparar `created_at`/`updated_at` do usuário criado com e sem `app_metadata`, esperando ver um delta maior no caso com `role` (evidência de uma segunda escrita). Os deltas ficaram pequenos e inconsistentes nos dois casos (4–29ms) — provavelmente contaminados por outras escritas que `email_confirm:true` também dispara. Essa técnica **não** serviu de evidência; descartada em favor do teste 2 acima.

*Por que isso já é causa raiz confirmada, não só inferência sobre o GoTrue*: a trigger é `after insert on auth.users for each row`, lendo `NEW.raw_app_meta_data` — pela própria semântica de trigger `AFTER INSERT` do Postgres, `NEW` reflete exatamente o que **aquele INSERT específico** gravou, nunca o que uma instrução separada grava depois (mesmo dentro da mesma transação). Combinado com o teste 2 (mesma chamada, `user_metadata` chega e `app_metadata` não), a única explicação estruturalmente possível é: **o INSERT que o GoTrue de fato executa em `auth.users` não inclui `app_metadata` mesclado no momento da criação da linha** — esse campo é aplicado por uma escrita separada e posterior (muito provavelmente um UPDATE, dado que a Auth API trata `app_metadata` como campo privilegiado/sensível, com um caminho de gravação mais controlado que `user_metadata`) — depois que a trigger já rodou e já decidiu. Não temos acesso ao código-fonte do GoTrue nem a logs de SQL deste ambiente pra literalmente ver as duas instruções acontecendo — a conclusão vem da semântica de trigger (não está em disputa) mais o comportamento 100% reproduzível, não de uma leitura direta do código do GoTrue.

*Opções de correção levantadas, decisão ainda pendente com o PO — nenhuma implementada ainda:*

- **(a) Trigger ler o role de outro lugar/momento** — **não parece ter solução por essa via**. Se `app_metadata` genuinamente não está na linha no instante do INSERT, uma trigger síncrona `AFTER INSERT` estruturalmente não pode "esperar" por uma escrita futura de uma requisição diferente — teria que ser um evento `AFTER UPDATE` (ver opção c) ou abandonar o trigger como mecanismo pra esse caso (ver opção b/d).
- **(b) Desacoplar a criação de staff do trigger de signup** — criar um caminho explícito e controlado (RPC/função `security definer`, restrita a `service_role`) que assume que o `auth.users` já existe (criado via Admin API **sem** `role` nenhum, o que já cai em `customers` de forma previsível e sem corrida) e faz a "promoção" pra staff como uma operação separada, explícita: `delete from customers` + `insert into profiles`, protegida por `service_role`, nunca exposta a `anon`/`authenticated`. **Prós**: elimina o problema de tempo por completo (não depende de nenhuma ordem de escrita do GoTrue, nem de instrumentar/adivinhar o comportamento dele); simples de auditar; reaproveita exatamente o padrão que já foi usado manualmente como workaround em sessões de teste anteriores, só que formalizado. **Contras**: staff passa a nascer em 2 passos em vez de 1 (mais um lugar pra lembrar de chamar); precisa lidar com a guarda cruzada da migration `035` (que já impede a mesma linha existir nas duas tabelas — favorável aqui, já que o `delete` roda antes do `insert`).
- **(c) Trigger adicional `AFTER UPDATE on auth.users`, promovendo quando `app_metadata` mudar** — detectaria a escrita tardia do `role` e faria a promoção nesse momento (deletar de `customers`, inserir em `profiles`). **Prós**: mantém tudo automático, um único `admin.createUser()` continua "funcionando" do ponto de vista de quem chama. **Contras**: mais complexo e mais frágil — precisa distinguir "essa UPDATE foi a gravação do app_metadata do signup" de qualquer outra UPDATE futura em `auth.users` que mexa em `app_metadata` por outro motivo (ex.: alguém editando outro campo do app_metadata de um cliente já existente, sem querer promovê-lo); depende implicitamente de um comportamento do GoTrue que não é documentado publicamente (pode mudar numa atualização futura do Supabase sem aviso, quebrando essa trigger silenciosamente).
- **(d) Variante mais limpa da (b): parar de tentar fazer `role` funcionar no `createUser()` de jeito nenhum** — tratar provisionamento de staff como um fluxo administrativo de 2 passos SEMPRE (nunca uma chamada única "mágica"): passo 1, criar o usuário auth sem `role` (vira `customers` previsivelmente); passo 2, uma função/RPC de "promover a staff" explícita, chamada depois, só por `service_role`. Essencialmente a mesma ideia da (b), mas comunicada/documentada como o processo oficial desde o início (nunca a promessa de "uma chamada só resolve"), evitando que uma sessão futura tente de novo o caminho de uma chamada só e caia no mesmo bug.

Convergência preliminar (a decidir junto com o PO): **(b)/(d) parecem estruturalmente mais seguras que (c)** — não dependem de comportamento não-documentado do GoTrue nem de distinguir motivos de UPDATE, e reaproveitam um padrão já usado manualmente. (a) não é viável dado o que a evidência mostra. Continua valendo o requisito de segurança da migration `033`: signup público (`anon key`) tem que continuar **estruturalmente incapaz** de virar staff, em qualquer opção escolhida — nenhuma das opções levantadas expõe a etapa de "promoção" fora de `service_role`.

### Padrão de multi-tenant nas escritas

Colunas `tenant_id` ganham `DEFAULT public.current_tenant_id()` (função SQL que resolve o tenant do usuário autenticado via `profiles`/`customers`). Assim nenhum Route Handler precisa descobrir/hardcodar o tenant no `insert()` — já feito para `delivery_cities` (migration `008`), `categories` (migration `010`) e `category_attributes` (migration `012`); qualquer tabela nova gerida pelo painel deve repetir o padrão. `products` e `product_variants` tinham esse mesmo gap desde as migrations `003`/`009` (nunca corrigido, só notado ao planejar o CRUD de Produtos) — corrigido na migration `015` (renumerada de `013` — ver §6).

**⚠️ Gap de isolamento multi-tenant identificado em 30/07/2026:** `is_admin()`/`is_staff()` (`002_core.sql`) são checagens **globais**, não filtram por `tenant_id`. Nenhuma policy de RLS de escrita staff hoje (`categories_staff_write`, `products_staff_write`, `product_variants_staff_write`, `category_attributes_staff_write`, etc.) restringe por tenant — só exige `is_staff()`. Hoje é inofensivo porque existe um único tenant (`capua`); mas antes de um segundo tenant entrar, **todas as policies `*_staff_write` precisam ganhar `and tenant_id = current_tenant_id()`** (ou equivalente), senão staff de um tenant enxergaria/escreveria dados de outro via PostgREST direto. As RPCs `gerar_codigo_produto` e `criar_produto_com_variacoes` (migration `016`) já fazem essa checagem explicitamente no corpo da função (obrigatório ali por serem `SECURITY DEFINER`, que ignora RLS) — mas isso não substitui o conserto das policies em si.

**Revisado em 31/07/2026 — corrigido em `013_rls_tenant_isolation.sql` (criada e commitada, aguardando aplicação):** todas as policies de leitura privilegiada/escrita staff listadas acima, mais `tenants_admin_all`, `profiles_*`, `customers_*`, `settings_admin_write`, `pfv_staff_write`, `images_staff_write`, `price_history_staff_select`, `cities_*`, ganharam `and tenant_id = current_tenant_id()`. Também corrige a RPC `set_category_ativo_cascade` (migration `011`, **já aplicada em produção**), que tinha o mesmo gap (aceitava `category_id` de qualquer tenant). Detalhe completo em §6.

**Leitura pública (anon) fica de fora, de propósito — vínculo com a futura Vitrine:** RLS não consegue restringir por tenant um visitante anônimo (sem sessão, `current_tenant_id()` não resolve nada pra ele). Isso vale em especial para `store_settings` (`settings_select_public` usa `using (true)`, hoje devolve a configuração de **todos** os tenants pra qualquer `anon`) — fica assim deliberadamente até a Vitrine existir. **Quando a Vitrine for construída, toda query pública que ler `store_settings`, `categories`, `products` etc. precisa filtrar `tenant_id` explicitamente na query da aplicação** (via `src/lib/tenant.ts`, resolvendo o tenant pelo subdomínio) — a mesma nota já registrada como requisito futuro na decisão #9 (visibilidade de categoria inativa na vitrine) se estende a `store_settings` e a toda leitura pública.

**⚠️→✅ Vazamento REAL confirmado por teste em 31/07/2026, corrigido em `014_rls_select_anon_authenticated_split.sql` (✅ aplicada e validada em 31/07/2026 — ver resultado do reteste logo abaixo):** ao testar o isolamento com o tenant sintético (`supabase/tests/isolamento_test.sql`, depois da `013` aplicada), o staff do tenant de teste enxergava as categorias **ativas** do Cauã (vazamento real, não hipotético). Causa raiz: as policies `*_select_public` da `013` valiam para `anon` **e** `authenticated` com a mesma condição `ativo = true or (is_staff() and tenant_id = current_tenant_id())` — como RLS faz OR entre policies aplicáveis, a branch `ativo = true` (sem tenant nenhum) sozinha já bastava pra qualquer sessão `authenticated` (staff **ou cliente final logado**, achado extra desta investigação) ver toda linha ativa de **qualquer** tenant. A branch de escrita (`*_staff_write`) nunca teve esse problema (só é consultada em INSERT/UPDATE/DELETE) — o vazamento era exclusivamente de leitura.

**Correção (Opção A, decidida em 31/07/2026):** a policy de SELECT de cada tabela afetada (`categories`, `category_attributes`, `products`, `product_variants`, `delivery_cities`, `store_settings`, `product_attribute_values`, `product_images`) foi separada em **duas policies distintas** por tabela — uma pra `anon` (mantém `ativo = true` / `using(true)` sem filtro de tenant, é a leitura pública da vitrine) e uma pra `authenticated` (sempre `tenant_id = current_tenant_id()`, sem nenhum escape hatch — nas tabelas com coluna `ativo`, mais `and (ativo = true or is_staff())` pra staff continuar vendo inativo do próprio tenant no painel). Detalhe completo em §6.

**✅ Resultado do reteste, 31/07/2026 — isolamento provado, não só implementado:** depois de aplicar a `014`, o script `supabase/tests/isolamento_test.sql` foi rodado de novo com o mesmo tenant sintético (canário). Antes da `014`: staff do tenant de teste via `categorias_do_caua_que_vejo = 6` (todas as categorias ativas do Cauã, vazamento real). Depois da `014`: `categorias_do_caua_que_vejo = 0`, `categorias_visiveis = 1` (só a própria categoria do tenant de teste). **Dois vazamentos distintos foram identificados e corrigidos nesta investigação**: (1) **staff cross-tenant** — staff de um tenant lendo dados de outro (o achado original, que motivou o teste); (2) **cliente final logado cross-tenant** — um cliente autenticado (não-staff) também conseguia ler o catálogo ativo de outro tenant pela mesma policy tenant-blind, achado incidental durante a mesma investigação. Ambos fechados pela mesma migration `014` (a condição `tenant_id = current_tenant_id()` no topo da policy `authenticated` bloqueia os dois casos igualmente, já que nenhum dos dois tem `is_staff() = true` de outro tenant). Isolamento por tenant agora é **testado e comprovado**, não só desenhado — combinação das migrations `013` (escrita/leitura privilegiada) + `014` (leitura pública vs. autenticada).

**⚠️→✅ TERCEIRO vazamento diagnosticado e corrigido em 01/08/2026 (view, não policy) — migration `018_products_com_status_security_invoker.sql` aplicada e validada:** depois de fechar os dois vazamentos de tabela acima (`013`/`014`), ficou uma discrepância visível no painel de Produtos — a listagem (`/painel/produtos`, que consulta a view `products_com_status`) mostrava produtos do tenant sintético `_teste_isolamento` pra staff do Cauã, enquanto a tela de edição de produto (que lê `products` diretamente, sem passar pela view) isolava corretamente. Causa raiz: views do Postgres, por padrão, executam com os privilégios de quem as **criou**, não de quem as **consulta** — isso faz a view ignorar o RLS de `products`/`categories`/`product_variants` por completo, mesmo com as policies de `013`/`014` corretas e em vigor nas tabelas de base. Diagnóstico confirmado via `select relname, reloptions from pg_class where relname = 'products_com_status'` retornando `reloptions = null` (nunca teve `security_invoker` habilitado, nem na criação original na `009` nem na redefinição da `016`). Correção: `alter view ... set (security_invoker = true)`, fazendo a view herdar o RLS de quem consulta. Levantamento em toda a árvore de migrations confirma que `products_com_status` é a **única view** do projeto — não havia outras views/materialized views a corrigir na mesma migration. **✅ Aplicada pelo usuário em 01/08/2026** — `reloptions` reconferido no banco (`security_invoker=true`), e o reteste de isolamento com o canário `_teste_isolamento`, desta vez consultando a view diretamente (não só as tabelas), confirmou `de_outro_tenant=0`. Isolamento por tenant agora cobre tabela **e** view.

**📐 Aprendizado de processo registrado em 01/08/2026 — views sobre tabelas com RLS precisam de `security_invoker` explícito:** o Postgres não propaga RLS pra view automaticamente; o comportamento padrão (`security_invoker=false`, implícito) faz a view rodar com o privilégio de quem a criou, ignorando qualquer policy da tabela de base — isso não aparece em nenhum teste de policy isolada, só se manifesta na hora de consultar a view de verdade (foi exatamente assim que o vazamento da `products_com_status` passou despercebido desde a criação, migration `009`). **Regra adotada daqui pra frente**: toda `CREATE VIEW`/`CREATE OR REPLACE VIEW` nova sobre tabela protegida por RLS deve incluir `security_invoker = true` explicitamente na definição (`create view nome with (security_invoker = true) as ...`), no mesmo commit que cria a view — não como correção posterior. Mesma lógica já aplicada ao padrão `DEFAULT current_tenant_id()` em colunas novas (ver "Padrão de multi-tenant nas escritas" acima): a garantia de isolamento não pode depender de alguém lembrar de configurar depois.

**Opção C (fortalecida em 31/07/2026) — requisito obrigatório da fase Vitrine:** a nota acima ("app filtra `tenant_id` na query") deixa de ser suficiente sozinha como estratégia de longo prazo — depender de toda query pública lembrar de filtrar é exatamente a classe de risco que a Opção A acabou de eliminar pro lado `authenticated`. **Quando a Vitrine for desenhada, a leitura pública (`anon`) deve passar por RPC ou view parametrizada por tenant** (ex.: `get_public_categories(p_tenant_slug text)`), com o tenant resolvido no servidor a partir do subdomínio (`src/lib/tenant.ts`) e filtrado **dentro** da função/view — não confiar em cada Route Handler lembrar de acrescentar `.eq('tenant_id', ...)` manualmente. Isso centraliza a garantia de isolamento num único lugar revisável, em vez de espalhar a responsabilidade por N call sites futuros. Este é um requisito de design a resolver **no início do trabalho da Vitrine**, não depois.

**`handle_new_user()` (migration `006`) fica fora do escopo desta correção, de propósito:** hardcoda `tenant = 'capua'` no signup — não é vazamento (não expõe dado de outro tenant), é uma premissa single-tenant deliberada do MVP. Vira tema da **Fase SaaS** (decisão #22: resolver o tenant pelo contexto do cadastro/subdomínio no onboarding de um novo cliente), não desta migration de isolamento.

### Tenant sintético de teste (canário de isolamento)

**📐 Decidido em 31/07/2026 — ✅ criado e validado em uso na mesma data.** Serve pra validar de verdade o isolamento multi-tenant enquanto só existe o Cauã em DEV, e já cumpriu isso: foi o canário que **encontrou o vazamento real** documentado logo acima (leitura cross-tenant via a branch pública das policies de SELECT).

- **Nunca faz parte da cadeia de `supabase/migrations/`** — não é schema, é dado de teste. Criado via script `supabase/tests/isolamento_test.sql` (linha em `tenants` + linha em `profiles` associada a um usuário real do Supabase Auth criado manualmente pra este fim), fora de qualquer arquivo de migration versionado. Isso garante que ele **nunca é clonado pra produção nem pro clone da Fase SaaS** (decisão #22) — a cadeia de migrations é exatamente o que se replica nesses dois casos.
- **Identificação inequívoca**: `slug = '_teste_isolamento'`, `nome` contendo explicitamente "TESTE" (ex.: "TESTE — Canário de Isolamento").
- **`tenants.ativo = false`**: mantém o tenant de teste fora de `tenants_select_public` (`using (ativo = true)`) e de qualquer futura listagem/métrica que já filtre por tenant ativo — sem precisar de coluna nova. Não afeta a autenticação/RLS do tenant em si (`is_staff()`/`current_tenant_id()` não checam `tenants.ativo`), então continua 100% funcional pra testar isolamento.
- **Mantido permanentemente** como canário de regressão (não descartado após o primeiro teste) — decisão explícita: reaproveitar pra qualquer mudança futura de RLS/multi-tenant.
- **✅ Confirmado em uso, 31/07/2026**: o mesmo canário (usuário `8c0c4252-a38e-406f-94f7-a6f2aa3b7dcb` no tenant `_teste_isolamento`) foi reaproveitado pra retestar depois da migration `014` — sem precisar criar nada novo — e confirmou o fechamento do vazamento (`categorias_do_caua_que_vejo` caiu de 6 pra 0). Continua de pé como canário permanente pra qualquer mudança futura de RLS/multi-tenant.
- **Regra geral daqui pra frente**: qualquer tela/consulta futura que liste ou conte dados entre tenants (dashboards, métricas, Super Admin VLUMA) deve excluir tenants inativos/de teste — já é o comportamento padrão de tudo que filtra por `ativo = true`, só reforçando aqui que o canário depende disso pra nunca aparecer nas telas do Cauã.
- **⚠️ Característica conhecida, registrada em 18/08/2026 — o usuário do canário (`8c0c4252-…`) está em `profiles` E `customers` ao mesmo tempo.** Achado no diagnóstico prévio à migration `035` (guarda contra conta dupla staff+cliente, ver item 37 abaixo): o canário acumula uma linha de staff (`profiles`, `role='admin'`, "TESTE - Staff Canário") e uma linha de cliente (`customers`, `email='teste-isolamento@vluma.local'`) simultaneamente — provavelmente resíduo de como o script de teste original foi montado. **Não é bug, não afeta o Cauã, não bloqueia nada** — a `035` é `BEFORE INSERT`, então não mexe em dado já existente, só passa a impedir a mesma sobreposição em contas novas dali pra frente. Decisão do usuário: deixar como está por enquanto (sem urgência), avaliar limpeza numa rodada futura de reorganização de dados de teste — registrado aqui só pra ninguém confundir isso com um bug novo ao encontrar essa sobreposição depois.

### Padrão de mensagens de erro (voltadas ao usuário)

**✅ Em vigor a partir de 30/07/2026** — aplicar em toda funcionalidade nova; retroaplicar às existentes quando conveniente. Regra completa em `REGRAS_DE_NEGOCIO.md` §9: português claro, orienta a próxima ação, zero jargão técnico (nada de código de erro Postgres, nome de constraint/tabela/coluna, "RPC", stack trace). Detalhe técnico completo continua existindo — só que **apenas em log de servidor/console**, nunca na tela do usuário.

Catálogo de mensagens da feature **Código do Produto** (migration `016`, renumerada de `014`), como referência de implementação para os Route Handlers de `/api/painel/produtos` (ainda não escritos — Etapa 1 em andamento):

| Situação técnica | Mensagem ao usuário |
|---|---|
| Categoria sem `prefixo_codigo` | "Esta categoria ainda não tem um prefixo de código. Abra o cadastro da categoria e salve para gerá-lo." |
| `category_id` inexistente ou de outro tenant | "Categoria não encontrada." |
| Nenhuma variação enviada na criação | "Adicione pelo menos uma variação para o produto." |
| Colisão `unique(tenant_id, codigo)` (código manual) | "Já existe um produto com este código. Escolha outro." |
| Colisão `unique(tenant_id, slug)` | "Já existe um produto com esse nome. Ajuste o nome." |
| Colisão `unique(tenant_id, sku)` em variação | "Já existe uma variação com este SKU. Ajuste o SKU." |
| `chk_variant_preco_promo` (promo ≥ preço) | "O preço promocional deve ser menor que o preço normal." |
| `chk_variant_preco`/`chk_variant_estoque`/`chk_variant_qtd_min` | "O preço/estoque não pode ser negativo." / "A quantidade mínima deve ser pelo menos 1." |
| Colisão `unique(tenant_id, prefixo_codigo)` (prefixo manual na categoria) | "Já existe uma categoria com este prefixo. Escolha outro." |
| Tentativa de alterar `codigo` após criado | "O código do produto não pode ser alterado depois de criado." |
| Erro genérico/não mapeado | "Não foi possível salvar o produto. Tente novamente." |

As mensagens acima já estão implementadas nas RPCs `gerar_codigo_produto` e `criar_produto_com_variacoes` (via `raise exception` com texto amigável, capturando `unique_violation`/`check_violation` e traduzindo pelo nome da constraint). As linhas de colisão de slug/prefixo de categoria dependem dos Route Handlers ainda não escritos — devem seguir esta tabela literalmente quando implementados.

### Padrão de CRUD administrativo (fixado a partir de Cidades, replicado em Categorias)

- Listagem com filtro de **status** (Ativos/Inativos/Todos) e **busca por nome**, ambos sincronizados na **URL** via `useQueryParamState` (sobrevive a reload, compartilhável por link).
- Criação/edição sempre em **modal** (`Dialog`), nunca página separada — `FormDialog` genérico em `components/painel/crud/`.
- **Soft delete universal**: nenhuma entidade do painel sofre `DELETE` real. Existe um campo `ativo`; "excluir" = inativar. Confirmação (`ConfirmDialog`) só é pedida ao **inativar** (ação com efeito colateral); reativar é instantâneo (exceto quando reativar também tem efeito em cascata — ver Categorias).
- Toast (`sonner`) em toda mutation (criado/atualizado/inativado/reativado).
- Validação client-side (Zod) **e** server-side (Zod de novo, no Route Handler) — o client nunca é a única linha de defesa.
- Componentes genéricos reutilizáveis em `components/painel/crud/`: `StatusFilterTabs`, `SearchInput`, `StatusBadge`, `ConfirmDialog`, `FormDialog`. Primitivo `components/ui/combobox.tsx` (Base UI, select com busca) para seletores com listas grandes (ex.: categoria-pai).
- **`Sheet` (painel lateral) é o padrão quando abrir a gestão de uma sub-lista que por sua vez precisa de modais próprios** — evita aninhamento de `Dialog` dentro de `Dialog`. Usado em Características de categoria (`caracteristicas-sheet.tsx`): o Sheet lista/reordena, e o `FormDialog` de criar/editar característica abre *sobre* ele. Filtro/busca/URL-state não são obrigatórios nessa tela — aplicar onde o volume de dados justifica (lista curta por categoria não precisa).
- **📐 Planejado, não implementado:** toda ação de escrita (criar/editar/inativar/reativar) deve chamar um helper `registrarAuditoria()` a partir do Route Handler, gravando em `audit_log` — ver "Auditoria" logo abaixo e decisão #15.
- **📐 Exceção registrada em 08/08/2026 — Produtos deixa de seguir "criação sempre em modal" para formulários grandes/multi-seção**: o CRUD de Produtos (página dedicada `/painel/produtos/novo` e `/painel/produtos/[id]`, não modal — já era assim desde a Etapa 1, por causa das seções de código/variações/características/imagens) ganhou um padrão novo de saída de criação: ao salvar um produto **novo**, a tela **não redireciona pra listagem** — troca a URL pra `/painel/produtos/[id]` e a mesma página vira modo edição, destravando a seção de imagens no lugar (que dependia do produto já existir). Ver detalhe completo do comportamento e do teste na tabela "Concluídas" (§4), linha "Fluxo de cadastro de produto — permanece na tela após salvar". Continua valendo pra todo o resto (Cidades, Categorias, Unidades de Venda): criação/edição em modal, sem essa mecânica — é específico de formulários em página própria com seções que dependem do registro já existir.

### Auditoria (planejada — 📐 decidida, não implementada)

Tabela genérica `audit_log`: `tenant_id`, `usuario_id`, `acao`, `entidade`, `entidade_id`, `dados_antes jsonb`, `dados_depois jsonb`, `timestamp`. **Registro feito na aplicação** (helper `registrarAuditoria()` chamado explicitamente por cada Route Handler de escrita), **não via trigger de banco** — a decisão foi deliberada: um trigger só vê o `UPDATE` cru na tabela, sem o contexto de negócio (ex.: "esta inativação foi uma cascata disparada a partir da categoria X", "por qual usuário/sessão"), que só a camada de aplicação tem no momento da ação. Isso amarra diretamente com a cascata de categorias (decisão #8): quando a inativação em massa acontecer, o audit_log é o que permite ao Super Admin reconstruir *por que* várias categorias mudaram de status de uma vez.

Propósito: rastreabilidade para o **Super Admin VLUMA** (não para o admin comum do tenant, que não tem acesso a isso) — ver §4 "Planejadas (não iniciadas)", item Super Admin VLUMA. Candidata a feature premium monetizável no SaaS.

---

## 3. Modelo de dados atual

> Tabelas conforme aplicadas até a migration `012` (todas confirmadas aplicadas no banco — ver seção 6).

### Núcleo (`002_core.sql`)

| Tabela | Representa |
|---|---|
| `tenants` | Uma loja/cliente do SaaS. Hoje uma linha (`capua`). |
| `profiles` | Equipe interna (`admin` \| `operador`), 1:1 com `auth.users`. `pode_aceitar_pedido` é permissão granular do operador. |
| `customers` | Cliente final da loja, 1:1 com `auth.users`. |
| `store_settings` | Configuração da loja por tenant (1 linha): `loja_aberta`, `mensagem_loja_fechada`, `pedidos_abertos`, `mensagem_pedidos_fechados`, `permite_autocadastro`, `valor_minimo_pedido`, `baixa_estoque_na_reserva`, `minutos_expiracao_reserva`. Os dois níveis de fechamento (`loja_aberta`/`pedidos_abertos`) foram adicionados na migration `007`. |

Funções: `set_updated_at()` (trigger genérico), `current_tenant_id()`, `is_admin()`, `is_staff()` — todas `security definer`, usadas em RLS e em `DEFAULT` de coluna.

### Catálogo v2 — árvore + variações (`003` recriada pela `009_catalog_v2.sql`)

| Tabela | Representa |
|---|---|
| `categories` | Árvore de categorias via `parent_id` (auto-FK, N níveis). `parent_id = null` → raiz. `slug` único por tenant (namespace global, não por parent — decisão de produto). `inativado_em_cascata` (migration `011`) marca linhas derrubadas por cascata a partir de um ancestral — ver decisão #8 e RPC `set_category_ativo_cascade`. |
| `category_attributes` | Ficha técnica / filtros configuráveis por categoria — "Características" na interface (substituiu `subcategory_fields`). `tipo`: `texto \| numero \| selecao \| booleano \| data` (enum `field_type`; valor `'lista'` renomeado para `'selecao'` na 009; UI só expõe os 4 primeiros). `opcoes` (jsonb, array de strings) quando `tipo=selecao`. `chave` gerada uma única vez na criação (nunca muda depois, mesmo editando `rotulo`) — identificador interno estável. **CRUD completo em `/painel/categorias` → botão "Características" (Sheet lateral)**, com reordenação por drag-and-drop (`@dnd-kit`). |
| `products` | Produto de vitrine: `nome`, `slug`, `descricao`, `category_id`, `unidade_venda`, `destaque` (curadoria manual), `ativo`. **Sem preço nem estoque** — isso vive na variação. |
| `product_variants` | SKU do produto (substituiu os campos de preço/estoque que existiam direto em `products`). `nome` (rótulo, ex. "1kg"), `sku`, `preco`, `preco_promocional` (nullable, `check` garante `< preco`), `modo_estoque` (`quantitativo \| disponibilidade`), `saldo_estoque`, `quantidade_minima`, `ativo`. Produto simples = 1 variant "Padrão"; produto com tamanhos = N variants. |
| `product_attribute_values` | Valor de um `category_attributes` para um produto específico (substituiu `product_field_values`). |
| `product_images` | Fotos do produto (ligadas a `product_id`, compartilhadas entre variações — per-variant image é extensão futura). |
| `product_price_history` | Histórico de preço, agora por **variação** (`variant_id`, com `product_id` denormalizado pra listar o histórico do produto inteiro sem join). Trigger `log_variant_price_change()` dispara em `INSERT/UPDATE OF preco ON product_variants`. |

**Status derivado, não armazenado** (view `products_com_status`, criada na `009`): `esgotado` (soma de `saldo_estoque` das variações ativas = 0), `em_promocao` (existe variação ativa com `preco_promocional < preco`), `novidade` (`created_at` recente), `preco_a_partir_de` (menor preço entre variações ativas). Não existe coluna de "cor"/flag manual de status — é sempre calculado em query.

`subcategories` e `subcategory_fields` **foram removidas** (dropadas na `009`, sem necessidade de migrar dados — ambiente de dev sem catálogo real).

### Codificação de produtos — Código vs SKU (planejada — 📐 decidida, não implementada)

**⏸️→✅ Em espera desde 31/07/2026, liberada em 31/07/2026:** migrations `015`/`016` (renumeradas de `013`/`014` — ver §6) foram criadas e commitadas depois de resolver primeiro o isolamento total por tenant (`013_rls_tenant_isolation.sql` + `014_rls_select_anon_authenticated_split.sql`, decisão #21) — nessa ordem, como planejado. **Ambas aplicadas e validadas em 31/07/2026** (colunas/tabela/RPCs conferidas presentes no banco, `src/types/database.ts` atualizado, `npm run build` passou). O modelo abaixo já existe no banco.

Decisão de produto fechada em 30/07/2026 (ver decisão #18 em §5), registrada antes de iniciar o CRUD de Produtos. Formaliza dois identificadores distintos:

- **Código** — por *produto*: identificação/referência, pensado inclusive para lojistas migrando de outro sistema que já têm códigos próprios. Prefixo vem sempre da **categoria** do produto, nunca de um prefixo genérico de loja. Visibilidade na vitrine é configurável (toggle) — quando visível, aparece na ficha do produto e é buscável pelo cliente.
- **SKU** (`product_variants.sku`, já modelado na `009`) — por *variação*: controle de estoque, uso interno/discreto, sem seletor automático/manual.

Regras do Código:

1. Prefixo definido no cadastro da categoria (`categories.prefixo_codigo`, novo campo, opcional). Vazio → derivado automaticamente do nome (ex. "Ciclídeos" → `CIC`), mostrado pré-preenchido no formulário da categoria para o lojista ajustar se quiser (transparência, não é caixa-preta).
2. Prefixo único por tenant; colisão na derivação automática exige ajuste do sistema ou confirmação do lojista.
3. Formato `PREFIXO-NNNN` — sequência numérica com zeros à esquerda, **contada por categoria** (cada categoria começa do zero: `CIC-0001`, `CIC-0002`; `RAC-0001`...).
4. Gerado na criação do produto; **imutável depois** — mesmo que o produto mude de categoria, o código original permanece (preserva referência em pedidos já feitos e no histórico do cliente).
5. Lojista escolhe automático (regra acima) ou manual (campo editável, pré-preenchido com a sugestão automática) no cadastro do produto.

Modelagem afetada (nenhuma migration criada ainda):

- `categories.prefixo_codigo` (opcional; derivado do nome se vazio).
- `products.codigo` (imutável) + `products.codigo_visivel` (boolean, toggle de vitrine).
- Mecanismo de sequência por categoria (contador garantindo unicidade do código).
- SKU em `product_variants` permanece automático-editável, sem seletor (distinção reforçada acima).

### Entrega (`005_delivery_cities.sql`)

| Tabela | Representa |
|---|---|
| `delivery_cities` | Cidade de entrega: `nome`, `uf`, `ponto_entrega`, `horario`, `observacoes`, `ordem`, `ativo`. `customers.delivery_city_id` referencia esta tabela. Primeiro CRUD do painel, define o padrão de gestão administrativa. |

### Infra

| Tabela | Representa |
|---|---|
| `keepalive_ping` | Singleton (padrão VLUMA v2.1) pingado por GitHub Action a cada 8h pra manter o projeto Supabase free tier ativo. |

### Provisionamento de usuário (`006_handle_new_user.sql`)

Trigger `handle_new_user()` em `auth.users`: lê `raw_user_meta_data.role` no signup e cria a linha correspondente em `profiles` (staff) ou `customers` (cliente final) automaticamente.

**⚠️→✅ Atualizado pela migration `033` (18/08/2026) — não lê mais `role` de `raw_user_meta_data`.** Gap de segurança crítico: esse campo é gravável por qualquer `signUp()` público (`anon key`), permitindo virar staff só mandando `role:'admin'` no metadata. A partir da `033`, `role` vem de `raw_app_meta_data` — só gravável via Admin API/service role, nunca pelo próprio usuário. Ver §2 "Padrão de autenticação" e §0 item 36 pro relato completo (diagnóstico, prova de blindagem testada, e a regra fixada pra qualquer criação de staff futura).

---

## 4. Fases

### Concluídas

| Fase | Escopo | Observação |
|---|---|---|
| Setup | Next 16, Tailwind v4, shadcn (Base UI), Supabase, keep-alive | — |
| Modelo de dados núcleo + catálogo v1 + RLS | `002`–`004` | Catálogo v1 (categories/subcategories 2 níveis fixos) depois substituído pela v2 |
| Autenticação | Login, cadastro, recuperação de senha, callback, logout, proteção de rotas via `proxy.ts` | Passou por vários ciclos de bug real (cookie httpOnly + redirect atômico + prefetch de logout) — ver seção 2 |
| Painel — acesso e shell | Layout protegido, sidebar, roteamento por papel (staff → `/painel`), logo/identidade da sidebar | — |
| CRUD de Cidades de entrega | `/painel/cidades` | Define o padrão de CRUD administrativo (URL state, modal, soft delete, Route Handlers) |
| Catálogo v2 (modelagem) | Migration `009`: árvore de categorias, `category_attributes`, `product_variants`, status derivado | Só modelagem — telas de Produtos ainda não existem |
| CRUD de Categorias em árvore | `/painel/categorias` | Árvore colapsável, busca com auto-expand preservando caminho até a raiz, combobox de categoria-pai com exclusão anti-ciclo (client **e** servidor) |
| Características de categoria | Botão "Características" na árvore abre `Sheet` lateral (`caracteristicas-sheet.tsx`) com lista reordenável (`@dnd-kit`) + `FormDialog` de criar/editar. Migration `012` (`category_attributes.tenant_id` ganha `DEFAULT`) | Nome, Tipo (texto/número/seleção/sim-não), Opções (quando seleção), Obrigatória, Usar como filtro, soft delete. `chave` auto-gerada e estável. Sem herança entre categorias nesta fase — modelo não impede adicionar depois (resolução via `parent_id` em tempo de consulta, ver `use-caracteristicas.ts`). Testado com Chromium real: criar, editar (opções pré-carregadas), inativar/reativar, drag-and-drop com persistência confirmada no banco |
| Fix do bug de slug trocado no modal de editar Categoria | `categoria-form-dialog.tsx` | Causa raiz real: `useState`/`defaultValues` estáticos do `react-hook-form` dependiam de um `reset()` em `useEffect` que corria contra o efeito de auto-slug. Corrigido inicializando os dois direto de `categoria` (lazy init + `defaultValues` computados) **e** `key={categoria?.id}` no ponto de renderização. Testado com Chromium real: 4 edições seguidas em categorias diferentes, slug correto em todas |
| Cascata de inativação/reativação de Categorias | Migration `011` (`inativado_em_cascata` + RPC `set_category_ativo_cascade`, atômica, com checagem `is_staff()` própria) + `ConfirmDialog` de inativar (conta descendentes ativos) e de reativar (conta descendentes cascateados, só aparece se > 0) | Testado com Chromium real contra o banco: filha já inativa por decisão própria não é tocada por nenhum dos dois sentidos; filha ativa é cascateada e depois restaurada corretamente |
| Fix da colisão de prefixo em nomes compostos de Categoria | `categoria-form-dialog.tsx`, `route.ts` (POST/PATCH de `/api/painel/categorias`) | Causa raiz real: o formulário sempre pré-preenchia o campo prefixo com a derivação automática antes do submit, então o servidor tratava qualquer save como "prefixo digitado manualmente" e nunca tentava o auto-sufixo em caso de colisão — categorias com nomes parecidos (ex.: categoria-mãe "Betas" e subcategoria "Beta Azul", ambas derivando pra `BET`) ficavam com `prefixo_codigo = null` para sempre: qualquer save (mesmo sem mudar nada) falhava com "Já existe uma categoria com este prefixo", travando a linha inteira (não só o prefixo — nenhum outro campo da categoria conseguia ser salvo junto). Esse era o mesmo bug relatado inicialmente como aparente duplicidade de "nome" ao editar sem mudar nada — investigação com Chromium real mostrou que a mensagem exibida era sempre sobre prefixo, e a causa era a mesma em ambos os casos. Corrigido: o formulário só envia o prefixo como "digitado" quando o usuário de fato editou o campo (`prefixoManual`); caso contrário manda vazio e deixa o servidor derivar e resolver a colisão sozinho. Sufixo de desempate mudou de incremento simples (`BET2`, `BET3`) para 4 dígitos colados (`BET0001`, `BET0002`...) — decisão do usuário em 01/08/2026. **Testado com Chromium real contra a URL pública, pós-deploy**: as categorias que estavam travadas (`prefixo_codigo = null` desde antes do fix) se auto-curaram na primeira edição depois do deploy, sem precisar de migration de dados — "Beta Azul" ganhou `BET0001`, "Beta Negro" ganhou `BET0002` (resolvido automaticamente contra o `BET0001` que "Beta Azul" acabara de reservar), "Ração de Superfície" ganhou `RAC0001`. Uma categoria a mais no mesmo estado (`Ração para Peixes de Fundo`, `prefixo_codigo = null`) não foi tocada no teste — deve se autocurar da mesma forma na próxima vez que alguém salvar seu cadastro, sem ação manual necessária. **Auditoria adicional em 02/08/2026** (reconfirmando a hipótese original do usuário — "a validação deveria excluir o próprio id"): varredura completa de Categorias, Cidades, Produtos e Unidades de Venda (Route Handlers + RPCs) não encontrou **nenhum** código de checagem manual de duplicidade em lugar nenhum das 4 entidades — toda unicidade é delegada a índices/constraints do Postgres, capturados via `unique_violation`/23505, e todo `UPDATE`/RPC de edição é sempre escopado por `where id = <id_do_próprio_registro>`. Um `UPDATE` de uma linha pro valor que ela já tinha não pode colidir com ela mesma por semântica pura de SQL — só colide se existir outra linha de verdade com o mesmo valor, que foi exatamente o caso do prefixo de categoria acima. Conclusão: a classe de bug "esqueceu de excluir o próprio id" **não existe** nesta base — o que parecia isso era sempre o mecanismo do prefixo |
| **Unidades de Venda (Melhoria 3)** | Cadastro `unidades_venda` por tenant (migration `019`) substitui o campo de texto livre do produto por seleção — `products.unidade_venda_id` (uuid, FK), Combobox no formulário de produto com auto-seleção de "Unidade" em produto novo, tela de gestão `/painel/unidades-venda` (padrão de Cidades: nome + ativo), Route Handlers `/api/painel/unidades-venda` + `[id]`. Seed inicial: Unidade, Dupla, Cardume, Kg, Litro, Pacote | **Testado com Chromium real contra a URL pública** em 01/08/2026: listagem confirmada com as 6 unidades do seed, criação e inativação de unidade de teste confirmadas (sem lixo em produção). Ver §0 itens 7–9 para o histórico do incidente de produção que motivou boa parte deste trabalho (migration `019` aplicada antes do frontend estar pronto) e seu fechamento |
| **Visualização read-only ao clicar na linha (Melhoria 4)** | Categorias e Produtos: clique na linha da listagem abre `CategoriaViewDialog`/`ProdutoViewDialog` (só leitura — categoria-pai/breadcrumb, slug, prefixo, descrição, status para categoria; código, categoria, unidade de venda, preço, tabela de variações para produto), com botão "Editar" que leva ao fluxo de edição de sempre (mesmo `CategoriaFormDialog`, mesma página `/painel/produtos/[id]`). Ações existentes (lápis, inativar/reativar, características, chevron de expandir) preservadas via `stopPropagation`. Linha ganha `tabIndex`/`onKeyDown` (Enter) para acessibilidade por teclado. Sem migration — mudança só de frontend | **Testado com Chromium real contra a URL pública** em 01/08/2026: clique na linha de categoria abre o modal com todos os campos esperados, "Editar" abre o form pré-preenchido corretamente; clique no lápis abre a edição direto, sem o modal de visualização por baixo (`stopPropagation` confirmado); mesmo roteiro em Produtos (modal com variações, "Editar" navega para a página de edição, lápis não dispara o modal) |
| **Módulo de Estoque — incremento 1 (listagem, movimentação, histórico)** | Migrations `021` (tabela `stock_movements` + RPC `registrar_movimentacao_estoque`, ledger com saldo assinado, `FOR UPDATE`, amarração tipo↔sinal via `check`, `revoke update(saldo_estoque)`) e `022` (RPCs de produto passam a gerar movimentação de inventário em vez de gravar saldo direto) aplicadas pelo usuário. Tela `/painel/estoque`: listagem de variações `quantitativo` de produtos **ativos** com status derivado (ok/abaixo do mínimo/esgotado), filtro de 4 posições + busca. Ação "Registrar movimentação" (Entrada/Saída/Ajuste/Devolução — Inventário reservado ao sistema) chama a RPC; Ajuste pede o novo saldo total (não o delta). Clique na linha abre histórico read-only (usuário nulo exibe "Sistema"). Route Handlers `/api/painel/estoque` (+`/movimentacao`, +`/[variantId]/historico`), hook `use-estoque.ts` | **Testado com Chromium real contra a URL pública** em 02/08/2026: criação de produto de teste, Entrada de estoque confirmada na listagem e no histórico (usuário real registrado, não "Sistema"), Ajuste sem motivo bloqueado com mensagem amigável, Ajuste com motivo aplicando o **saldo total** informado (não um delta), Saída maior que o saldo bloqueada com "Estoque insuficiente" sem travar a tela, filtro de 4 posições confirmado na URL. **1 bug real encontrado e corrigido durante o teste**: a listagem inicial filtrava só `product_variants.ativo`, não `products.ativo` — produto inativado (toggle rápido só mexe em `products.ativo`, nunca cascateia pra variação) continuava aparecendo na tela de dia a dia de Estoque; corrigido buscando produtos ativos primeiro. Ordenação também corrigida para `produto_nome, variacao_nome` (só por nome de variação colidia muito — "Padrão" se repete entre produtos). Produtos de teste limpos (inativados) ao final, listagem real fica vazia até o próximo produto ativo com estoque existir |
| **Módulo de Estoque — incremento 2 (campo "Estoque inicial" no formulário de produto) — ✅ módulo completo** | Campo do formulário de produto renomeado de "Estoque" (`saldo_estoque`, sempre editável) para "Estoque inicial" (`estoque_inicial`, opcional): editável em produto novo e em variação nova adicionada durante uma edição; em variação **já existente** na edição, vira exibição read-only do saldo atual, com nota "Estoque gerenciado em [Estoque](/painel/estoque)" linkando para o módulo — nunca mais editável por ali. Route Handlers de produto e schema do formulário atualizados para o payload `estoque_inicial`, batendo com o que a RPC (`022`) espera | **Testado com Chromium real contra a URL pública** em 02/08/2026, sem nenhum bug encontrado: produto novo criado com "Estoque inicial" = 15 → saldo 15 confirmado na listagem de Estoque e histórico mostrando `Inventário +15 (0→15)`, motivo "Estoque inicial no cadastro do produto", usuário real (não "Sistema"); edição do mesmo produto confirmou o campo da variação existente desabilitado, exibindo 15, com o link "Estoque" apontando pra `/painel/estoque`; variação nova ("Grande") adicionada durante a edição com "Estoque inicial" = 8 → saldo 8 confirmado na listagem e histórico com o mesmo padrão de movimentação `inventario`. Produto de teste limpo (inativado) ao final. **Com este incremento, o módulo de Estoque está completo**: modelagem (`021`/`022`), listagem/movimentação/histórico (incremento 1) e integração no cadastro/edição de produto (incremento 2) — todos aplicados e testados na URL pública |
| **Produtos Etapa 3 — Imagens: galeria do produto (incremento 1)** | Migration `023` aplicada pelo usuário: `product_images` (já existia desde a `003`, isolada por tenant desde `013`/`014`, nunca usada até agora) ganha `variant_id` (nullable — imagem de variação, extensão futura), `check` impedindo capa em imagem de variação, `DEFAULT current_tenant_id()` (gap corrigido), RPC `definir_imagem_principal` (troca de capa atômica), bucket `product-images` (público, 5MB, JPG/PNG/WebP) e 3 policies de `storage.objects` (insert/update/delete separadas — **sem** policy de select, correção de revisão do usuário: uma versão anterior com `for all` descasava leitura anon vs. authenticated, mesma classe de bug da migration `014`). Seção "Imagens do produto" no formulário de edição (só aparece com o produto já salvo — imagem depende de `product_id` existente): upload múltiplo com otimização client-side (canvas → WebP, ~1600px, qualidade 0.8, normaliza qualquer formato de entrada), preview instantâneo, reordenação por drag-and-drop (`@dnd-kit`, mesmo padrão de Características), definir capa, remoção com confirmação (**exclusão real, não soft delete** — exceção explícita registrada abaixo), descrição (`alt_text`) opcional por imagem. Limite de 10 imagens por produto validado no client e no Route Handler (trava dura) | **Testado com Chromium real contra a URL pública** em 03/08/2026: upload de 2 imagens via input de arquivo real confirmado (preview, contador, capa definida com toast de sucesso, `alt_text` salvo corretamente — tudo confirmado também via leitura direta da API depois). **Isolamento por tenant confirmado no path real**: todo `storage_path` gravado começa com `{tenant_id}/`. **URL pública da imagem testada com requisição HTTP direta e isolada (sem sessão nenhuma) → 200**, confirmando que o bucket serve leitura pública de verdade. **Limite de 10 testado até o fim**: 8 uploads extras via API direta (somados às 2 já existentes) todos aceitos até bater 10, o 9º extra corretamente recusado com 400 — nunca passou de 10. Remoção testada (exclusão real confirmada — contagem cai exatamente 1 a cada remoção). Todas as imagens de teste + o produto de teste limpos ao final. **Não testado nesta rodada**: o gesto visual de arrastar-e-soltar em si (a mecânica de reordenação usa o mesmo `@dnd-kit` já validado em Características, e a atribuição de `ordem` sequencial no upload foi confirmada correta — mas o drag real do mouse não foi exercitado por Chromium desta vez). **Mini-uploader por variação (limite 5, escopo `variant_id`) implementado no incremento 2 — ver linha abaixo, 03/08/2026** |
| **Produtos Etapa 3 — Imagens: mini-uploader por variação (incremento 2) — ✅ Etapa 3 completa** | Sem migration nova — a `023` (incremento 1) já suportava `variant_id` em `product_images`, contagem por escopo e o `check` que impede capa em imagem de variação. Mudança só de frontend: dentro de cada variação **já existente** no formulário de edição de produto, um bloco colapsado ("Fotos específicas desta variação — opcional", componente `Collapsible` novo em `components/ui/collapsible.tsx`, primeiro uso no projeto, sobre o primitivo `@base-ui/react/collapsible`) esconde o mesmo `ProdutoImagensSection` do incremento 1, agora com `variantId` preenchido e `maxImagens=5`. Variação nova (ainda sem `id` salvo) não mostra o uploader — mesma regra "salve primeiro" já usada na galeria do produto. `ProdutoVariacoesSection` passou a receber `produtoId`/`imagens` do form pai para poder filtrar `imagens.filter(img => img.variant_id === variacao.id)` por variação | **Testado com Chromium real contra a URL pública** em 03/08/2026, produto "TESTE imagens variacao incremento2" com 2 variações: upload confirmado escopado à variação certa (5 imagens subidas todas com o mesmo `variant_id`, a outra variação lida em `0/5 imagens` do início ao fim — isolamento por `variant_id` confirmado tanto na UI quanto direto no banco via leitura read-only); limite de 5 confirmado nos dois lados — botão de upload desabilitado no client ao atingir 5/5, **e** chamada direta à API (bypassando o client) devolveu `400` com a mensagem amigável "Limite de 5 imagens por variação atingido."; `storage_path` confirmado no mesmo formato do incremento 1 (`{tenant_id}/{product_id}/{imageId}.webp`, sem segmento de variação — a distinção é só a coluna `variant_id` no banco); leitura pública do Storage reconfirmada (200, sem sessão); remoção testada via UI (exclusão real confirmada — contagem caiu de 5 para 4 imediatamente). Zero erros de console. Produto de teste inativado e todas as imagens de teste removidas ao final, sem lixo em produção. **Com este incremento, Produtos Etapa 3 (Imagens) está 100% completa**: galeria do produto (incremento 1) + fotos por variação (incremento 2) |
| **CRUD de Produtos — Etapa 1 (núcleo)** | Pré-requisito: `prefixo_codigo` na tela de Categorias (auto-derivado, editável, colisão tratada). Listagem `/painel/produtos` (status/busca/categoria na URL, `products_com_status`). Cadastro `/painel/produtos/novo` em página dedicada (3 blocos: Dados básicos, Código com peek+seletor automático/manual, Variações com SKU automático) via RPC `criar_produto_com_variacoes` (migration 016). Edição `/painel/produtos/[id]` via RPC `atualizar_produto_com_variacoes` (migration 017) — código sempre read-only, variações sincronizadas por diff (soft delete das removidas). Migrations `015`–`017` aplicadas | **Testado com Chromium real** (canário `_teste_isolamento`) em 31/07–01/08/2026: categoria com prefixo, produto com código automático (peek + reserva real batendo), código manual, código manual duplicado (erro amigável confirmado), preço negativo, promo ≥ preço, impedir remover a última variação, SKU automático com colisão evitada (`TES-0001-PADR`, `TES-0001-GRAN`), edição com variação nova + variação removida (soft delete confirmado via `estoque_total` recalculado), inativar/reativar. **3 bugs reais encontrados e corrigidos durante o teste** (não hipotéticos — pegos ao vivo): (1) `preco_promocional` vazio virava `0` em vez de `null` por ordem errada num `z.union` (`Number('') === 0` em JS) — afetava todo produto criado sem promoção; (2) botões `Button` renderizados como `<Link>` via `render=` disparavam aviso de acessibilidade do Base UI, faltava `nativeButton={false}`; (3) `min="0"`/`min="1"` nos inputs de variação disparavam tooltip nativo do navegador em vez da mensagem estilizada do Zod — removidos para manter Zod como única fonte de validação, consistente com o resto do painel. `product_images` (Etapa 3, fotos) e `product_attribute_values` (Etapa 2, ficha técnica por categoria) **ainda não implementados** — fora do escopo desta etapa. **⚠️→✅ Correção, 01/08/2026: o teste acima rodou contra `localhost` (não pushado, não deployado) — commits só chegaram no `origin/main`/Vercel em 01/08/2026, depois que o usuário reportou 404 em produção.** **Re-teste contra a URL pública ✅ concluído em 02/08/2026**: teste dedicado (criar produto, `preco_promocional` vazio confirmado `null` não `0`, zero warnings de acessibilidade no console, validação Zod sem tooltip nativo do navegador, adicionar/remover variação com soft delete confirmado, busca na listagem, inativar/reativar) somado a toda a atividade cumulativa do resto da sessão (Bug 1/2, Unidades de Venda, Melhoria 4, módulo de Estoque) que já vinha exercitando CRUD de produto na URL pública repetidamente. **Etapa 1 de Produtos está 100% fechada e confirmada em produção** — falta só Características (Etapa 2, `product_attribute_values`) e Imagens (Etapa 3, `product_images`), ambas fora do escopo desta etapa desde o início. |
| **Código do produto — modo automático por nome + busca por SKU/variação** | **Item 1 (decisão #25)**: 3º modo de geração de código — "Automático" (novo padrão) deriva o prefixo do **nome do produto** via `derivarPrefixo()` (decisão #24, reaproveitada sem mudança), sequência própria por prefixo em tabela nova `product_code_sequences` (migration `024`), RPC nova `gerar_codigo_produto_por_prefixo` (recebe o prefixo já derivado em TypeScript, só reserva o número — não duplica a regra de linguística em SQL). "Herdar da categoria" vira modo explícito separado, usando a RPC/sequência por categoria de sempre (`016`) sem nenhuma alteração. **Nenhuma mudança em `criar_produto_com_variacoes`/`atualizar_produto_com_variacoes`** — as duas já recebiam o código pronto como string. Peek (preview sem reservar) estendido para os dois modos automáticos, com debounce de 400ms no nome. **Item 2**: busca da listagem (`GET /api/painel/produtos`) passa a considerar também SKU e rótulo de variação (query server-side em `product_variants`, ids combinados no `.or()` principal via `id.in.(...)`, sem filtro client) — inclui variações inativas de propósito (staff pode estar procurando pelo SKU de uma variação já inativada). Filtro de categoria por Combobox com busca/hierarquia já existia de uma sessão anterior, não precisou de mudança | **Testado com Chromium real contra a URL pública em 05/08/2026**: **modo "Automático"** — "Ração de Fundo Pyotar" → peek e código final batendo `RAFP-0001`; segundo produto com nome diferente que deriva o mesmo prefixo ("Ração Filhotes Premium") → `RAFP-0002`, confirmando a sequência por prefixo (não por produto) funcionando; sequência reconferida direto no banco (`product_code_sequences`, `ultimo_numero=2`). **Modo "Herdar da categoria"**: peek e código final batendo `BET-0014`. **Modo "Manual"**: `MANUAL-9999` salvo corretamente. **Busca por SKU/variação**: busca por `"PADR"` (substring que só existe no SKU, não no nome/código de nenhum produto de teste) trouxe os produtos certos; busca pelo rótulo completo `"Padrão"` também; string inexistente mostrou o estado vazio. **Regressivo do módulo de Produtos**: edição preservando código imutável (confirmado 2x), variação nova com estoque inicial (movimentação `inventario` 0→30 confirmada no módulo de Estoque), upload de imagem na galeria do produto **e** numa variação (confirmado 2 blocos de uploader, imagens com `variant_id` correto no banco), filtros de status (Ativos/Todos), Combobox de categoria abrindo e filtrando pela URL, inativar → aparece em Inativos → reativar → volta a aparecer em Ativos. **11/11 checks automatizados passaram, zero erros de console.** Antes da migration `024` ser aplicada, testado também o caminho de erro: tentativa de criar produto em modo automático sem a RPC existir não trava a tela, mostra toast (mensagem técnica nesse cenário transitório, que deixa de ocorrer depois de aplicada — não precisou de tratamento extra). Todos os produtos e imagens de teste desta rodada limpos ao final (soft delete nos produtos, exclusão real nas imagens) |
| **Produtos Etapa 2 — Características por produto (ficha técnica) — ✅ módulo de Produtos 100% completo** | Migration `025` (`product_attribute_values.tenant_id` ganha `DEFAULT current_tenant_id()`; `criar_produto_com_variacoes`/`atualizar_produto_com_variacoes` ganham parâmetro novo `p_caracteristicas jsonb default '[]'::jsonb` — valida obrigatoriedade server-side contra as características **ativas** da categoria sendo salva, sincroniza os valores: troca de categoria apaga os valores da categoria antiga — decisão de produto, sem órfão invisível —, característica inativada na mesma categoria preserva o valor) + migration `026` (corrige overload: `create or replace` com assinatura nova criou função **nova** ao lado da antiga em vez de substituir — `pg_proc` chegou a ter 2 versões de cada RPC convivendo; `026` dropa as antigas). Diff automatizado contra a versão anterior (`022`) confirmou que as 9 camadas existentes das duas RPCs (tenant, unidade de venda, estoque inicial, SKU automático, código imutável, soft delete de variações, garantia de ≥1 variação ativa, mensagens amigáveis) não foram alteradas, só inserções. Frontend: bloco "Características" no formulário de produto (entre Variações e Imagens), reativo à categoria selecionada (`useCaracteristicas` já existente, reaproveitado), campos dinâmicos por tipo (texto→input, número→input numérico, seleção→`Combobox` com as opções, sim/não→`Switch`), some por completo se a categoria não tem nenhuma característica ativa configurada. Obrigatoriedade validada no client (fonte única: `ProdutoForm` busca a lista de características ativas **uma vez** e passa pronta via prop pro `ProdutoCaracteristicasSection`, usada tanto pra renderizar quanto pra montar o payload/validar no `onSubmit` via `methods.setError`) e no servidor (RPC, fonte da verdade) | **Testado com Chromium real contra a URL pública em 06/08/2026**, categoria de teste com 4 tipos de característica (texto obrigatória, número opcional, seleção obrigatória, sim/não opcional) + segunda categoria com 1 característica obrigatória distinta: bloco "Características" ausente em categoria sem características configuradas; bloco presente com os 4 controles certos por tipo; submit sem preencher obrigatórias bloqueado, com "Obrigatório." exatamente nos 2 campos certos; produto criado com os 4 valores corretos confirmados direto no banco; edição pré-carrega os valores existentes corretamente; troca de categoria na edição apagou os 4 valores da categoria antiga e exigiu preencher a obrigatória da nova (confirmado no banco: só sobrou o valor da nova categoria); característica inativada sumiu do formulário mas o valor sobreviveu no banco mesmo depois de salvar o produto de novo sem tocar nela, e voltou a aparecer com o valor certo ao reativar. **2 bugs reais encontrados e corrigidos durante o teste** (não hipotéticos — pegos ao vivo): (1) `ProdutoForm` e `ProdutoCaracteristicasSection` chamavam `useCaracteristicas` separadamente — mesma `queryKey`, cache compartilhado, mas sujeito a resolver em momentos levemente diferentes entre os dois componentes; o form chegou a submeter sem bloquear um campo obrigatório vazio mesmo com os campos já visíveis na tela, porque a lista usada pela validação (no pai) ainda estava vazia nesse instante — corrigido unificando numa fonte só (pai busca, passa pronta via prop); (2) campos `Controller` (seleção, sim/não) sem `defaultValue` explícito ficavam `undefined` no form state quando nunca tocados, violando o schema Zod (`z.record(z.string(), z.string())`) **antes** do `onSubmit` do pai rodar — `handleSubmit` nem chegava a chamar a validação de obrigatoriedade, e o erro mostrado era um `"Invalid input: expected string, received undefined"` cru do Zod, não a mensagem "Obrigatório." esperada — corrigido com `defaultValue=""` nos dois `Controller`. Produtos e categorias de teste desta rodada limpos ao final (soft delete). **Com este incremento, o módulo de Produtos (Etapas 1, 2 e 3) está 100% completo** |
| **Remoção do modo de estoque "disponibilidade" da UI** | **Bug real reportado pelo usuário em 06/08/2026**: produto criado com duas variações preenchendo "Estoque inicial" em cada uma, mas o estoque não aparecia registrado. Causa raiz: `registrar_estoque_inicial_variacao` (migration `022`) sempre ignorou `estoque_inicial` de propósito quando `modo_estoque = 'disponibilidade'` (esse modo não controla quantidade) — mas o formulário deixava o campo editável nesse modo sem nenhum aviso, então o lojista preenchia um número que era silenciosamente descartado ao salvar. **Correção em 2 passos**: (1) fix imediato — campo "Estoque inicial" passou a ficar desabilitado com nota explicativa quando a variação estava em modo "disponibilidade"; (2) **decisão de produto**: o negócio sempre trabalha com controle de quantidade, então o seletor "Modo de estoque" foi **removido por completo da tela** de cadastro/edição de produto — toda variação nova nasce `quantitativo` (sem opção de escolher outra coisa), eliminando a classe de bug inteira em vez de só avisar sobre ela. Enum/coluna `modo_estoque` **não foram removidos do banco** (evita migration destrutiva) — variações já existentes em `disponibilidade` mantêm o valor intacto (campo agora oculto no formulário, só pra não perder o dado ao salvar), sem conversão forçada | **Testado com Chromium real contra a URL pública em 06/08/2026**: confirmado visualmente que nenhum texto "Modo de estoque"/"disponibilidade" aparece mais em lugar nenhum do cadastro; produto criado com 2 variações (`Pequeno` estoque inicial 15, `Grande` estoque inicial 30) — **as duas com `modo_estoque = 'quantitativo'` e `saldo_estoque` corretos confirmados direto no banco**, com movimentação de inventário registrada certinha pras duas (`0→15`, `0→30`). Zero erros de console. **Levantamento do banco**: só 3 variações estavam em `disponibilidade` no tenant — as 2 do produto real do usuário ("Beta Red") e 1 de um produto de teste já inativo de uma sessão anterior; nenhuma outra. **Produto "Beta Red" (ativo, do usuário) não foi tocado** — só o produto de teste criado agora para validar foi inativado ao final |
| **Lote de ajustes de UX no formulário/listagem de Produtos — itens 2, 4 e 6 (feedback de uso real, 07/08/2026)** | **Item 2**: campos "Preço"/"Preço promocional" da variação viram `CurrencyInput` (`components/ui/currency-input.tsx`, novo componente reutilizável) — máscara de moeda Real desde a digitação (dígitos preenchem da direita pra esquerda, formato `R$ 1.234,56`), nunca mostra "0" pré-preenchido (só placeholder fantasma) nem zero à esquerda sobrando. `VARIACAO_PADRAO.preco` mudou de `0` pra vazio; schema Zod ganhou `refine` novo pra "Informe o preço." (antes a obrigatoriedade vinha do próprio `z.coerce.number()`, que não é mais suficiente já que o campo agora aceita vazio durante a digitação). Estoque inicial/Quantidade mínima não precisaram de mudança — `type="number"` nativo já evita zero à esquerda sozinho, e já começavam vazios/em 1. **Item 4**: coluna/campo "Preço a partir de" removido da listagem de produtos e do dialog de visualização — redundante com o preço por variação já visível em cada lugar (listagem de variações do próprio dialog, formulário de edição). **Item 6**: botão de salvar ganha barra fixa no rodapé (`position: fixed`, não `sticky` — o layout do painel não tem container de scroll próprio, a página inteira rola no document; `lg:left-64` desvia da largura da sidebar em telas grandes) — produtos com muitas variações deixavam o botão do topo fora de alcance sem rolar de volta | **Testado com Chromium real contra a URL pública em 07/08/2026**: confirmado que "Preço a partir de" não aparece mais na listagem; preço começa vazio num produto novo (`inputValue() === ''`, confirmado via atributo, não só visual); digitar `"12345"` formata pra `"R$ 123,45"`; digitar `"050"` não deixa zero sobrando (`"R$ 0,50"` limpo); botão "Criar produto" continua visível mesmo com a página rolada até o meio, em um produto com 5 variações (bem comprido). Zero erros de console. Nenhum produto de teste chegou a ser criado (teste todo client-side, sem submeter) — nada pra limpar |
| **Lote de ajustes de UX — itens 1 e 3 (dois mínimos + SKU por palavra) — migration `027`** | **Item 1**: `product_variants.quantidade_minima` (ambígua) renomeada pra `quantidade_minima_estoque` (nível de alerta de reposição, o que o módulo de Estoque já usava) + nova coluna `quantidade_minima_venda` (mínimo de compra do cliente, checkout futuro, default 1, sem uso ainda). Formulário de produto ganha os 2 campos rotulados com texto de ajuda explicando cada um; módulo de Estoque também relabelado ("Mínimo de estoque"). **Item 3**: `abreviar_rotulo()` (SQL) reimplementada com a mesma regra de `derivarPrefixo()` (decisão #24) em vez do corte cru dos 4 primeiros caracteres — depende da extensão `unaccent`. Nenhuma das duas RPCs de produto mudou de assinatura (diff automatizado confirmou, sem risco de overload como na `025`/`026`) | ⚠️→✅ **Incidente real, mesmo dia**: a migration `027` foi aplicada pelo usuário **antes** do frontend/backend acompanhar (só o SQL tinha sido revisado/aprovado até então) — resultado: **a página de Estoque ficou quebrada em produção** (query pra coluna que não existia mais) e criar/editar produto gravava o mínimo de estoque sempre como `1` (payload enviava a chave antiga, RPC não encontrava e caía no default), silenciosamente. Achado e corrigido na hora, como parte do fechamento de sessão: todos os pontos do código (Route Handlers de produto e de estoque, hooks, tipos gerados, formulário) atualizados pros nomes novos. **Testado com Chromium real contra a URL pública em 07/08/2026** logo em seguida: página de Estoque voltou a carregar normalmente (coluna "Mínimo de estoque" visível); produto de teste criado com variação "Oscar Albino", `quantidade_minima_estoque=3` e `quantidade_minima_venda=2` confirmados certinhos no banco; SKU gerado `TEFU-0001-OSAL` (regra por palavra funcionando — "Oscar Albino" → `OS`+`AL`, sem colidir com um eventual "Oscar" sozinho, que derivaria `OSC`). Zero erros de console. Produto de teste inativado ao final. **Lição de processo**: mesmo com o SQL revisado e aprovado à parte, uma migration aplicada sem o código correspondente já pronto para acompanhar pode quebrar produção na hora — mesma classe de risco já registrada no incidente da migration `019` (item 7, §0) |
| **Fluxo de cadastro de produto — permanece na tela após salvar** | Fecha o beco sem saída "salvar só pra poder subir foto, ser expulso pra listagem, ter que reabrir o produto". Sem migration — mudança só de frontend em `produto-form.tsx`/`use-produtos.ts`. **(1) Fica na tela após salvar produto novo**: `onSuccess` da criação não redireciona mais pra `/painel/produtos` — chama `router.replace(\`/painel/produtos/${id}\`)`, trocando a URL pra edição; a mesma página (`[id]/page.tsx`) busca o produto recém-criado e remonta `ProdutoForm` já em modo "editar", destravando a seção de imagens no lugar. Toast mudou de "Produto criado com sucesso." pra "Produto salvo — agora você pode adicionar imagens." **(2) Botão "Voltar para produtos" explícito**: substitui o ícone de seta que antes era um `Link` direto — agora intercepta o clique, mostra `ConfirmDialog` ("Há alterações não salvas. Sair mesmo assim?") só se `formState.isDirty`, senão sai direto. **Ajuste no mesmo dia, a partir de feedback do usuário revendo a tela**: o botão nasceu só no cabeçalho do formulário — em produto com várias variações/características/imagens, isso obrigava rolar a página inteira de volta pro topo só pra sair, contradizendo o próprio motivo pelo qual o Salvar já tinha ganhado barra fixa no rodapé (item 6 do lote de 07/08/2026). Replicado na mesma barra fixa, ao lado do Salvar (`justify-between` em vez de `justify-end`) — visível o tempo todo, em qualquer ponto do scroll. **(3) Salvar desabilitado até haver alteração**: `disabled = salvando || carregandoCaracteristicas || (!!produto && !isDirty)` — o gate por `isDirty` só vale a partir do momento em que o produto existe (modo editar, que já inclui o "recém-criado" via o redirect do item 1); em modo "novo" puro o botão sempre começa habilitado, sem essa trava. Na edição, cada save bem-sucedido chama `methods.reset(methods.getValues())` pra zerar o dirty state (reset pros próprios valores atuais, nada muda visualmente) até a próxima mudança | **Testado com Chromium real contra a URL pública em 08/08/2026**, 17/17 checks automatizados passando: botão "Criar produto" habilitado desde o início; após criar, URL vira `/painel/produtos/[id]` (não volta pra listagem), toast novo confirmado, card "salve o produto primeiro" some e a seção de imagens aparece no lugar (`0/10 imagens`, "Adicionar imagem") — fetch do produto recém-criado leva ~1,3–1,8s nesse instante (pequeno flash de "Carregando...", esperado pela mecânica de troca de rota, sem quebrar nada); título vira "Editar `<nome>`"; "Salvar alterações" nasce desabilitado logo após criar e reativa ao mexer em qualquer campo (testado com campo de texto **e** com `Switch` via `Controller` — "Produto ativo"); "Voltar" com alteração pendente abre o dialog de confirmação (URL não muda até confirmar), "Continuar editando" fecha sem sair, "Sair mesmo assim" navega; reabrindo o produto sem mexer em nada, Salvar nasce desabilitado e "Voltar" sai direto, sem dialog. **3 falhas do primeiro test-script investigadas e descartadas como bug real**: dois clicks encadeados sem espera (toggle do `Switch` de "Produto ativo" seguido imediato do click em "Salvar") flakaram 2 de 4 vezes com o botão preso desabilitado por 30s; diagnóstico isolado (aguardando e conferindo `aria-checked`/`disabled` passo a passo) confirmou que o mecanismo de dirty-state responde corretamente tanto via `page.goto` direto quanto via navegação client-side pelo lápis da listagem — tratado como race do script de teste (sem `wait` entre as duas ações), não do produto. Erros 404 de console vistos durante o teste são prefetch do Next.js pra `/painel/configuracoes`, `/clientes`, `/pedidos` (páginas do roadmap, ainda não construídas) — pré-existente, sem relação com esta mudança. Produtos de teste (inclusive os 3 que sobraram das rodadas flakadas) inativados ao final, zero lixo ativo em produção |
| **Busca mostra qual variação bateu (item 5, parte 2 do lote de UX)** | A parte mais urgente do item 5 (pedida em primeiro lugar pelo usuário): antes, buscar por SKU ou rótulo de variação trazia só o produto na listagem — o lojista precisava abrir o produto pra descobrir qual variação era. `GET /api/painel/produtos` (`route.ts`) passa a devolver `variacoes_encontradas` por produto quando há busca ativa: consulta os `product_variants` que batem (já existia, reaproveitada), mas agora seleciona `id/nome/sku` de cada um e calcula em JS (mesmo critério do `ILIKE` — substring case-insensitive) se o match foi no SKU, no rótulo, ou nos dois. Tabela (`produtos-table.tsx`) ganha uma faixa (`TableRow` com `colSpan`) logo abaixo da linha do produto pra cada variação encontrada, mostrando nome + SKU com o trecho digitado destacado em `<mark>` (componente novo `highlight-match.tsx`). Sem faixa quando o match foi no nome/código do **produto** (não numa variação) — só aparece quando a razão de aparecer na busca foi mesmo uma variação. Sem migration | **Testado com Chromium real contra a URL pública em 08/08/2026**, 11/11 checks automatizados passando: produto de teste com 2 variações ("Pequeno Dourado" sem SKU manual, "Grande Azulado" com SKU manual) — busca por um trecho do SKU da 2ª variação mostra a faixa certa (nome "Grande Azulado", **não** "Pequeno Dourado") com o trecho destacado em `<mark>`; busca por "Dourado" (rótulo da 1ª variação) mostra a faixa certa da 1ª, destaque no nome dessa vez; busca pelo **nome do produto** inteiro encontra o produto normalmente, mas **sem** nenhuma faixa (o match não foi em variação); filtro de status "Inativos" + busca pelo SKU de um produto **ativo** corretamente não retorna nada (confirma que a busca por variação continua respeitando o filtro de status, igual sempre foi). Zero erros de console. Produto de teste inativado ao final |
| **Filtro de categoria em árvore navegável (item 5, parte 1 do lote de UX)** | O `Combobox` de lista achatada (cada opção mostrando o caminho completo tipo "Peixes > Ciclídeos > Nanicos") virou um painel em árvore expansível dentro de um `Popover` (`categoria-tree-filter.tsx`, novo) — escala melhor com dezenas de categorias em vários níveis, deixando a hierarquia visual (indentação + expandir/recolher) em vez de uma lista longa achatada. Reaproveita 100% de `lib/category-tree.ts` (`buildTree`, `computeVisibleIds`, `getPath` — já existiam pra `/painel/categorias`, sem nenhuma mudança): campo de busca por digitação com auto-expand preservando caminho até a raiz, mesmo padrão já usado na árvore de gestão de categorias. `CategoriasTree` (componente da tela de gestão) **não foi reaproveitado** — é acoplado a 5 ações de CRUD por linha (editar/inativar/reativar/características), sem modo de seleção; o componente novo é mais simples, só seleção. Cada nó mostra um **contador de produtos** ao lado do nome — novo endpoint `GET /api/painel/produtos/contagem-por-categoria` (`category_id → total`, agrupado em JS a partir de uma única query em `products_com_status`, sem view/RPC nova — volume de produtos não justifica), respeitando o filtro de status atual da listagem (Ativos/Inativos/Todos muda a contagem). Novo primitivo `components/ui/popover.tsx` (Base UI `@base-ui/react/popover`, seguindo o mesmo padrão de `dialog.tsx`/`dropdown-menu.tsx` já existentes) — não existia no projeto até agora, reaproveitável por qualquer feature futura. Sem migration | **Testado com Chromium real contra a URL pública em 09/08/2026**, 11/11 checks automatizados passando: botão do filtro abre o painel com campo de busca + árvore + contadores; busca por termo inexistente reduz a árvore a zero categorias (só "Todas as categorias" continua visível); busca por "Ração" encontra e mantém visível a categoria certa; selecionar uma categoria da árvore atualiza a URL (`?categoria=...`) e fecha o popover automaticamente; selecionar "Todas as categorias" limpa o filtro. Confirmado visualmente por screenshot: contadores corretos por categoria (`Betas: 1`, `Ciclídeo: 1`, `Plantas: 0`, `Ração: 0`, `Todas as categorias: 3`), categorias com filhas (`Betas`, `Ração`) mostrando a seta de expandir. Zero erros de console. Teste todo read-only sobre dados já existentes — nenhum produto/categoria de teste criado, nada pra limpar |
| **Filtro de categoria busca-primeiro + drill-down (substitui a linha acima — árvore não escala além de dezenas)** | `categoria-tree-filter.tsx` (linha acima) removido, não só desativado. Substituído por `categoria-filter-popover.tsx`: o lojista nunca vê todas as categorias de uma vez. **(1) Busca** — caminho principal, casa pelo **caminho inteiro** (nome + ancestrais, não só o nome próprio — resolve lembrar só pelo ramo e nomes repetidos entre ramos), lista achatada com o caminho completo, capada em 30 resultados com aviso pra refinar. **(2) Navegação por nível** — raízes na abertura, clicar numa com filhos entra nela (só os filhos diretos, nunca a árvore inteira), breadcrumb clicável pra voltar, linha fixa "Selecionar `<nível atual>`" pra escolher um nível intermediário como filtro. **(3) Recentes** — até 5 categorias usadas por último, no topo ao abrir. Seleção vira chip removível. Contador por categoria mantido. `lib/category-tree.ts` ganhou `getPathSegments()`. Sem migration — reaproveita `useCategorias()`/contagem já existentes, filtragem toda no client | **Testado com Chromium real contra a URL pública em 10/08/2026, 28/28 checks passando** (depois de 1 bug real corrigido — `handleSelect` fechava o popover sem resetar busca/nível-atual, reabrir ficava preso no nível anterior em vez de voltar pra raiz+recentes): raízes sem filhos nem categoria inativa; drill-down em "Betas"/"Ração" mostra só filhos diretos **ativos**; busca por "Betas" encontra "Beta Azul"/"Beta Negro" via caminho (confirmado que o nome sozinho não contém a substring "Betas" — prova que bateu pelo caminho); seleção de folha e de resultado de busca sempre seleciona (nunca navega); "Selecionar Betas" aplica a categoria-pai; reabrir após selecionar não fica mais preso no nível anterior; busca de produto por nome/SKU/variação e filtro de status continuam intactos. Zero erros de console. Nenhum produto de teste criado — reaproveitou dados já existentes |
| **Vitrine Fase 1, Etapa 1 — dados de teste (catálogo de peixes ornamentais)** | Script `scripts/seed_vitrine_fase1_dados_teste.mjs` (service role, sem migration) populou o tenant `capua`: 7 subcategorias novas, 28 características, 20 produtos com 24 variações, 20 movimentações de estoque inicial, 58 valores de característica, 20 imagens placeholder — árvore real estendida sem tocar nos 3 produtos pré-existentes. Detalhe completo em §0 item 23 | Validado via RPCs públicas (`anon key`): `get_public_categories`/`get_public_products`/`get_public_product_detail` retornando o catálogo novo corretamente para o slug `capua`, isolamento de categoria inativa preservado, nenhum `saldo_estoque` numérico exposto. Manifesto de IDs em `docs/vitrine_fase1_seed_manifest.json` |
| **Sessão expirando ~1h sem renovação — diagnosticada e corrigida (Fase 1)** | Pendência aberta desde 01/08/2026. Causa raiz: `getUser()` já renova o token sozinho perto de expirar, mas só o `setAll` de quem chama decide se isso persiste no cookie — `proxy.ts` tinha `setAll` vazio (descarta sempre) e Server Components também descartam (cookie read-only nesse contexto); só Route Handlers de `/api/painel/**` persistiam. Achado no `git log -- src/proxy.ts`: o `setAll` vazio veio de um incidente de debug real em 27-28/07/2026 (padrão oficial da Supabase pra middleware foi implementado e revertido na época, sem causa raiz documentada). Confirmado no dashboard: Refresh Token Rotation **ativa** (reuse interval 10s) — o refresh descartado já consumia o token da rotação sem o navegador saber, podendo quebrar a sessão de forma menos previsível que só "1h exata". Corrigido: `proxy.ts` volta a persistir a renovação (mesmas opções de cookie do resto do app: `httpOnly, secure, sameSite=lax, path=/`), grava no `request` encaminhado e na `response`, log leve só quando um refresh de verdade acontece. Matcher **não alterado** — `/api/*` continua fora, caminho de renovação via Route Handler intocado. Comentário desatualizado em `server.ts` corrigido. Fase 2 (incluir `/api/painel/*` no matcher) fica separada, só depois desta provar estável | **Testado com Chromium real contra a URL pública em 10/08/2026**: 10/10 checks nos 2 bugs de cookie já caçados aqui antes (login preserva sessão no redirect `/entrar`→`/`, logout POST remove o cookie, deslogado `/painel`→`/entrar` preservando `?proximo=`). **Teste de renovação real**: JWT expiry baixado temporariamente pra 60s no dashboard (a pedido do usuário, ~~revertido pra 3600s depois~~ **⚠️ na verdade não foi — ficou em 60s até 21/08/2026, ver item 48**), login → esperar passar dos 60s → navegar de novo → sessão continuou logada **e** o valor do cookie mudou (renovou de verdade, não sobreviveu por coincidência) — repetido em 2 ciclos consecutivos, os dois com sucesso. Zero erros de console |
| **Bug bloqueador: checkout 401 mesmo logado — causa raiz JWT expiry preso em 60s, não código** | Diagnóstico completo (código + comportamento com Chromium real, sem tocar em nenhuma configuração até confirmar a causa) em §0 item 48. Nenhuma mudança de código — resíduo de configuração de uma sessão de diagnóstico anterior (10/08/2026) que nunca foi revertida de verdade | **Corrigido pelo usuário (JWT expiry de volta a 3600s no dashboard) e confirmado com um pedido real finalizado com sucesso pelo checkout normal**, sem 401. Reconfirmado de forma independente via probe de leitura (login de teste + token decodificado): `expires_in = 3600`. Zero resíduo de teste (contas de teste da investigação removidas) |
| **Lote de polimento de UX pós-teste manual (24/08/2026) — edição inline na Revisão do checkout + tipografia dos preços** | Revisão do checkout ganha ajuste de quantidade/remoção de item inline (reaproveita `useCarrinho`, nunca sai de `/checkout` — cidade/observação nunca se perdem). Componente novo `Preco` (`src/components/ui/preco.tsx`) separa "R$" do valor, aplicado nos dois preços "hero" (ficha de produto, Total da Revisão) no lugar da fonte decorativa da marca. Detalhe completo em `REGRAS_DE_NEGOCIO.md` §15 | **Testado com Chromium real contra a URL pública**: carrinho com 2 produtos — aumentar quantidade e remover item recalculam o total corretamente; **cidade e observação confirmadas intactas** depois dos dois ajustes; remover o último item cai no estado "carrinho vazio" já existente. Tipografia confirmada visualmente nas duas telas (símbolo menor/discreto, número em destaque na fonte de corpo). Zero resíduo (nenhum pedido de teste finalizado) |
| **Cidade de entrega sai do cadastro de cliente (24/08/2026) — item 1 da sequência pré-incremento 8** | `/cadastro` perde o campo de cidade — pertence ao checkout/`/minha-conta`, não à criação de conta (acoplamento com a modalidade de entrega do Cauã + decisão que é da compra, não do cadastro). Nenhuma migration (`delivery_city_id` já nullable, `handle_new_user` já defensivo). Detalhe completo em `REGRAS_DE_NEGOCIO.md` §14.3 e §0 item 49 | **Testado com Chromium real contra a URL pública**: formulário sem campo de cidade; cadastro real confirmado no banco com `delivery_city_id = null`; checkout do mesmo cliente pedindo cidade normalmente; `/minha-conta` intacto. Zero resíduo |
| **Painel de Pedidos do vendedor — tela + observação interna + cron (Fase 2, incremento 7, COMPLETO)** | `/painel/pedidos` (listagem, filtro por status) + `/painel/pedidos/[id]` (detalhe/validação — cliente com atalho WhatsApp, entrega, itens com estoque disponível ao lado, observações, ações). Plugado nas RPCs da migration `039` (validar/editar/cancelar/concluir) + migration `040` (`atualizar_observacao_interna_pedido`, RPC de escrita que faltava na `039`) + `.github/workflows/cancelar-pedidos-expirados.yml` (cron de hora em hora chamando `cancelar_pedidos_expirados()`). Detalhe completo em `REGRAS_DE_NEGOCIO.md` §20/§17.2 e `ESCOPO_PROJETO.md` §0 item 47 | **Testado com Chromium real contra a URL pública em 21/08/2026** (duas sessões, mesmo dia): 3 pedidos de teste via checkout real com as 4 ações (Validar/Editar/Cancelar/Concluir) confirmadas ponta a ponta incluindo baixa/devolução real de estoque no banco; observação interna (migration `040`, já aplicada) salva pela tela, persiste após reload, confirmada no banco, e confirmada nunca vazando pra `/meus-pedidos`; chamada do cron reproduzida com a mesma `anon key`/endpoint (HTTP 200, `0` cancelados). Achado e corrigido no processo: mutations não aplicavam o retorno da RPC no cache, deixando a tela demorar pra refletir o status novo — corrigido (mescla direto no cache antes do invalidate). Zero resíduo de teste no banco ao final. **Pendência real**: o disparo do workflow via `workflow_dispatch` dentro do GitHub Actions não foi executado (sem `gh` CLI/token de API liberado nesta sessão) — recomendado confirmar manualmente pela aba Actions. **Atualização de 24/08/2026, achado no teste manual completo do usuário**: botão "Voltar para pedidos" adicionado ao rodapé de `/painel/pedidos/[id]` (faltava, inclusive em pedido terminal onde a linha de ações nem renderizava antes) — testado nos dois cenários (pedido ativo/pedido concluído), zero resíduo. Ver `REGRAS_DE_NEGOCIO.md` §20 |

### Em andamento / decidido mas não implementado

| Item | Status |
|---|---|
| Dois níveis de fechamento da loja | Migration `007` **aplicada**; nenhuma UI consome `pedidos_abertos`/`mensagem_pedidos_fechados` ainda (tela de Configurações da loja é planejada, não iniciada). **📌 15/08/2026, decisão registrada com o PO (§0 item 30)**: o bloqueio do Nível 2 (`pedidos_abertos=false`) vai acontecer no momento de adicionar ao carrinho, dentro do fluxo do Carrinho (Fase 2) — não é módulo/tela à parte |
| Navegação do painel — agrupamento "Configurações" | 📐 **Decidido com o PO em 15/08/2026 (§0 item 30), execução adiada de propósito pra fase de polimento do painel, pós end-to-end.** Reúne Configurações da Vitrine/Cidades/Unidades de venda/Dados da loja/Gestão de equipe num agrupamento só, deixando o menu principal (`painel-sidebar.tsx`) restrito ao operacional. Nada a reorganizar hoje — só uma diretriz pra toda tela de configuração nova já nascer pensando nesse agrupamento futuro |
| ~~Cancelamento automático — workflow do cron~~ | **✅ Concluído em 21/08/2026** — `.github/workflows/cancelar-pedidos-expirados.yml` criado, revisado/aprovado pelo PO, commitado. Ver tabela "Concluídas" acima e §0 item 47 |
| ~~Migration `040` (observação interna do pedido)~~ | **✅ Concluído em 21/08/2026** — aplicada e testada com Chromium real. Ver tabela "Concluídas" acima e §0 item 47 |

### Planejadas (não iniciadas)

- **Produtos com variações**: CRUD de `products` + `product_variants` + `product_images` + valores de atributos, reaproveitando o padrão de CRUD e o combobox de categoria. **✅ Etapa 1 (núcleo: listagem/cadastro/edição de produto+variações) concluída em 01/08/2026 — ver tabela "Concluídas" acima.** **✅ Etapa 3 (Imagens, `product_images`) concluída em 03/08/2026** — galeria do produto (incremento 1) + fotos por variação (incremento 2), ambas testadas na URL pública. **✅ Etapa 2 (Características por produto, `product_attribute_values`) concluída em 06/08/2026** — ver tabela "Concluídas" acima. **Com isso, o módulo de Produtos (Etapas 1, 2 e 3) está 100% completo.**
- **Codificação de produtos (Código vs SKU)**: ver decisão #18 e §3 "Codificação de produtos" — prefixo por categoria, sequência por categoria, código imutável, visibilidade configurável na vitrine. **✅ Implementado e testado em 01/08/2026** junto com a Etapa 1 do CRUD de Produtos.
- **Configurações da loja**: tela para `store_settings` (dois níveis de fechamento, valor mínimo de pedido, autocadastro, regra de baixa de estoque).
- **Vitrine** (`(loja)`): catálogo público, ficha de produto, Open Graph dinâmico por produto. **Fase 0 (fundação de tenant público) ✅ completa** — ver §0 itens 19–21. **Fase 1 (telas), plano de 4 etapas registrado em §0 item 23** — Etapa 1 (dados de teste) ✅ concluída em 12/08/2026; **Etapa 2 (telas públicas: home, listagem, ficha de produto) ✅ concluída em 12/08/2026** — ver §0 item 24; **refinamento de fidelidade visual ao mockup aprovado + logo real aplicada ✅ concluído em 13/08/2026** — ver §0 item 25; **"a partir de" só com faixa de preço real ✅ concluído e migration `029` aplicada em 13/08/2026** — ver §0 item 26; **Etapa 3 (banner/selos/WhatsApp/identidade editáveis em `store_settings`) ✅ concluída e migration `030` aplicada em 13/08/2026** — ver §0 item 27; **Etapa 4 (tela de Configurações da Vitrine + rascunho/publicar + preview seguro por sessão + Open Graph dinâmico) ✅ concluída e migration `031` aplicada em 14/08/2026** — ver §0 item 28 (proposta original, preview por token — descartada) e item 29 (revisão aplicada, preview por sessão de staff, testada com sessão real de ponta a ponta). **✅ As 4 etapas do plano da Fase 1 da Vitrine estão completas.**
- **Carrinho e checkout**: valor mínimo, quantidade mínima por variação, reserva/baixa de estoque atômica no Postgres. **Pré-requisito pendente identificado em 10/08/2026** (migration `028`, Vitrine Fase 0): a RPC pública `get_public_delivery_cities(p_tenant_slug)` ainda não existe — leitura anônima de `delivery_cities` está fechada desde a `028` (mesmo padrão `SECURITY DEFINER` das outras 4 RPCs públicas), precisa ser criada como parte do desenho desta fase, antes do checkout conseguir calcular frete/área de entrega pro visitante anônimo. Ver §0 item 20 e `MIGRATIONS.md` (entrada da `028`) pro detalhe completo. **📌 15/08/2026 — plano de decisões de produto da Fase 2 registrado com o PO antes de qualquer implementação** (carrinho client-side evolutivo, quantidade/valor mínimo, estoque configurável em 2 modos faseados, entrega extensível, contas de cliente) — ver §0 item 31 e `REGRAS_DE_NEGOCIO.md` §§11–14.
- **Pedidos**: fluxo pendente → aceite (staff) → PDF → envio (WhatsApp/Evolution API, mencionado na visão original, não iniciado).
- **Importação em massa via CSV**: o catálogo real do cliente piloto tem **~1.000 itens** — cadastro manual produto a produto é inviável nessa escala. Importação de produtos/variações via CSV é planejada; fase exata a definir (provavelmente logo após o CRUD de Produtos existir).
- **Super Admin VLUMA**: camada pós-MVP, separada do painel do tenant — gestão multi-tenant (criação de novas lojas), métricas entre clientes, e consumidora do `audit_log` (decisão #15). Ainda não desenhada.
- **Manual do usuário/lojista**: entregável planejado para o final do desenvolvimento — gerado a partir de `docs/REGRAS_DE_NEGOCIO.md` (que já é escrito em linguagem clara, pensado pra isso desde a origem).
- **Documentos Legais e Aceite (LGPD)**: aceite obrigatório de Política de Privacidade e Termos de Uso no cadastro do cliente, registro do aceite (quem/quando/versão/IP) para comprovação, documentos versionados por tenant, páginas públicas linkadas no rodapé. Ver decisão #17 e `REGRAS_DE_NEGOCIO.md` §7. **Sequenciado para depois da vitrine (F4)**, já na fase de preparação para produção.
- **Módulo de Clientes**: o lojista administra os próprios clientes (ver quem comprou, histórico de pedidos). **📌 Decidido com o PO em 15/08/2026 (§0 item 30)**: entra junto com ou logo depois do módulo de Pedidos (Fase 3) — o cliente aparece no contexto de um pedido, não faz sentido como tela isolada antes disso.
- **Relatórios financeiro/vendas**: faturamento, produtos mais vendidos, etc. **📌 Decidido com o PO em 15/08/2026 (§0 item 30)**: frente futura, depois do end-to-end — só há o que relatar depois que houver vendas de verdade rodando.

---

## 5. Decisões de produto já tomadas

| # | Decisão | Status |
|---|---|---|
| 1 | Catálogo em **árvore** de categorias (N níveis via `parent_id`), não dois níveis fixos | ✅ Implementado |
| 2 | Características de produto (ficha técnica/filtro) são **configuráveis pelo lojista por categoria** (`category_attributes`), não fixas no código | ✅ Implementado (`/painel/categorias`, Sheet "Características") |
| 3 | Produto tem **variações/SKU** (`product_variants`); preço, promoção e estoque vivem na variação, não no produto | Modelado; UI ainda não existe |
| 4 | Status de produto (esgotado, em promoção, novidade) é **sempre derivado em query**, nunca uma flag manual gravada | Modelado (view `products_com_status`) |
| 5 | Loja tem **dois níveis independentes de fechamento**: `loja_aberta` (Nível 1, loja inteira inacessível) e `pedidos_abertos` (Nível 2, catálogo visível mas checkout bloqueado) | ✅ Aplicado no banco (migration `007`); UI de configuração ainda não existe |
| 6 | **Soft delete universal**: nenhuma entidade do painel tem exclusão real; sempre campo `ativo` + filtro Ativos/Inativos/Todos | ✅ Implementado no padrão de CRUD |
| 7 | **Modal é o padrão** para toda criação/edição no painel; nunca página separada | ✅ Implementado |
| 8 | Inativar categoria com filhas ativas **cascateia** a inativação pela subárvore, marcando quem foi arrastado (`inativado_em_cascata`) pra permitir restaurar só essas na reativação; quem já estava inativo por conta própria não é tocado em nenhum dos dois sentidos. Ligado à decisão #15: é justamente esse tipo de ação (uma escrita que afeta N linhas por consequência de uma decisão sobre 1) que o `audit_log` vai existir para explicar depois | ✅ Implementado (migration `011` + RPC `set_category_ativo_cascade`) |
| 9 | Vitrine (quando construída) deve esconder da navegação uma categoria inativa **e toda a sua subárvore** | Registrado como requisito futuro (comentário em `src/lib/category-tree.ts`); nada a implementar ainda (vitrine não existe) |
| 10 | Slug é gerado automaticamente a partir do nome (editável) — na entrega final ao lojista, o campo de slug deve ficar **oculto/secundário** na interface (detalhe técnico de URL não deve poluir o cadastro do dia a dia de quem não é técnico) | Decidido; hoje o campo aparece visível e editável no modal de Categoria (uso interno/DEV) — ocultar é um ajuste de UX pendente antes da entrega ao cliente final |
| 11 | Arquitetura multi-tenant desde a primeira tabela, tenant fixo no MVP | ✅ Implementado (`tenant_id` + `current_tenant_id()` + RLS) |
| 12 | Sessão em cookie `httpOnly`; toda mutation autenticada do painel passa por Route Handler, nunca client Supabase direto do browser | ✅ Implementado, é o padrão fixado |
| 13 | Perfis de acesso: Admin (total) e Operador (cadastros + permissão granular `pode_aceitar_pedido`) | ✅ Modelado (`profiles.role`, `pode_aceitar_pedido`) |
| 14 | Anti-ciclo em árvores (categoria não pode ter como pai um descendente dela mesma) validado **no client** (combobox exclui opções) **e no servidor** (defesa em profundidade) | ✅ Implementado |
| 15 | **Auditoria** (`audit_log`): toda escrita do painel passa a chamar `registrarAuditoria()` na aplicação (não trigger de banco), pra capturar contexto de negócio (ex.: qual usuário, se foi cascata). Uso: rastreabilidade do Super Admin VLUMA, não do admin do tenant. Candidata a feature premium | 📐 Decidido; não implementado — ver §2, "Auditoria" |
| 16 | **Vocabulário de interface**: telas voltadas ao lojista usam "Características" (nunca "atributos") e "Variações" (nunca "SKU"/"variants") — termos técnicos ficam só no código/banco. Decisão de UX baseada em pesquisa de mercado (Nuvemshop/Shopify) | ✅ Já seguido no CRUD de Categorias (rótulo "Características"); aplicar também ao CRUD de Produtos — ver `REGRAS_DE_NEGOCIO.md` §4.1 |
| 17 | **Documentos Legais e Aceite (LGPD)**: aceite obrigatório (checkbox + links) de Política de Privacidade e Termos de Uso no cadastro do cliente; aceite registrado com quem/quando/versão/IP para comprovação; documentos **versionados por tenant** (nova versão pode exigir re-aceite); páginas públicas linkadas no rodapé; texto-base gerado por IA **com ressalva explícita de revisão jurídica obrigatória antes de produção**. Sequenciado para depois da vitrine (F4) | 📐 Decidido; não implementado — ver `REGRAS_DE_NEGOCIO.md` §7 |
| 18 | **Código do Produto**: identificação/referência do produto, distinta do SKU (que é da variação, para estoque). Prefixo vem da **categoria** (`categories.prefixo_codigo`, derivado do nome se vazio, ex. "Ciclídeos" → `CIC`, único por tenant). Formato `PREFIXO-NNNN`, sequência própria por categoria (cada uma começa do zero). Gerado na criação do produto, **imutável** mesmo que o produto mude de categoria. Lojista escolhe automático ou manual (editável — migração de outro sistema). Toggle `codigo_visivel` controla se aparece e é buscável na vitrine | ✅ **Implementado e testado com Chromium real em 01/08/2026** — migrations `015`/`016`/`017` aplicadas; telas de criação e edição do CRUD de Produtos (Etapa 1) concluídas — ver §3 "Codificação de produtos", tabela "Concluídas" em §4 e `REGRAS_DE_NEGOCIO.md` §4.6. **Revisado em 04/08/2026 — ver decisão #25**: o modo "automático" original (prefixo da categoria) não foi removido, virou o modo explícito "Herdar da categoria"; um novo modo "Automático" (padrão) passou a derivar o prefixo do **nome do produto**. |
| 19 | **Padrão de mensagens de erro**: toda mensagem voltada ao usuário final é em português claro, orienta a ação, nunca expõe jargão técnico (código de erro, nome de constraint/tabela, "RPC"). Detalhe técnico só em log de servidor. Vale para todas as features, não só Produtos | ✅ Decidido e em vigor a partir de 30/07/2026 — ver §2 "Padrão de mensagens de erro" e `REGRAS_DE_NEGOCIO.md` §9 |
| 20 | **Gap de isolamento multi-tenant em RLS**: policies de escrita staff (`*_staff_write`) checam só `is_staff()`, não `tenant_id` — staff de um tenant teoricamente acessaria dados de outro. Inofensivo hoje (1 tenant só); precisa ser corrigido antes do 2º tenant | ⚠️ Identificado, não corrigido — ver §2. **Revisado em 31/07/2026**: decidido corrigir AGORA, de forma completa, antes de seguir com Produtos — ver decisão #21 (levantamento completo de tabelas/policies/RPCs feito em sessão, plano apresentado, implementação ainda pendente de aprovação) |
| 21 | **Isolamento total por tenant priorizado antes de Produtos**: pausar o CRUD de Produtos (Etapa 1) e corrigir primeiro TODAS as policies de RLS e RPCs `SECURITY DEFINER` do projeto para restringir leitura/escrita por `tenant_id = current_tenant_id()`, não só `is_staff()`/`is_admin()`. Migrations `015`/`016` (Código do Produto, renumeradas de `013`/`014`) ficam em espera — ver §6 — até essa base estar pronta | 📐 Decidido em 31/07/2026; migration `013_rls_tenant_isolation.sql` aplicada. **Teste real com o tenant sintético encontrou um SEGUNDO vazamento** (leitura cross-tenant via branch pública das policies de SELECT, não coberto pela `013` — que só tratava escrita/leitura privilegiada) — corrigido em `014_rls_select_anon_authenticated_split.sql` (✅ **aplicada e validada em 31/07/2026** — reteste com o canário confirmou `categorias_do_caua_que_vejo = 0`, era 6 antes). Ver detalhe completo em §2 e §6. **Com o isolamento provado, as migrations `015`/`016` (Código do Produto) também foram aplicadas e validadas em 31/07/2026** — nenhuma migration pendente no momento; próximo passo é implementar o CRUD de Produtos (Etapa 1) em cima da base já isolada. |
| 22 | **Estratégia de evolução para SaaS**: isolar tenant agora → desenvolver Cauã sobre a base isolada → Cauã completo vai a produção como tenant único → clonar código+banco pra abrir a fase SaaS (Cauã congela, só bugfix compartilhado) → SaaS pronto decide se migra o Cauã pra plataforma ou mantém standalone | 📐 Decidido em 31/07/2026 — ver §1 "Estratégia de evolução para SaaS" |
| 23 | **Sufixo de desempate do prefixo de código**: quando o prefixo derivado automaticamente de uma categoria colide com o de outra já existente (mesmo tenant), o sistema desempata colando um número de 4 dígitos ao prefixo (`BET`, depois `BET0001`, `BET0002`...) em vez de um incremento simples de 1 dígito. ~~Mantém a derivação em si simples (3 primeiras letras do nome inteiro, sem dividir por palavras) — toda a responsabilidade de evitar colisão fica no sufixo, não numa regra de derivação mais elaborada~~ **Revisto no mesmo dia (01/08/2026)**: decisão trocada pela combinação das duas coisas — ver decisão #24 logo abaixo. O sufixo de 4 dígitos continua existindo como rede de segurança, mas agora é o segundo nível de defesa, não o único | ✅ Implementado e testado com Chromium real em 01/08/2026 — ver linha "Fix da colisão de prefixo..." na tabela "Concluídas" acima. **Revisto** — ver decisão #24 |
| 24 | **Derivação de prefixo por divisão de palavras (revisão da decisão #23)**: em vez de derivar sempre as 3 primeiras letras do nome inteiro, `derivarPrefixo()` agora considera o número de palavras do nome (ignorando conectores curtos: de/da/do/e/em/com) — 1 palavra → 3 primeiras letras (`Ciclídeos`→`CIC`, sem mudança); 2 palavras → 2 primeiras letras de cada (`Beta azul`→`BEAZ`); 3+ palavras → 2 letras da 1ª palavra + 1ª letra de cada palavra seguinte, até 4 caracteres (`Ração Filhotes Premium`→`RAFP`). Reduz a chance de colisão entre categoria-mãe e subcategoria com nome parecido (o caso real que motivou a correção: "Betas"→`BET` e "Beta Azul", que antes também derivava `BET`, agora deriva `BEAZ`) — mas não é garantia absoluta, então o sufixo numérico da decisão #23 continua como rede de segurança pros casos raros que ainda colidirem | ✅ Implementado em `src/lib/produto-codigo.ts` (`derivarPrefixo`) em 01/08/2026, build local validado. Categorias já autocuradas antes desta revisão (`Beta Azul`=`BET0001`, `Beta Negro`=`BET0002`, `Ração de Superfície`=`RAC0001`) **não são recalculadas automaticamente** — só ganhariam o prefixo "por palavras" se o lojista editar o campo prefixo manualmente de novo |
| 25 | **Código automático de produto passa a derivar do NOME DO PRODUTO, não mais da categoria por padrão (revisão da decisão #18, reaproveitando `derivarPrefixo()` da decisão #24 sem nenhuma mudança na função)**: o cadastro de produto ganha um terceiro modo — agora são 3, não 2 — "Automático" (novo padrão, prefixo do nome do produto, ex. "Ração de Fundo Pyotar" → `RAFP-0001`), "Herdar da categoria" (o modelo antigo da decisão #18, prefixo da categoria, ex. `RAC-0001`, preservado como opção explícita) e "Manual". Sequência própria por **prefixo** (não por categoria nem por produto) numa tabela nova (`product_code_sequences`, migration `024`) — dois produtos com nomes diferentes que derivam o mesmo prefixo dividem a sequência (`RAFP-0001`, `RAFP-0002`), o sufixo numérico de 4 dígitos (decisão #23) continua sendo a rede de segurança contra colisão. Nenhuma mudança em `criar_produto_com_variacoes`/`atualizar_produto_com_variacoes` (016/017): as duas RPCs já recebiam o código pronto como string, indiferente de como foi decidido — só uma RPC nova (`gerar_codigo_produto_por_prefixo`) e o Route Handler de criação, que passou a computar o prefixo do nome no servidor (nunca confiando em valor vindo do client). Continuam valendo: código imutável após criado, `codigo_visivel`, mensagens amigáveis | 📐 Decidido em 04/08/2026, ✅ **implementado e testado com Chromium real contra a URL pública em 05/08/2026** — ver linha "Código do produto — modo automático por nome + busca por SKU/variação" na tabela "Concluídas" abaixo para o detalhe completo do teste |
| 26 | **Os dois níveis de fechamento da loja (decisão #5) se resolvem dentro do Carrinho (Fase 2), não como módulo à parte**: Nível 1 (`loja_aberta=false`) já funciona (só mensagem, sem catálogo). Nível 2 (`pedidos_abertos=false`) vai bloquear no momento de **adicionar ao carrinho**, mostrando `mensagem_pedidos_fechados` — o catálogo continua 100% navegável antes disso. Cobre o padrão real do Cauã (venda/entrega em ciclo mensal determinado) sem precisar de lógica de datas | 📐 Decidido com o PO em 15/08/2026 — ver §0 item 30 e `REGRAS_DE_NEGOCIO.md` §2 |
| 27 | **Navegação do painel vai ganhar um agrupamento "Configurações"** (Configurações da Vitrine, Cidades, Unidades de venda, Dados da loja, Gestão de equipe), deixando o menu principal só com o operacional — padrão de SaaS, evita menu poluído. Execução **adiada de propósito** pra fase de polimento do painel, pós end-to-end | 📐 Decidido com o PO em 15/08/2026 — ver §0 item 30. Não implementado, sem prazo definido |

---

## 6. Ambientes e referências

| Item | DEV (atual) | PRD |
|---|---|---|
| Repositório | `VlumaOficial/ecommercecauadev` | `VlumaOficial/ecommercecaua` (ainda não criado) |
| Domínio | `ecommercecauahml.vluma.com.br` | a definir |
| Supabase ref | `embgxkrfwtbqfkwmquvo` | a definir |
| Deploy | Vercel, deploy automático no push pra `main` | a configurar |

**Pasta local:** `/home/sdorea/vluma/caua` (filesystem nativo WSL — instalação em `/mnt/c/...` é ~1500x mais lenta, lição registrada em `VISAO_CAUA.md`).

**Migrations**: versionadas em `supabase/migrations/`, **aplicadas manualmente pelo usuário via SQL Editor do Supabase** (nunca via `supabase db push` nem CLI a partir do assistente — a conta do CLI usada neste ambiente não tem acesso ao projeto). Numeração sequencial `NNN_nome.sql`, idempotentes (`if not exists`/`if exists` sempre que possível).

**Geração de tipos** (`npm run types` → `supabase gen types typescript`): indisponível neste ambiente (mesma limitação de acesso do CLI). `src/types/database.ts` é **editado à mão** após cada migration aplicada — sempre conferir que reflete exatamente o schema real.

### Estado das migrations

> **Ver `docs/MIGRATIONS.md`** para o mapa completo migration a migration (001–018, todas aplicadas e validadas), incluindo a explicação do número `014` que não existe na sequência original (renumeração da feature Código do Produto — nenhum conteúdo perdido, só o arquivo pulou de número).

| # | Arquivo | Aplicada? |
|---|---|---|
| 001–006 | núcleo, catálogo v1, RLS, cidades, provisionamento de usuário | ✅ |
| 007 | `store_settings_fechamento` (dois níveis de fechamento) | ✅ |
| 008 | `delivery_cities` ganha `DEFAULT current_tenant_id()` | ✅ |
| 009 | `catalog_v2` (árvore + atributos + variações) | ✅ |
| 010 | `categories` ganha `DEFAULT current_tenant_id()` | ✅ |
| 011 | `categories` ganha `inativado_em_cascata` + RPC `set_category_ativo_cascade` (cascata) | ✅ |
| 012 | `category_attributes` ganha `DEFAULT current_tenant_id()` | ✅ |
| 013 | `rls_tenant_isolation` — isolamento total por tenant: todas as policies de leitura privilegiada/escrita staff (tenants, profiles, customers, store_settings, categories, category_attributes, products, product_variants, product_attribute_values, product_images, product_price_history, delivery_cities) ganham `and tenant_id = current_tenant_id()`; corrige a RPC `set_category_ativo_cascade` (mesmo gap). Ver decisão #21 | ✅ Aplicada em 31/07/2026 |
| 014 | `rls_select_anon_authenticated_split` — corrige o SEGUNDO vazamento encontrado ao testar a `013` com o tenant sintético: as policies `*_select_public` valiam pra `anon` e `authenticated` com a mesma condição, e a branch pública (`ativo = true` / `using(true)`, sem tenant) vazava leitura cross-tenant pra qualquer sessão autenticada (staff e cliente logado). Separa em policy `_select_anon` (mantém aberta, pensada pra vitrine) e `_select_authenticated` (sempre `tenant_id = current_tenant_id()`) nas 8 tabelas afetadas: categories, category_attributes, products, product_variants, delivery_cities, store_settings, product_attribute_values, product_images. Ver decisão #21 | ✅ **Aplicada e validada** em 31/07/2026 — preenche o número que tinha ficado vago na renumeração abaixo |
| 015 | `products`/`product_variants` ganham `DEFAULT current_tenant_id()` | ✅ **Aplicada e validada** em 31/07/2026. **Renumerada de `013` para `015` em 31/07/2026** — ver nota de rastreabilidade abaixo. |
| 016 | Código do Produto: `categories.prefixo_codigo`, `products.codigo`/`codigo_visivel`, `category_code_sequences`, RPCs `gerar_codigo_produto` e `criar_produto_com_variacoes` | ✅ **Aplicada e validada** em 31/07/2026 (corrigida antes da aplicação: `products_com_status` trocou `CREATE OR REPLACE VIEW` por `DROP`+`CREATE`, ver `docs/MIGRATIONS.md`). **Renumerada de `014` para `016` em 31/07/2026**; policy de `category_code_sequences` ajustada para já nascer com o filtro de tenant embutido (sem precisar de migration de correção posterior). Ver nota de rastreabilidade abaixo. |
| 017 | Edição atômica de produto+variações: RPC `atualizar_produto_com_variacoes` (simétrica à `criar_produto_com_variacoes`, `codigo` nunca no payload, variações removidas viram soft delete) + SKU automático das variações (`abreviar_rotulo`, `gerar_sku_variacao`) + revisão de `criar_produto_com_variacoes` (016, já aplicada) via `create or replace` para ganhar o mesmo SKU automático — extensão de comportamento, não descarte. Ver `REGRAS_DE_NEGOCIO.md` §4.4 | ✅ **Aplicada e validada com Chromium real** em 01/08/2026 — testada via edição de produto (variação nova + variação removida com soft delete confirmado, código imutável confirmado) |
| 018 | **Fecha o TERCEIRO vazamento cross-tenant** (decisão #21), desta vez numa view, não numa policy: `products_com_status` foi criada sem `security_invoker = true`, ignorando o RLS e vazando produtos entre tenants na listagem `/painel/produtos` (a edição, que lê a tabela direto, isolava certo). Correção: `alter view ... set (security_invoker = true)`. Única view do projeto — nenhuma outra a corrigir. Ver §2 e `docs/MIGRATIONS.md` | ✅ **Aplicada e validada** em 01/08/2026 — `reloptions` confirmado `security_invoker=true`; reteste de isolamento com o canário confirmou `de_outro_tenant=0` |

**Nota de rastreabilidade (regra de §0)**: as migrations descritas como `013`/`014` nas sessões de 30/07/2026 (Código do Produto) foram **renumeradas para `015`/`016`** em 31/07/2026, sem perda de conteúdo — só o número do arquivo mudou (via `git mv`) e a policy de `category_code_sequences` ganhou o filtro de tenant diretamente (em vez de precisar de uma correção posterior). O motivo: decidiu-se tratar o isolamento total por tenant (decisão #21) *antes* de aplicar qualquer coisa relacionada a Produtos, e esse isolamento precisava do número `013` por ser cronologicamente anterior. Nenhuma decisão de produto foi descartada — só a numeração dos arquivos `.sql` ainda não aplicados. **Atualização de 31/07/2026**: o número `014`, que tinha ficado vago nessa renumeração, foi preenchido pela migration `014_rls_select_anon_authenticated_split.sql` (o segundo vazamento encontrado no teste de isolamento) — ver `docs/MIGRATIONS.md` pra confirmação de que isso não é um arquivo "reaproveitado" por engano, é uma migration nova e distinta que só coincide de usar o número que estava livre.

### Variáveis de ambiente

```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
NEXT_PUBLIC_TENANT_SLUG
SUPABASE_SERVICE_ROLE_KEY      (servidor apenas — client em src/lib/supabase/admin.ts, ignora RLS, nunca no browser)
```

Configuradas na Vercel e em `.env.local` (fora do Git).

---

## 7. Estrutura do projeto (atual)

```
src/
├── app/
│   ├── (auth)/           entrar, cadastro, recuperar-senha, nova-senha
│   ├── (loja)/           vazio — vitrine ainda nao construida
│   ├── api/
│   │   ├── auth/login/   POST — login (303 + Set-Cookie atomico)
│   │   └── painel/       Route Handlers de CRUD (cidades, categorias + caracteristicas/reorder,
│   │                     produtos + [id] + codigo-sugerido — GET/POST/PATCH)
│   ├── auth/callback/    callback de confirmacao de e-mail / magic link
│   ├── painel/           layout protegido (so staff) + cidades/ + categorias/ + produtos/ (novo/, [id]/)
│   └── sair/             POST — logout
├── components/
│   ├── painel/
│   │   ├── crud/         StatusFilterTabs, SearchInput, StatusBadge, ConfirmDialog, FormDialog
│   │   ├── cidades/
│   │   ├── categorias/   arvore, form (com prefixo_codigo), Sheet+lista+form de Caracteristicas (drag-and-drop)
│   │   └── produtos/     produtos-view/table, produto-form (3 blocos: dados-basicos,
│   │                     codigo-section, variacoes-section)
│   └── ui/                20 componentes shadcn (preset Base UI) + combobox.tsx, sheet.tsx (custom/reaproveitado)
├── hooks/                 use-cidades, use-categorias, use-caracteristicas, use-delivery-cities,
│                          use-produtos, use-query-param-state
├── lib/
│   ├── supabase/          client.ts (browser), server.ts (SSR/Route Handlers), admin.ts (service_role)
│   ├── auth.ts             getStaffProfile()
│   ├── category-tree.ts   buildTree, getDescendantIds (anti-ciclo), computeVisibleIds, getPath, slugify
│   ├── produto-codigo.ts  derivarPrefixo, formatarCodigo
│   └── tenant.ts           resolucao de tenant (hoje constante)
├── types/database.ts       tipos gerados/ajustados a mao do schema Supabase
└── proxy.ts                somente leitura: redireciona /entrar <-> /painel

supabase/
├── migrations/             001 a 018, ver secao 6 (todas aplicadas e validadas)
└── tests/                  isolamento_test.sql (script de verificacao RLS, nao e migration)

docs/                        VISAO_CAUA.md (visao original) + este documento + REGRAS_DE_NEGOCIO.md + MIGRATIONS.md
```

**Nota de 10/08/2026 — a árvore acima é uma fotografia antiga (parou em `018`/"tenant.ts hoje constante"/"proxy.ts somente leitura"), não atualizada linha a linha desde então de propósito (baixo valor manter uma ASCII art em sincronia perfeita a cada migration). Descrição atual de `proxy.ts`/`tenant.ts`/`supabase/tests/`, pra quem ler isto depois**: `src/proxy.ts` hoje faz três coisas, nessa ordem — renovação de sessão (Fase 1, item 18), os dois redirects de auth (`/entrar`↔`/painel`, inalterados desde sempre), e resolução de tenant por host pra Vitrine (Fase 0, item 21, só no caminho de "passa direto", nunca redireciona por causa de tenant). `src/lib/tenant.ts` não é mais só uma constante — `getTenantFromHeaders()` lê o tenant resolvido pelo proxy via header, pra uso futuro da Vitrine. `supabase/tests/` tem dois scripts agora: `isolamento_test.sql` (canário de RLS, `_teste_isolamento`) e `vitrine_isolamento_test.sql` (novo, tenant dedicado `_teste_vitrine`, item 19). Migrations vão até `028` (ver `MIGRATIONS.md` pro mapa completo e atualizado).

### Débito técnico conhecido

- ~~`src/app/(auth)/entrar/entrar-action.ts` — Server Action de login de uma iteração anterior do fluxo de auth, não usada.~~ **Removida em 29/07/2026** (confirmado sem nenhuma referência no código antes de apagar; build seguiu passando).

---

*Este documento substitui o `docs/VISAO_CAUA.md` como referência de estado atual — o `VISAO_CAUA.md` permanece como registro histórico da concepção inicial do projeto (F1), não é mais atualizado.*
